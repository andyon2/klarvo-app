# Environment
.env

# Rust
target/
src-tauri/target/

# Node
node_modules/
dist/
dist-ssr/
*.local

# Logs
*.log
npm-debug.log*

# Tauri
src-tauri/gen/

# Android
android/.gradle/
android/build/
android/app/build/
android/jni/build/
*.apk
*.aab

# IDE
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*.suo
*.ntvs*
*.njsproj
*.sln

# OS
.DS_Store
Thumbs.db

# Generated assets
social-preview.png

# Whisper models (large files)
models/
*.bin
voxlit-debug.keystore
.dev-keys/
src-tauri/null

# Agent framework + cache (large / transient; never commit)
# Exception: _bmad/custom/ is our team override layer — committed so our
# customizations survive `bmad update` and stay reviewable. (*.user.toml stays
# local via _bmad/custom/.gitignore.) The disposable base install stays ignored.
_bmad/*
!_bmad/custom/
.claude/
shells/
_bmad-output/runtime/
_bmad-output/story-automator/
.claude/.story-automator-active

# Design handoff package (local only — contains screenshots with personal dictation content)
design-handoff/

# Epic-Conductor run lock (logical run marker; never committed — Naht 4, Postmortem 2026-06-15)
.conductor-lock

# yt-scout research output (transcripts, ~110 MB videos, picked frames, report.md — local only)
yt-scout/
