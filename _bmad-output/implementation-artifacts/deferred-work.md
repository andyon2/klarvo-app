# Deferred Work

Items that were identified but not actioned. Each entry includes the source review and rationale.

## Deferred from: Epic-3 code review (2026-04-25)

- **F1 — `tauri-plugin-global-shortcut` Version-Pin spot-check** — Workspace `Cargo.toml` außerhalb des Review-Diff-Range; Spec verlangt exact-Pin analog ADR-0002. Externally verifizieren.
- **F2 — Cargo.lock `image`-Crate Supply-Chain-Notiz** — `image-png`-Feature zieht größeres Surface ein (transitiv via tauri/tray-icon). Pre-existing; ADR-Note nice-to-have.
- **F3 — `app.manage(Arc<SessionOrchestrator>)` doppeltes Arc-Wrapping** [`shells/windows/src-tauri/src/main.rs:878`] — Tauri::State liefert Arc-Semantik bereits; redundant aber funktional. Phase-2-Cleanup.
- **F4 — `drop(pipeline_task)` graceful-Shutdown** [`klarvo-shell-orchestrator/src/session.rs:208`] — Explizit als Phase-2-TODO in Story. *D5-Cross-Ref (commit 95a96e1):* 3-State-Lifecycle (`RecordingStarted`/`Stopped`/`Completed`) liegt jetzt als State-Machine-Foundation vor; Graceful-Shutdown kann auf `RecordingCompleted` warten oder `pipeline_task.abort()` rufen. F4 selbst (await/abort-on-exit) bleibt offen.
- **F5 — Story 3.11 AC-I Scope-Fence per-commit verification** — Combined-Diff verbirgt Per-Commit-Scope; via `git show 4603b87 --stat` separat verifizieren.
- **F6 — `setup_closure_types_compile` no-op assertion** [`shells/windows/src-tauri/src/main.rs:978-993`] — Compile-only Function-Definition; Standard-Rust-Pattern, kein echter Schaden.
- **F7 — `ShortcutState` non_exhaustive Robustness** [`shells/windows/src-tauri/src/hotkey.rs:631`] — Future-Plugin-Upgrade-Coverage; niedrige Priorität.
- ~~**F8 — `MockVadProvider` queue-size brittleness**~~ — **OBSOLET durch D1-Resolution (RmsVad-Refactor):** energy-basiert statt queue-basiert, kein Exhaustion-Pfad mehr.
- **F9 — `wait_for_delivery` busy-poll vs notify** [`klarvo-shell-orchestrator/tests/e2e_test.rs:204-215`] — Test-Ergonomie; gering bei 5s-SLA.
- ~~**F10 — TODO(de)-Prefix in production locale**~~ — **OBSOLET durch Story 4.3 (Translation-Pass).** Alle 13 Bestand-Keys haben jetzt finale deutsche Strings.

## Deferred from: Epic-4 code review (2026-04-25)

- **F11 — TOML-Type-Mismatch UX** [`shells/windows/src-tauri/src/config.rs:103-116`] — `parse_from_str` aliased Type-Mismatch (`ui_language = 42`) auf `error.config.missing` ("Configuration file not found"). Sauberer Fix bräuchte neuen Key `error.config.invalid_type` + Match auf TOML-Error-Patterns. Pre-existing aus Story 3.2-Branch-Logic; Phase-2-Settings-UI eliminiert das Problem strukturell.
- **F12 — `both_locale_files_valid_json_even_when_en_active` Regression-Guard** [`shells/windows/src-tauri/src/i18n.rs:189-197`] — Test prüft Happy-Path statt corrupt-DE-Regression. Echter Test bräuchte `load_from_strs(ui_language, en_json, de_json)`-Extraktion mit Inject-Fixture. `DE_JSON` ist aktuell `include_str!`-statisch.
- **F13 — Symmetric TODO-Marker-Test für `de.json`** [`shells/windows/src-tauri/src/i18n.rs::tests`] — `no_todo_markers_in_en_json` schützt nur EN-Master. Re-Introduction-Risiko niedrig (EN-Master + `de_json_covers_same_key_set` fangen primäre Drift); symmetrische Coverage wäre 5-Zeilen-Test.

## Deferred from: Epic-5 code review (2026-04-26)

- **F14 — verify-release rustup-vs-target-Distinktion** [`xtask/src/verify_release.rs:212-219`] — Bei fehlendem `rustup` wird "target not installed" gemeldet statt "rustup not found". Lower-priority — Dev-Experience.
- **F15 — verify-release Windows CRLF auf `rustup target list`** [`xtask/src/verify_release.rs:251-260`] — `lines()` handled CRLF, aber `trim()` fehlt explizit. False-Positive-Risk auf Windows-Hosts.
- **F16 — `cargo check` ≠ Build (kein NDK-Linker im Check)** [`xtask/src/verify_release.rs:222-247`] — Gate ist green ohne Link-Path-Validation. Story 5.4 Technical-Notes erwähnen das als bewusste CI-Speed-Wahl; Phase-2 könnte `--target`-Build ergänzen.
- **F17 — Shared `target/`-Dir Race bei concurrent xtask-Runs** [`xtask/src/verify_release.rs:222-247`] — Cross-Compile poisons incremental-state. CARGO_TARGET_DIR-Isolation wäre Phase-2.
- **F18 — xtask Cargo-Features baken Phase-1-Stages ein** [`xtask/Cargo.toml:11-13`] — `stage-passthrough/stt/cleanup` hardcoded; Phase-2 stage-llm würde nicht erkannt. Re-evaluieren bei Stage-Erweiterung.
- **F19 — G3-A `is_excluded_g3` Scope-Inkonsistenz** [`xtask/src/lint_events.rs:545`] — Excludiert `android/`, nicht `klarvo-bridge-jni` oder deeper-nested `target/`. Pre-existing Convention-Drift.
- **F20 — G3-C Heuristic-Edge-Cases** [`xtask/src/lint_events.rs:466-507`] — Nested wildcard-arms, `use PipelineStageType as Pst`-Aliase. Story 5.3 Technical-Notes flag das explizit als Heuristik-Toleranz.
- **F21 — G3-B Locale-JSON nested/BOM/CRLF/Duplicate-Keys** [`xtask/src/lint_events.rs:340-357`] — Aktuelle Locale-Files sind flat JSON ohne BOM. Future-format-shifts würden break.
- **F22 — G3-B Forward-Drift fängt nur Literal-Keys** [`xtask/src/lint_events.rs:149`] — `format!("error.{}.network", ...)` und Const-Imports werden nicht erfasst. Pre-existing-Limitation; Story 5.3 Technical-Notes-akzeptiert.
- **F23 — G3-A `extract_string_literal_from_expr` recurses ohne Path-Validation** [`xtask/src/lint_events.rs:355`] — `Some(SomeOtherCtor("x"))` würde "x" extrahieren. Niedrig: nur `String::from`/`Cow::*`/`.into()` kompiliert für `Option<Cow<'static, str>>`-Field. Defer-Patch: Path-Segment-Allowlist.
- **F24 — manifest-strict missing fixture-File silent inconsistency** [`xtask/src/manifest_strict.rs:75`] — `continue` ohne Counter-Decrement; `total = expected.len()`. Final-Count `N-1/N` ohne klaren Cause. Phase-2 sauberer Fail-Path.

## Deferred from: code review of story-2a-A4 (2026-04-29)

- **A4-D1 — `set_raw` emit-after-DB-write Reorder-Risk** [`klarvo-core/src/settings/mod.rs:170-180`] — Mutex-Drop vor Emit; theoretisch Multi-Thread-Setter Reorder-Race. In Single-User-UI nicht realistisch.
- **A4-D2 — Plugin-emit failure swallowed (warn + continue)** [`shells/windows/src-tauri/src/commands/settings.rs:67-69`] — Tauri-Emit-Failure rare; kein Retry, kein Backpressure-Signal an Frontend. MVP-akzeptabel.
- **A4-D3 — `SettingsChangedEvent.new_value` als plain String — keine Type-Info im Payload** [`klarvo-core/src/settings/mod.rs:34`] — Schema unterstützt 4 Typen (`string|i64|bool|json`); Event-Payload droppt Type. Aktuell alle 5 Core-Fields strings. Plugin-i64/bool ist Phase-2-B+.
- **A4-D4 — `type`-Spalte ist dekorativ — keine Roundtrip-Validation auf value** [`klarvo-core/src/settings/mod.rs:154-162`] — `get_raw` validiert nur dass `type` aus 4-Tuple ist, prüft nicht dass `value` zum `type` passt. Strings only aktuell; Future-Phase-Issue.
- **A4-D5 — Multi-Process SQLITE_BUSY Risk** [`klarvo-core/src/settings/mod.rs:74-86`] — Kein `PRAGMA journal_mode=WAL`, kein `PRAGMA busy_timeout`. Single-Instance-Annahme; bei zwei parallelen Klarvo-Prozessen → SQLITE_BUSY. Backlog Multi-Window/Tray-Doubleclick.
- **A4-D6 — Schema-Migration v1 syntax error blockt zukünftige Migrationen** [`klarvo-core/src/settings/migrations.rs:19-26`] — Pure Forward-Migrations ohne Down-Path und ohne Schema-Drift-Check. Theoretisch Future-Pfad-Blocker.
- **A4-D7 — AppError Display impl Debug-Repr; `#[non_exhaustive]` AppErrorKind serde** [`klarvo-core/src/error.rs`] — `Display` nutzt `{:?}: {}`; im Frontend bei rolling-update kann Frontend unbekannten `kind`-String empfangen. Backwards-compat / log-format-issue.
- **A4-D8 — Form-State-Race auf schnellen Edits (kein Dirty-Tracking, kein Unsaved-Warning)** [`shells/windows/src/index.html:415-430`] — User-Edits NACH Save-Click gehen verloren ohne Notice. Spec-AC-9 verlangt kein Unsaved-Warning. UX-Polish-Backlog.
- **A4-D9 — SettingsChangedEvent-Subscription fehlt komplett im Frontend** [`shells/windows/src/index.html:386-481`] — Backend emittet `settings.changed`, Bindings exportieren `events.settingsChanged`, aber Panel subscribed nirgendwo. Single-Window-Fall aktuell unkritisch; A8-Sub/C2/C3 holen das Listening nach.
- **A4-D10 — HTML-Panel selbst nicht i18n-übersetzt (`lang="en"`, "Klarvo Settings" hardcoded)** [`shells/windows/src/index.html:305,449`] — Panel ist Phase-2-A-Minimal-Implementation; volle i18n-Integration in Phase-2-B Vite+React-Migration.

## Deferred from: code review of story-2a-A4 (Pass-2, 2026-04-29)

- **P2-W1 — `CORE_PREFIXES` reserviert `"license."` + `"history."` ohne Doc-Comment** [`klarvo-core/src/settings/mod.rs:393`] — Premature Reservation; Plugin-Author bekommt confusing Reject ohne Erklärung. Doc-Update genügt; ADR-pending.
- **P2-W2 — `migration_does_not_block_when_only_plugin_rows_exist`-Test verifiziert nicht "block-detection"** [`klarvo-core/src/settings/mod.rs:899-912`] — Schwacher Assert (nur Core-Field-Equality); stronger Assert wäre `count(*)==6` (1 plugin + 5 sentinel). Test-Hardening, nicht code-bearing.
- **P2-W3 — Test-Boilerplate `Settings::in_memory(noop()).unwrap()` 20× repetition** [`klarvo-core/src/settings/mod.rs::tests`] — `fn fresh()`-Helper würde Lesbarkeit erhöhen; reine Test-Quality-Polish.
- **P2-W4 — `invoke("get_user_settings")`-Hang → Infinite-Spinner** [`shells/windows/src/index.html:271-289`] — Tauri-IPC ist lokal, sollte immer returnen; Timeout wäre defensive Ceremony. Optional Hardening.
- **P2-W5 — Power-Loss zwischen `BEGIN` und `COMMIT` + edited TOML zwischen Crashes** [`klarvo-core/src/settings/mod.rs::migrate_from_toml_if_needed`] — Extrem rare; SQLite-Atomicity hält den primären Pfad; Mitigation braucht Design-Pass.
- **P2-W6 — React ESM-Imports ohne Offline-Cache** [`shells/windows/src/index.html:184-185`] — First-Launch ohne Netz = blank Panel. Phase-2-B Vite+React-Migration löst strukturell.
- **P2-W7 — Story-1B `rusqlite_migration`-Präzedent in AC-1 unverifiziert** [Story-File AC-1] — Per `feedback_reviewer_external_fact_verification`: behauptetes externes Crate-Detail in Story-Statement braucht Source-Ref oder "zu verifizieren"-Markierung. Doc-Only, Memory-Hygiene.
- **P2-RC1 — SQLite-Hardening (WAL/busy_timeout/synchronous)** [`klarvo-core/src/settings/mod.rs::open`] — Pass-1 als A4-D5 deferred; Pass-2 (Edge Case Hunter E17) bestätigt zusätzlichen Cross-Process-Race auf `migrations.apply` zwischen Main-App und xtask-Binary. Verschmilzt mit A4-D5.
- **P2-RC2 — `settings.changed`-Listener fehlt im Frontend** [`shells/windows/src/index.html`] — Pass-1 als A4-D9 deferred; Pass-2 (E7) bestätigt: External-Writer-Stomp wird Foundation-Issue für A8-Sub/C2/C3. Verschmilzt mit A4-D9.
- **P2-RC3 — HTML `lang`-Attribut statisch `"en"`** [`shells/windows/src/index.html:129`] — Pass-1 als A4-D10 deferred; Pass-2 (BH#18) ergänzt a11y/Screen-Reader-Mispronunciation als zusätzliche User-Visible-Konsequenz. Verschmilzt mit A4-D10.
- **P2-RC4 — `type`-Spalte ohne `CHECK`-Constraint, Roundtrip-Validation fehlt** [`klarvo-core/src/settings/mod.rs::settings-schema`] — Pass-1 als A4-D4 deferred; Pass-2 (BH#5) verschärft: Schema-Level `CHECK(type IN ('string','i64','bool','json'))` würde Future-Phase-Bug strukturell verhindern statt code-only. Verschmilzt mit A4-D4 + Phase-2-B-Schema-Migration-Trigger.

### Pass-2 Skipped-Patches (Folge-Story-Scope, 2026-04-30)

Story 2.A.A4 bleibt nach Pass-2-Closure auf `done`. Die folgenden 4 Pass-2-Patches sind nicht unter A4 verbucht, sondern explizit als eigene Phase-2-A-Welle-3- oder Phase-2-B-Stories zu scopen — sie brauchen Spec-Arbeit (UX-Decision / Architectural-Trait-Extension / Error-Path-Discrimination / Semantic-Default-Choices), die unter Foundation-Closure-Scope nicht gehört.

- **P2-P1-deferred — Load-fail Data-loss: Form-UX-Redesign** [`shells/windows/src/index.html:271-289`] — Load-Fail-Pfad setzt FORM_DEFAULTS und erlaubt Save → reale DB-Werte werden mit Defaults gestompt. Echter Fix: Form `disabled` halten + explizites "Reload" anbieten + Save-Button hidden bis Load OK. Pass-1-P11-Intent (kein blank/undefined) muss umgekehrt werden — UX-Decision-Folge-Story.
- **P2-P8-deferred — `Settings::user_snapshot` single-mutex Bulk-Read** [`shells/windows/src-tauri/src/commands/settings.rs:127-137` + `klarvo-core/src/settings/mod.rs`] — `get_user_settings` nimmt 5× den Mutex, concurrent `set_*` zwischen den Lock-Cycles erzeugt torn read. Architectural-Extension: neuer `Settings::user_snapshot()`-Trait-Member der Mutex einmal hält + 5 SELECTs in einem Call macht; ripples durch Tauri-Command. Folge-Story für Phase-2-A-Welle-3 oder Phase-2-B (zusammen mit Pill-Bar-Foundation).
- **P2-P12-deferred — Corrupt-on-disk-DB Recovery-Pfad** [`klarvo-core/src/settings/mod.rs::open` + `main.rs::settings-fallback`] — Pass-1-P3 macht Two-Step-Fallback (file→in-memory) aber lässt korrupte File auf Disk shadowed. Echter Fix: `Connection::open`/`migrations::apply`-Err → File rename zu `settings.db.corrupted-<unix-ts>` + `tracing::error!`-Audit + dann in-memory. Discrimination welcher Error trigert Rename (corruption vs file-permission vs disk-full) braucht Decision.
- **P2-P21-deferred — Frontend-Reject + TomlMigrationSource semantic-fallback (D3-Resolution)** [`klarvo-core/src/settings/mod.rs:498-545` + `shells/windows/src/index.html:238-243,291-307`] — Code-Review Pass-2 D3 entschied Frontend-Authority für Locale/Output-Target-Validation. Implementation: Frontend hard-rejected unbekannte Werte vor Save (`langOptionsFor`-Helper aus Pass-1-P10 wird redundant), Migration bekommt semantic-fallback (Unknown locale → Default + warn). Zwei-System-Change mit Default-Choices (welcher Locale-Default? welcher Output-Target-Fallback?) — eigene Story.

### Pass-2 Dismissed (1, dokumentiert für Pass-3-Vermeidung)

- **P2-P19-dismissed — `Settings::in_memory` `pub` in Release** — Blind-Hunter-Blind-Spot ohne Projekt-Kontext. `in_memory` IST Production-Fallback (Pass-1-P3 Two-Step-Fallback in main.rs); cfg-test-Gating würde Boot-Resilience-Pfad brechen. Plugin-Authors können den Helper aufrufen, aber er greift auf eine flüchtige In-Memory-DB zu — kein Sicherheits-/State-Risiko. Sollte Pass-3-Reviewer das wieder flaggen: erste Antwort = "production fallback path".
- **P2-D1-resolved — CSP + SRI auf React ESM CDN-Imports** [`shells/windows/src/index.html:184-185`] — Resolved 2026-04-30 als defer (Option b). Phase-2-A intern, EA zurückgezogen, kein User-Release; SRI auf `esm.sh`-Sub-Imports (chained react→react-dom→scheduler) ist Halbsicherheit + false sense of security; CSP-Lockdown gegen `esm.sh` brüchig; saubere Lösung = Vendor-Bundling oder Vite-Migration in Phase-2-B. Investment in (a/c) wäre Wegwerf-Code. **Trigger zur Wieder-Aufnahme:** wenn Phase-2-A ungeplant Public-Release-Pfad bekommt ODER Phase-2-B Vite-Migration startet (dann strukturell durch Bundle-Inlining gelöst).
- **P2-D2-resolved — Sequential 5-await Save → Partial-State auf Mid-Fail** [`shells/windows/src/index.html:291-307`] — Resolved 2026-04-30 als defer (Option c). Mid-Fail-Trigger-Surface schrumpft mit P2-P2 (catch_unwind) + P2-RC1 (SQLite-Hardening) auf reine Validation-Fehler; Validation ist pro-Wert deterministisch → entweder alle 5 OK oder UI hat Garbage. Atomare `set_user_settings`-Batch-Command (Option a) ist Phase-2-B-Wert (Vite + Save-Status-pro-Field-UX) und sollte als eigene Story scoped werden, nicht als A4-Spec-Amendment. **Trigger zur Wieder-Aufnahme:** Phase-2-B Pill-Bar-UX-Mini-Pass oder External-Writer-Foundation für A8-Sub/C2/C3.

## Deferred from: code review of story-2a-D2 (2026-04-30)

Code-Review auf D2-fokussiertem Diff (Option B: `commands/settings.rs` ausgeklammert, da unter A4 reviewt). 0 Patches, 0 Decisions, 10 Defer, 9 Dismiss. Acceptance Auditor: "No findings" (alle ACs verifiziert, klarvo-core-Scan-Claim per `grep` bestätigt). 8 von 10 Defer-Items sind A4-Pass-3-Kandidaten (main.rs Settings-Init-Surface war in A4-Scope, in e25c308-Commit aber nicht von A4-Pass-1+Pass-2 vollständig abgedeckt); 2 sind D2-Out-of-Scope-Folge-Items.

- **D2-W1 — Kein Test für `Clone`-shared-state-Semantik** [`klarvo-shell-orchestrator/src/session.rs:39`] — Future-Footgun: nicht-Arc-Field bricht Shared-State-Invariante still. Test wäre `clone orch; on_press auf clone1, on_release auf clone2; assert state-transition`. Good-to-have, ACs verlangen nicht.
- **D2-W2 — Andere Tauri-State-Typen noch in `Arc::clone(...)`** [`shells/windows/src-tauri/src/main.rs:242-245`] — `config`, `keystore`, `emitter`, `clock` haben das gleiche Doppel-Arc-Pattern wie das D2 für Settings/Orch gefixt hat. Explizit out-of-scope per Story-2.A.D2-Scope-Fence; eigene Folge-Story für gleichartige Cleanup-Welle.
- **D2-W3 — `debug_assert!(app.manage(x))` no-op in Release-Builds** [`shells/windows/src-tauri/src/main.rs:241-246`] — Pre-existing Pattern an 6 manage-Sites. `assert!` würde Release-Diagnostic geben (`app.manage` returnt `false` bei Doppel-Registrierung). D2 erweitert nur Footprint, introduziert nicht.
- **D2-W4 — `ts_ms: 0u64` hardcoded in 3 `app.error`-Emit-Sites** [`shells/windows/src-tauri/src/main.rs:113,153,170`] — Verletzt MonotonicClock-Konvention (`project_event_ts_ms_convention`); Frontend-Dedup/Sort kollabiert auf t=0. Fix: `clock.now_ms()` Baseline (Clock wird in Step 7 konstruiert; Reorder oder Early-Construction).
- **D2-W5 — `resolve_config_path()`-Err-Arm asymmetrisch** [`shells/windows/src-tauri/src/main.rs:118-121`] — A4-P5 hat nur `load_config`-Err-Arm gefixt (`error.config.parse_failed`-Toast); Outer-Arm (Path-Resolution-Fehler, fehlende APPDATA, Permission-Issue) emittiert kein `app.error` und setzt `toml_loaded_ok=false` ohne User-Signal.
- **D2-W6 — `migrate_from_toml_if_needed`-Err nur `tracing::warn!`** [`shells/windows/src-tauri/src/main.rs:200-202`] — User-TOML-Settings erreichen SQLite nicht, kein Toast — Silent-Failure-Hole. Fix: `error.settings.validation`-Toast oder neuer Key emittieren.
- **D2-W7 — `PathBuf::new()`-Sentinel + `as_os_str().is_empty()`-Check** [`shells/windows/src-tauri/src/main.rs:142,149`] — Empty-`PathBuf` als Sentinel ist invisible für Reader und brittle (Filesystem-API-misuse-Risiko). `Option<PathBuf>` wäre selbst-erklärend.
- **D2-W8 — `app.error` in `.setup()` emittiert vor Frontend-Listener-Attach** [`shells/windows/src-tauri/src/main.rs:108-115,151-154,168-171`] — Toast-Events können verloren gehen, wenn Webview noch nicht ready. Mitigation: Buffer setup-time errors in managed-state, flush nach erstem `window-ready`.
- **D2-W9 — Boot-N Corrupt-TOML → UI-Write → Boot-N+1 Fixed-TOML: Sentinel-Key-Presence ≠ Migration-Done** [`shells/windows/src-tauri/src/main.rs:187-202` + `klarvo-core/src/settings/migrate_from_toml_if_needed`] — Wenn Boot-N TOML korrupt war (Migration übersprungen) und User in UI ein Setting ändert, dann Boot-N+1 mit fixiertem TOML: 4 von 5 TOML-Feldern werden gedroppt weil Sentinel-Key existiert aus UI-Write. Fix: dedizierte Migration-Sentinel-Row (`settings_migrated_v1=true`) statt Partial-Sentinel-Key-Presence. Architektur-Frage.
- **D2-W10 — Keine Tests für neue Boot-Branches** [`shells/windows/src-tauri/src/main.rs:133-203`] — `app_data_dir`-fail, `mkdir`-fail, `db-open`-fail, `migration`-fail nicht abgedeckt. MockRuntime + tempdir + AppHandle::emit-Observation würde Fail-Soft-Chain-Regressions verhindern.

### Pass-Vermeidungs-Hinweis: Dismissed (9, dokumentiert)

D1-D9 siehe Story-File `_bmad-output/implementation-artifacts/2a-d2-arc-wrapping-fix.md` Review-Findings-Sektion. Drei häufigste Re-Flag-Targets:
- **D2-Dismiss-D1** (`SessionOrchestrator: Clone`-Safety): "AA-verified all fields Arc<…>, see session.rs:39-55".
- **D2-Dismiss-D5** (`expect()`-Panic in Fallback): "A4-P3 Two-Step-Fallback reviewed; final expect = dokumentiertes Last-Resort gegen rusqlite-in-memory-infallible".
- **D2-Dismiss-D8** (`NoopSettingsEmitter` swallows): "A4-P13: in-memory-Pfad nutzt TauriSettingsEmitter; Noop nur deepest Last-Resort".

## Deferred from: code review of story-2a-E1 (2026-04-30)

Code-Review auf E1-Diff (Single-File-Add `.github/workflows/windows-ci.yml`, 44 Zeilen). Acceptance Auditor: alle 4 ACs erfüllt. Blind+Edge: 5 Defer + 5 Decision-Needed (Decision-Needed bleibt in Story-File, hier nur Defer).

- **E1-W1 — `pull_request` triggert doppelt auf same-repo PRs** [`.github/workflows/windows-ci.yml:11-13`] — `push` + `pull_request` feuern beide auf same-repo-Branch-Pushes. Concurrency-Keys differieren (`refs/heads/<branch>` vs `refs/pull/<n>/merge`) → keine gegenseitige Cancellation, doppelter Windows-Runner. Repo-weiter Pattern (`ci-event-lint.yml`, `ci-bindings-drift.yml` haben dieselbe Form); Cross-Cutting Cleanup als Folge-Story für alle CI-Workflows.
- **E1-W2 — `klarvo-bridge-jni` Compile-Coverage = 0 auf CI** [Linux-CI vs Windows-CI] — Windows-CI excluded JNI-Bridge (F2-Rationale OK + temp); Linux-CI führt nur xtask aus, kein Workspace-Compile. Bridge wird daher auf KEINEM CI-Platform compile-checked. Nicht durch E1 eingeführt; nach F2-Closure auf Windows wieder gedeckt — Linux-Compile-Gate für Bridge bleibt offen als eigene Folge-Story.
- **E1-W3 — Kein `paths-ignore`** [`.github/workflows/windows-ci.yml:11-13`] — Docs-only / `_bmad-output/`-only PRs triggern Windows-Runner (teuerste Compute-Surface). Repo-Konvention (kein Workflow nutzt `paths`/`paths-ignore`). Cost-Concern, keine Korrektheit; Cross-Cutting Phase-2-Cleanup.
- **E1-W4 — `cancel-in-progress: true` maskiert cancelled Master-Runs** [`.github/workflows/windows-ci.yml:19-20`] — Rapid Master-Pushes brechen laufende Compile-Gates ab → grünes Badge auf cancelled Run möglich. Spec mandatiert `cancel-in-progress: true` (AC-1); `head_ref`-Keying / `false` für protected-branches ist Branch-Protection-Config out-of-scope.
- **E1-W5 — `pull_request` läuft auf Draft-PRs (default `types`)** [`.github/workflows/windows-ci.yml:13`] — Draft-PR-WIP-Commits konsumieren Windows-Runner-Minuten. Repo-Konvention (kein Workflow setzt `types:`). Cost-Concern.

### Cross-Cutting CI-Hardening-Folge-Story (E1-Decision-Resolutions, 2026-04-30)

4 Decisions aus E1-Code-Review zur Cross-Cutting-Folge-Story konsolidiert. Betrifft alle 4 GitHub-Workflows (`windows-ci.yml`, `ci-event-lint.yml`, `ci-bindings-drift.yml`, `ci-release-build.yml` ggf.). Hier alleine fixen schafft Inkonsistenz; Bündel-Story tightent simultan:

- **E1-D1 — Toolchain-Pin-Honoring verifizieren + fixen** [alle Workflows mit `dtolnay/rust-toolchain@stable`] — Reviewer-Claim („`@stable` ignoriert `rust-toolchain.toml` (1.94.0)") ist unverifizierte externe Crate-Behauptung (memory `feedback_reviewer_external_fact_verification`). Folge-Story: (1) Action-Verhalten verifizieren (Action-Source / GitHub-Actions-Run-Log inspizieren — installiert Action wirklich `1.94.0` oder current stable?); (2) Falls Pin nicht greift: entweder `with: toolchain: 1.94.0` explizit übergeben ODER auf `actions-rust-lang/setup-rust-toolchain@v1` wechseln; (3) inline-Kommentar in jedem Workflow harmonisieren.
- **E1-D2 — `--locked`-Flag** [alle `cargo`-Steps in CI-Workflows] — Reproducibility-Garantie. Aktuell kann `Cargo.lock` unter `cargo check`/`build` mutieren, Cache-Invariante geschwächt. Folge-Story tightent in einem Commit über alle Workflows.
- **E1-D3 — Lint-Gate-Story** — Compile-Gate akzeptiert Warnings; Windows-only-Warnings rotten unbemerkt. Lösung NICHT `RUSTFLAGS=-D warnings` (zu grob, bricht bei stable-Drift), sondern eigene `cargo clippy -- -D warnings`-Gate (workspace-weit, separate Workflow-File analog `ci-event-lint`). Erst E1-D1 (Toolchain-Pin) lösen, sonst stable-Drift = unerklärliches Lint-Rot.
- **E1-D5 — `timeout-minutes`** [alle Job-Definitionen in CI-Workflows] — Defensive gegen hung Runs (Default 6h). `timeout-minutes: 30` für alle compile-Jobs, vermutlich kürzer für xtask-Jobs. Folge-Story bündelt mit E1-D2.

## Deferred from: code review of 2b-a1-toggle-autostop-wait-and-type-modes (2026-04-30)

- **A1-F1 — Empty `de.json` außer Recording-Mode-Keys** [`shells/windows/src/locales/de.json`] — Pre-state `{}`; jetzt 5 Keys. Story-übergreifender i18n-Gap, nicht durch A1 verursacht. Story 4.3 oder Phase-2-B i18n-Sweep.
- **A1-F2 — Naming-Convention-Spread für Recording-Mode-Setting** — `hotkey_slot1_mode` (struct) / `hotkey.slot1.mode` (DB) / `hotkeySlot1Mode` (TS) / `settings.recording_mode.*` (i18n). Cosmetic; cross-cutting Refactor.
- **A1-F3 — Keine Deduplizierung identischer `settings.changed`-Updates** [`shells/windows/src-tauri/src/main.rs:325-338`] — Listener schreibt RwLock auch bei identischem Wert. Write-Amplification nur bei buggy upstream relevant.
- **A1-F4 — AutoStop-Pipeline-finishes-before-state-set Race** [`klarvo-shell-orchestrator/src/session.rs:161-246`] — Theoretisch wenn `run_capture_session` extrem schnell returniert. Praktisch durch Audio-Latenz blockiert.
- **A1-F5 — Audio-Capture läuft während STT in AutoStop weiter** [`klarvo-shell-orchestrator/src/session.rs:182-189`] — Cleanup nach STT statt direkt nach VAD-SpeechEnd. Resource-Waste, keine Korrektheits-Issue. Folgt ADR-0012 Amendment 1.
- **A1-F6 — WaitAndType emittiert `RecordingDelivered` mit empty text** [`klarvo-shell-orchestrator/src/session.rs:200-205`] — Bei silent-Recording. Pill-Bar (Story A3) müsste empty-Case rendern; in A3-AC-Design einplanen.
- **A1-F7 — `RecordingDelivered.text` unbounded String über Broadcast + IPC** [`klarvo-core/src/event/bus.rs`, `shells/windows/src-tauri/src/bridge.rs`] — Lange Transkripte → mehrere MB pro Subscriber. Pill-Bar-Design (A3) wird Cap definieren.
- **A1-F8 — Subscriber-Lag droppt `RecordingDelivered` aus Broadcast-Channel** — Broadcast-Capacity-Pattern bereits etabliert; Garantie wird in A3 designt.
- **A1-F9 — `emit("recording.delivered")`-Failure-Pfad** [`shells/windows/src-tauri/src/bridge.rs:177-179`] — `tracing::warn!`+continue, kein Retry. Pre-existing Pattern aus RecordingCompleted.
- **A1-F10 — AutoStop-Cleanup-Race überschreibt freshly-started new Recording** [`klarvo-shell-orchestrator/src/session.rs:182-189`] — Nur möglich wenn altes Pipeline-Cleanup-Lock erst nach neuer on_press läuft; durch Tokio-Mutex-Fairness blockiert.
- **A1-D5 — User-konfigurierbarer Recording-Timeout** [`klarvo-shell-orchestrator/src/session.rs` `MAX_RECORDING_DURATION_SECS`] — D5-Hard-Cap (60s default) ist eingebaut; Threshold ist hardcoded. Folge-Story: Settings-Key `recording.max_duration_secs`, UI-Slider im Settings-Panel, Per-Mode-Override (Hold ohne Cap, AutoStop mit User-Default). Nice-to-have: Toast-Countdown ab 50s.
- **A1-D5b — Hard-Cap auch für Toggle/WaitAndType erwägen** — Aktuell nur AutoStop hat Timeout. User könnte vergessen, Toggle-Aufnahme zu stoppen; Hold/WaitAndType durch Key-Release-Pressure begrenzt. Phase-2-B-Hardening-Frage: Symmetrischer Cap oder bewusst nur AutoStop.
- **A1-D2 — Test-Coverage für Toggle-OS-Auto-Repeat-Garantie** — D2-Resolution beruht auf Backend-Verhalten (`tauri-plugin-global-shortcut` v2.3.1 emittiert keine Repeats). Falls Backend in Folge-Version Repeats forwardet, oszilliert Toggle. Schutz: Integration-Test mit MockHotkeyBackend, der mehrere Pressed-Events emittiert; oder ADR-Note bei Plugin-Upgrade.
- **A1-D7 — i18n für Recording-Mode-Dropdown im React-Settings-Panel** [`shells/windows/src/index.html:99-103,274`] — Code-Review-Patch P2 beim Batch-Apply als Defer eingestuft. AC-9 + AC-10 verlangen i18n-Labels für Dropdown; aktuell hardcoded English. Sauberer Fix braucht `i18n_table` via `app.manage` + Inline-`t()`-Helper im Settings-Panel oder die Phase-2-B-Vite-Migration. Bündeln mit Story A8-Sub (Tray-Language-Switcher) — beide brauchen denselben Loader.

## Deferred from: re-review of 2b-a1-closure (commit 4f0e0f7, 2026-04-30)

- **A1-Re-F1 — Rapid Toggle triple-tap: VAD-Lock-Contention zwischen alten Pipeline-Cleanup und neuer Session** [`klarvo-shell-orchestrator/src/session.rs`] — Drittes `on_press` während erstes Pipeline-Task noch VAD-Lock für STT-Cleanup hält → neue Session emittiert `RecordingStarted` aber blockiert auf VAD-Acquire (silent-recording-Window). Architecture-Refactor (Session-ID + Abort-Old, oder VAD-per-Session). Phase-2-B-A3-Pill-Bar-Design-Window.
- **A1-Re-F2 — Test-Coverage-Lücken** [`klarvo-shell-orchestrator/tests/session_tests.rs`] — Vier missing tests: AutoStop-Hard-Cap-Timeout-Branch nie executed; `is_idle()` nur für AutoStop-Pfad asserted (Toggle/Hold offen); Mode-Change-Mid-Session-Snapshot (D4-Resolution) ungetestet; Triple-Tap-Toggle-Race ungetestet. Phase-2-B-Test-Hardening-Story.
- **A1-Re-F3 — Hard-Cap-Timeout-Toast-Emit blockiert Cleanup** [`klarvo-shell-orchestrator/src/session.rs:203-208`] — `error_emitter.emit_error(...).await` läuft synchron vor Cleanup-Block; bei IPC-Lag bleibt Audio-Capture > 60s nominal offen. Fix: Cleanup vor Emit oder Emit fire-and-forget via spawn.
- **A1-Re-F4 — BYOK-API-Cost-Transparency auf Hard-Cap-STT-Drop** — Hard-Cap-Cancel während mid-HTTP-Request unterbricht `reqwest`-Future via Drop; Server-seitig potentiell partial-streamed → User zahlt für orphaned API-Call. Geringes Volumen, aber relevant für BYOK-Narrativ. Tracing-Metric + Doc-Note Phase-2-B-Hardening.
- **A1-Re-F5 — `DEFAULT_RECORDING_MODE_SLOT1`-Const-Typo surfacelt nur runtime** [`klarvo-core/src/settings/mod.rs:301-307`] — Const-String-Typo (`"toogle"`) schlägt erst bei erstem `recording_mode_slot1()`-Read fehl. Compile-Time-Schutz via `phf_map!` oder `RecordingMode::default()`-Const wäre 5-Zeilen-Fix; aktuell Test-Coverage greift. Phase-2-Cleanup.

## Deferred from: code review of 2a-a8-sub-tray-language-switcher (2026-04-30)

- **A8-F1 — Rapid Locale-Toggling: kein Debounce/Coalescing** [`shells/windows/src-tauri/src/main.rs:351-365`] — Jedes `settings.changed` mit `key = "ui.language"` spawnt sofort einen `tray::rebuild_for_locale`. Bei 4 schnellen EN→DE→EN→DE-Klicks im Settings-Panel laufen 4 Rebuilds. Tauri main-thread serialisiert Menu-Mutationen, aber ordering-guarantee dass der letzte `set_menu` gewinnt ist nicht garantiert. Praktisch low-priority — User-driven Event, kein Hot-Path. Phase-2-Cleanup: optional 50-100ms Debounce.
- **A8-F2 — `build_menu`/`tray.set_menu`-Failure → kein User-Toast** [`shells/windows/src-tauri/src/tray.rs:92-105`] — Beide Failure-Pfade loggen mit `tracing::error!`/`tracing::warn!`, aber emittieren keinen User-facing Error-Toast (kein `ErrorEmitter` im Listener-Closure). Menü bleibt auf alter Locale, User sieht keine Diagnose. Fix-Pfad: `ErrorEmitter` in Listener-Closure injecten (analog 11b-Pattern wo `mode_arc_listener` cloned wird), bei Failure `error.tray.rebuild_failed`-Key emittieren. Medium-low priority; ErrorEmitter-Wiring zu Listener-Closures ist Cross-Cutting (mehrere Listener im Boot betroffen).
- **A8-F3 — Old-Menu-Lifecycle / Win32-GDI-Handle-Leak unverifiziert** [`shells/windows/src-tauri/src/tray.rs:103`] — `tray.set_menu(Some(new_menu))` ersetzt das alte Menu; ob Tauri den Win32-HMENU des alten Menus released ist nicht im Code dokumentiert und nicht durch Test gedeckt. Bei vielen Locale-Toggles theoretisch GDI-Handle-Exhaustion. Tauri-Internal; out-of-scope für A8-Sub. Phase-2-Verifikation per Tauri-Doc/Source-Audit oder Stress-Test.
- **A8-F4 — 2.B.A1-Code-Review-Findings (8 Items) — Coupling resolved by Commit-Split** — Per Scope-Audit gehörten die ~330 LOC `session.rs`/`commands/settings.rs`/`recording/mod.rs`-Patches in der uncommitted-Working-Tree zur 2.B.A1-Code-Review-Closure (Post-29ce800), nicht zu A8-Sub. **Resolution:** Working-Tree wurde 2026-04-30 in zwei Commits gesplittet — 2.B.A1-Closure als eigener `chore(phase-2-b)`-Commit. Blind-Hunter erhob darauf 8 Findings (AppError-`From`-Conversion-Visibility, AutoStop-Timeout-cancel-drops-vad-guard, `RecordingCompleted`-Emission im Refactor, `SessionState`-by-value-Pattern Compile-Check, sync-`set_recording_mode_slot1` über Tauri-Command-Dispatcher, Test-Race in `autostop_transitions_to_idle_after_vad`, Toggle-key-repeat-Guard-Wording, `RecordingCompleted` auf Timeout-Pfad). Diese sind in einem separaten `code-review` über den 2.B.A1-Closure-Commit zu auditen.

## Deferred from: code review of 2a-c2-hotkey-konflikt-erkennung (2026-05-01)

- **C2-W1 — ADR-0013 §181 contradicts Spec/Impl bei C2-Rollback-Verhalten** [`docs/adr/0013-settings-persistence-schema.md:181`] — ADR-Text sagt „bei `RegisterHotKey`-Fail rollt Settings-Mutation zurück", Spec AC-1+AC-2 + Implementation nutzen aber Pre-Validation-Modell (kein Rollback nötig). ADR-0013 braucht Amendment (eigener Commit gemäß `feedback_adr_amendment_convention`).
- **C2-W2 — Negativ-Pfad-Test: Conflict-Path triggert kein `settings.changed`** — Aktuell durch Control-Flow-Inspection abgedeckt; Regression-Test wäre Härtung. Mock-Conflict-Setup über `validate_hotkey_not_conflicting` ist nicht-trivial (Win32-spezifisch). Phase-2-Härtung.
- **C2-W3 — `tauri::Manager::state::<Arc<dyn ErrorEmitter>>()` Panic-on-missing in `reregister_hotkey`** [`shells/windows/src-tauri/src/hotkey.rs:97-98`] — Currently safe (Tauri-Commands fire post-`setup()` wo `app.manage(...)` läuft); defensives `try_state` + Log-on-None wäre Härtung gegen zukünftige Setup-Reordering oder Test-Constructed-AppHandles.

## Deferred from: code review of 2a-d3-graceful-shutdown (2026-05-01)

- **D3-W1 — `block_on` inside `RunEvent::Exit` runtime-deadlock concern** [`shells/windows/src-tauri/src/main.rs:490-492`] — `tauri::async_runtime::block_on` aus dem Tauri-Exit-Callback ist fragil, falls `shutdown()` etwas auf demselben Multi-Threaded-Runtime awaited, das Locks hält die für Drain nötig sind. Tauri's dokumentiertes Pattern nutzt aber genau `block_on` an dieser Stelle (Main-Thread, kein Runtime-Worker). Reale Sorge, framework-pattern-konform; revisit nur wenn Exit-Hangs in der Praxis beobachtet.
- **D3-W2 — State-Race-Window zwischen `drop(state)` und `pipeline_task.abort()`** [`klarvo-shell-orchestrator/src/session.rs:312-315`] — Nach `drop(state)` könnte nebenläufiges `on_press()` den Lock greifen und neues Recording starten bevor `shutdown()` den (bereits `mem::replace`d) Task aborted. Neue Pipeline leakt past Shutdown. Pre-existing Pattern in `on_release` (session.rs:352) bewusst für Lock-Ordering. Bei App-Exit sind Hotkey-Handler typisch abgemeldet → race theoretisch + bounded by Process-Death. Keine Regression.
- **D3-W3 — AutoStop-Self-Cleanup × `shutdown()` Lock-Contention** [`klarvo-shell-orchestrator/src/session.rs:248 vs :309`] — AutoStop-Pipeline acquired `session_state`-Lock intern für Cleanup. Flow-Analyse beider Richtungen (shutdown-zuerst-Lock vs AutoStop-zuerst-Lock) legt nahe: beide Pfade sicher (jeweils Wait-and-Complete oder Wait-and-No-op). Verlangt deeper Concurrency-Spike (auch im Hinblick auf zukünftige Pipeline-await-under-lock-Erweiterungen); out-of-scope für 2.A.D3.
- **D3-W4 — `.expect("error building tauri application")` swallowt Builder-Error-Kontext** [`shells/windows/src-tauri/src/main.rs:486`] — `.run().expect(...)` → `.build().expect(...).run(...)` führt zwei Panic-Sites statt einer ein. Kein structured logging, kein `app.error`-Toast (pre-build eh nicht erreichbar). Pre-existing Style; `.expect()` war an dieser Stelle bereits vorher.

## Deferred from: code review of 2a-f2-jni-regression-triage (2026-05-01)

- **F2-W1 — JDK auf windows-latest nur implicit verfügbar** [`.github/workflows/windows-ci.yml:34-38`] — Kein expliziter `actions/setup-java`-Step; `cargo check` mit `jni`-Crate (`invocation`-Feature) braucht `JAVA_HOME`/`jvm.dll` zur Link-Zeit. GitHub-Runner-Image liefert Temurin aktuell mit, ist aber undokumentierte Abhängigkeit. Bei Runner-Image-Wechsel bricht CI silent. Härtung: explizit `actions/setup-java@v4` mit Temurin 17 vor `cargo check` einfügen.
- **F2-W2 — `JavaVM::singleton()` + Multi-Test-Binary-Edge** [`klarvo-bridge-jni/tests/audio_level_callback.rs:40-57`] — `OnceLock<JavaVM>` plus `JavaVM::singleton()` koppeln Test-JVM an Production-Pfad. Bei Panic während `JavaVM::new` bleibt OnceLock uninitialisiert, JVM aber teilweise initialisiert; zweiter Versuch failt mit „JVM exists". Aktuell moot (single test file, single binary). Bei Phase-3 Multi-Test-File-Erweiterung re-evaluieren.
- **F2-W3 — Background-Tasks nicht durch TEST_MUTEX serialisiert** [`klarvo-bridge-jni/src/commands.rs:97-106`] — `tokio::JoinHandle::abort()` ist non-blocking; Bridge-Task kann nach Test-N-Ende noch in JNI-Call sein, wenn Test-(N+1) startet. TEST_MUTEX serialisiert Test-Bodies, nicht Hintergrund-Tasks. `sleep(50ms/100ms)` ist best-effort. Härtung: `JoinHandle::abort()` + busy-wait auf `is_finished()` oder `tokio::time::timeout(_, h).await`.
- **F2-W4 — `stop_meter` abort vs. drain race** [`klarvo-bridge-jni/src/commands.rs:97-106`] — broadcast-Channel-Receiver im Bridge-Task kann nach `abort()` noch `recv().await`-Wakeup bekommen; abort während laufendem JNI-Call ist undefined. Pre-existing, nicht von F2 verursacht. Phase-3 wenn JNI-Bridge umgebaut wird.
- **F2-W5 — Linux-CI exkludiert `klarvo-bridge-jni` weiterhin** [Linux-Epic-5-CI workflow] — F2 schließt nur Windows-Compile (T5 explizit). Linux-CI hat `--exclude klarvo-bridge-jni` aus demselben `project_jni_spike_scope`-Memo. Post-Fix natürliche Closure wäre auch hier zu entfernen, plus `actions/setup-java` zu ergänzen. Eigene CI-Hardening-Story (oder Phase-3-Voraussetzung).
- **F2-W6 — Production `register_listener` silently overwrites Listener** [`klarvo-bridge-jni/src/streams.rs` resp. wo register_listener live] — Production-API gleiche Shape wie Test-Race; bei doppeltem Aufruf verschwindet erster Listener ohne Log. Amendment-2-Claim "Architektur unverändert korrekt" stimmt nur unter Single-Caller-Vertrag der Kotlin-Shell. Härtung: `tracing::warn!("listener overwritten")` auf `Some→Some`-Transition. Phase-3-JNI-Rewrite redesignt das ohnehin (Multi-Listener-Hardening, ADR-0003 §4 Known Limitation).

## Deferred from: code review of 2a-c3-live-locale-switch (2026-05-01)

- **C25 — Tray vs Frontend Listener-Desync (kein Ordering-Guarantee)** [`shells/windows/src-tauri/src/main.rs:390` vs `shells/windows/src-tauri/src/commands/settings.rs:239`] — Story dokumentiert intentional als Tauri-Broadcast-Pattern ("Beide subscribed auf settings.changed. Das ist unabhängig und korrekt. Keine Koordination nötig."). Drift-Guard nur über `tray.rs:138` Runtime-Test (`tray::SUPPORTED_LOCALES ⊆ config::SUPPORTED_LANGUAGES`). Bei partial-failure (frontend-listener succeeds, tray-listener fails oder vice versa) drift; bei zukünftiger Allow-list-Divergenz (z. B. neue Locale nur in einem Set) silent permanent drift. Härtung optional: Compile-time-Single-Source via `const`-Re-Export oder Phase-2-B-Refactor.
- **C26 — `apply_locale_reload` getestet, aber nicht das `#[tauri::command] reload_locale`-Wrapper** [`shells/windows/src-tauri/src/commands/settings.rs:248-258`] — Tauri-State-Binding (`State<SharedI18nTable>`), Specta-Export, und `collect_commands!`-Wiring sind end-to-end ungetestet. `xtask bindings-drift` (Story 5.2) catched Symbol-Drift in TS-Bindings; Tauri-Test-Harness für State-resolution wäre Phase-2-Cleanup. Re-evaluieren wenn weitere Tauri-State-Komponenten nach C3-Pattern wachsen.
- **C27 — D3-RunEvent::Exit-Issues (Bündel)** [`shells/windows/src-tauri/src/main.rs:487-500` + `klarvo-shell-orchestrator/src/session.rs:314-326`] — In C3-Diff bled, gehört aber Story D3: (a) `tauri::async_runtime::block_on(orch.shutdown())` aus Exit-Callback ohne Timeout — Deadlock-Risk wenn `pipeline_task.await` non-cancel-safe (`spawn_blocking`). (b) `pipeline_task.await` nach `.abort()` ohne `tokio::time::timeout` — kann Exit hängen. (c) Kein `catch_unwind` um `block_on` — Panic im Shutdown skippt OS-Cleanup. (d) `try_state` returnt `None` silent wenn State-Type wrong — kein `tracing::warn!` für Operator-Visibility. Reviewen sobald D-1 entschieden ist (entweder im D3-Followup-Review oder als D3-Patch-Set in C3, je nach Outcome).
- **C28 — useEffect deps `[]` capturing outer `tauriEvent`/`invoke` + late-`__TAURI__`-injection-race** [`shells/windows/src/index.html:215-235`] — Pattern aus A4-useEffect 1:1 geerbt. (a) Closure capture `tauriEvent` und `invoke` aus outer scope — bei HMR/lazy-import-Refactor stale-Capture. (b) `if (!tauriEvent) return;` mit `[]`-deps: wenn `__TAURI__` post-mount injiziert wird (slow-cold-boot/WebView-race), Listener nie installiert für die Session. Konsistenz-Fix erfordert A4 + C3 + ggf. weitere useEffects gemeinsam — defer auf Phase-2-B Vite+React-Migration.
- **C29 — Cancelled-Listener-Cleanup-Race im `.then(fn => { if cancelled fn() else unlisten = fn })`-Pattern** [`shells/windows/src/index.html:228-231`] — Gleiche Shape wie A4-useEffect. Bei verzögerter Promise-Resolution + Re-Mount-Sequenz kann `fn()` aufgerufen werden während neue Subscription bereits in-flight. Editorial-Fragility, kein konkretes Reproducer; gemeinsam mit C28 in Phase-2-B-Migration konsolidieren.
- **C30 — AC-1 literale `Result<_, AppError>`-Asymmetrie (`load_locale` panics on corrupt JSON)** [`shells/windows/src-tauri/src/i18n.rs:26-32`] — Doc-Comment markiert Phase-2-Replace explizit ("Phase-2: replace panic with fail-soft AppError path per ADR-0009 SD-4"). AC-1 sagt "oder analog" und C3 wrappt mit `apply_locale_reload`-Fail-soft. Asymmetrie bleibt: corrupt `de.json` post-Boot panicst Command-Worker (Frontend-`.catch` schluckt). Re-evaluieren wenn ADR-0009 SD-4 Boot-Error-UX in Phase-2-B implementiert wird (dann auch Runtime-Pfad migrieren).
- **C31 — Empty-string-Locale silently no-ops** [`shells/windows/src/index.html:225` + `shells/windows/src-tauri/src/commands/settings.rs:239`] — Frontend-Filter `typeof newValue !== "string"` akzeptiert `""`; Backend `SUPPORTED_LANGUAGES.contains(&"")` returnt false → fail-soft no-op. Story 4.1 Schema-Validation gates `ui.language ∈ {en, de}`, also kein realer Pfad heute. Defensive Härtung wenn Settings-Migrationen oder Reset-Pfade `""` erzeugen können — dann gemeinsam mit AppError-Surfacing in Phase-2-B.

## Deferred from: code review of 5-5-disallowed-methods-lint-gate (2026-05-01)

- **panic!/todo!/unimplemented! Macro-Lücke im Lint-Scope** [`klarvo-core/src/registry.rs:32,46,60`; `shells/windows/src-tauri/src/i18n.rs:30,33`] — Spec-explicit out-of-scope (Story-Section "Out-of-Scope"). 5 reale Production-Panic-Sites in den 3 Ziel-Crates, die `disallowed_methods` nicht catched (Macros ≠ Methods). Folge-Story: zweiter Gate via `clippy::panic`/`clippy::todo`/`clippy::unimplemented` mit identischem Crate-Scope (`klarvo-core`, `klarvo-shell-orchestrator`, `klarvo-windows-shell`).
- **Sprint-Status-Hygiene: 5.6 silent-flip + last_updated overwrite** [`_bmad-output/implementation-artifacts/sprint-status.yaml:63,154`] — Story 5.6 `backlog → ready-for-dev` innerhalb des 5.5-Commits (mixed scope, evades audit). `last_updated` verlor Multi-Clause-Epic-Restructuring-Kontext durch Overwrite statt additive Log-Line. Memory-Pointer: `feedback_status_drift_audit`. Bei nächstem Sprint-Status-Touch konsolidieren.

## Deferred from: code review of 5-7-detection-gap-migration (2026-05-01)

- **5.7-D1 — `ts_ms: 0` Boot-Sentinel-Konvention** [`shells/windows/src-tauri/src/main.rs:113,156,170`] — Drei Early-Boot-Emits nutzen literal `0u64` als Timestamp; MonotonicClock wird erst Step 6 konstruiert, also gibt es keinen sauberen Wert. Pre-existing seit Pre-5.7-json!()-Pattern; Edge Case Hunter klassifiziert als "negligible". Phase-2-Härtung: typed `Timestamp(u64)`-Newtype mit `Timestamp::boot_sentinel()`-Konstruktor + `Clock::now_ms()`, gemeinsam mit `event.rs`-Payload-Contract.
- **5.7-D2 — Frontend-Listener-Race für Early-Boot-`app.error`-Emits** [`shells/windows/src-tauri/src/main.rs:99-156`] — Step 0/1/2 emittieren `app.error`-Events bevor WebView-JS `listen("app.error", ...)` registriert; Toasts gehen verloren. Pre-existing in Pre-5.7-json!()-Pattern. Härtung: Buffer-Queue im Backend bis `frontend.ready`-Event aus JS, dann Replay. Berührt ADR-0009 (Shell-Error-Bridge SD-4 Boot-Error-UX).
- **5.7-D3 — `load_config`-Specificity-Loss in main.rs** [`shells/windows/src-tauri/src/main.rs:111-113`] — `parse_from_str` setzt fein-granulare `user_message` (`error.config.unknown_field` / `error.config.missing` / `error.config.invalid_language`), main.rs collapsed alles auf das generische `error.config.parse_failed`. Behavior identisch mit Pre-5.7. Härtung: `emitter.emit_error(e.user_message.as_deref().unwrap_or("error.config.parse_failed"), 0)` — propagiert die spezifische Klassifikation an den User.
- **5.7-D4 — `resolve_config_path`-Err ohne emit_error** [`shells/windows/src-tauri/src/main.rs:117-120`] — Wenn `APPDATA` env-var unset (einziger Trigger), nur `tracing::error!`, kein User-Toast. Inkonsistent zur Sibling-Branch (parse_failed). Pre-existing. Härtung: zusätzlicher `emit_error("error.config.missing", 0)` Call. Niedrige Priorität (extrem selten in Production).
- **5.7-D5 — TOML-`unknown field`-Substring-Match-Fragility** [`shells/windows/src-tauri/src/config.rs:111`] — Substring-Match auf `e.to_string()` ist brüchig gegen: (a) toml-rs-Library-Updates die Message-Format/Lokalisierung ändern, (b) BOM-prefixed Files (toml-rs rejected → falsche Klassifikation als `error.config.missing`), (c) Source-Snippet-Kollisionen (TOML-Value enthält literal "unknown field"), (d) Mixed-Errors wo serde nur den ersten meldet. Pre-existing seit Story 3.2. Überlappt thematisch mit F11 (Type-Mismatch UX). Härtung: strukturierte API auf `toml::de::Error` (kind/category) statt String-contains.

## Deferred from: code review of 6-1-telemetry-logging-rolling-file (2026-05-02)

- **6.1-W1 — Grep-Sentinel-Design-Fragilität** [`klarvo-core/src/telemetry/logging.rs:21-26`] — `RELEASE_MAX_LEVEL` Source-Grep-Sentinel kann nicht zwischen Release/Debug unterscheiden, weil beide `cfg`-Blocks textuell präsent sind. Story 6.2 ersetzt den Grep-Sentinel durch echten `verify_release`-Filter-Gate, also moot für 6.1. Flag für 6.2: nicht-naive-grep-Strategie nutzen (z. B. `cargo expand` + AST-Match oder Build-Constants-Probe).
- **6.1-W2 — `debug_assertions` cfg-Gate kann Custom-Profiles überraschen** [`klarvo-core/src/telemetry/logging.rs:21-26`] — Cross-Compilation oder Custom-Profiles die `debug-assertions` unabhängig von `opt-level` overriden, können DEBUG-Logging in Production produzieren. Default-Cargo-Profiles sind safe. Phase-4 Release-Hardening: tie release filter to explicitem Cargo-Feature oder `cfg(profile = "release")` (sobald stabil).
- **6.1-W3 — EnvFilter / RUST_LOG fehlt — FR37 "configurable verbosity" partial** [`klarvo-core/src/telemetry/logging.rs:62-66`] — `init_tracing` hardcoded `RELEASE_MAX_LEVEL`. FR37 PRD verlangt "konfigurierbare Verbosity". Story 6.1 ACs mandaten EnvFilter nicht; FR37 nicht voll erfüllt bis env-filter wired. Address in Story 6.2 (verify_release-Filter-Gate) oder neuer Story; Achtung: hardcoded `LevelFilter` ist Hard-Ceiling über jedem zukünftigen `EnvFilter`-Layer — Reihenfolge im Layer-Stack matters.
- **6.1-W4 — Keine Opt-Out für File-Logging (Privacy-Toggle)** [`klarvo-core/src/telemetry/logging.rs`] — BYOK-Zielgruppe (Power-User/Devs/Institute) könnte File-Logging gänzlich deaktivieren wollen. Kein env-var (`KLARVO_NO_FILE_LOG`) oder Settings-Toggle. Flag für Epic 9 Settings-Panel.
- **6.1-W5 — Kein Cross-Process-Locking bei concurrent Klarvo-Instances** [`klarvo-core/src/telemetry/logging.rs`] — `RollingFileAppender` koordiniert nicht über Prozesse. Zwei Klarvo-Instanzen würden auf File-Handles und Retention-Sweep racen. Probability low: Single-Instance bereits per Tauri-Plugin enforced. Document als known-limitation; bei v1/v2-Coexistenz-Szenarios re-evaluieren.
- **6.1-W6 — Late-Drop-Tracing-Events nach `_tracing_guard`-Drop verloren** [`shells/windows/src-tauri/src/main.rs:22`] — Events aus späten `Drop`-Impls während Stack-Unwind nach `main()`-Return gehen an geschlossenen Writer. Letzte Sekunde des Error-driven Shutdowns ist unloggable. Hard to fix ohne major refactor (manuelle Flush-Reihenfolge). Future-Shutdown-Hardening.
- **6.1-W7 — Default-fmt-Timestamp eventuell nicht ISO-8601** [`klarvo-core/src/telemetry/logging.rs:62-66`] — PRD/architecture §4 mandaten ISO-8601 Timestamps. Story 6.1 Dev Notes markieren explicit `with_timer(OffsetTime::local_rfc_3339())` als "optional für Phase 1, aber empfohlen". `tracing-subscriber 0.3` ohne `time`/`chrono`-Feature könnte SystemTime-Default-Format produzieren (nicht-RFC-3339). Empirisch verifizieren in Story 6.2 oder Polish-Pass; falls non-RFC-3339, `with_timer` + `time`-Feature ergänzen.
- **6.1-W8 — eprintln! invisible under `windows_subsystem = "windows"` (silent fail-soft)** [`klarvo-core/src/telemetry/logging.rs:43,55,72`] — In Release-Build setzt `shells/windows/src-tauri/src/main.rs:2` `windows_subsystem = "windows"`; stderr wird verworfen, alle drei Fail-Soft-Pfade in `init_tracing` produzieren null beobachtbaren Output. User hat kein Signal dass File-Logging fehlgeschlagen ist — exakt der Fall in dem Diagnose nötig wäre. **Resolution: defer zu Story 6.3** (Panic-Hook + Export-Stub liefert ohnehin Diagnose-Surface; Logging-Init-Failure als Boot-Error-Toast über ADR-0009-`app.error`-Bridge melden). User-Reason 2026-05-02: "Story 6.3 hat den passenden Scope - dort sauber mitnehmen statt hier Scope aufzumachen."
- **6.1-W9 — `fmt::layer()` Format-Config explizit pinnen** [`klarvo-core/src/telemetry/logging.rs:62-66`] — Aktuell relyes auf `tracing-subscriber`-Defaults für `with_target`/`with_thread_*`/Timer. Defaults driften potenziell zwischen Minor-Versionen. Log-Format ist menschen-lesbares Diagnose-Surface (nicht Machine-Contract), aber Stabilität wäre nice. **Resolution: defer zu Story 6.2** (verify_release-Filter-Gate fasst Filter/Subscriber-Setup sowieso an, Format-Pinning gleich mit-erledigen — gleiche Logik wie 6.1-W8 bei Story 6.3). User-Reason 2026-05-02: "Story 6.2 fasst verify_release sowieso an; Format-Pinning dort sauber mit-erledigen statt hier Scope aufzumachen."
- **6.1-W10 — PRD "max 10 MB" cap vs AC-3 count-only Divergenz** [`klarvo-core/src/telemetry/logging.rs:47-51`] — PRD §Journey verlangt "max 10 MB, 5 Rotations"; AC-3 mandatet nur Count (5). `tracing-appender 0.2.5` kann mit DAILY-Rotation **kein** Per-File-Size-Cap setzen (`Rotation::SIZE` existiert nicht in 0.2.5). Theoretisch unbounded growth des aktiven Tages-Files bei chatty Boot-Loops. **Resolution: defer zu Phase-2-Hardening** — AC-3 hat das bewusst so spezifiziert (Spec-Author hat 0.2.5-Limitation gesehen und akzeptiert); INFO-Level im Release ist nicht hot-loop-prone (DEBUG/TRACE wären problematisch — sind aber gefiltert). Custom-Size-Check wäre Scope-Creep für minor Risk. Re-evaluieren falls echte Disk-Fill-Reports kommen oder `tracing-appender` Size-based-Rotation supportet.

## Deferred from: code review of story-6.2 (2026-05-02)

- **6.2-W2 — Opaque I/O-Error Messages im `check_tracing_release_filter` Wrapper** [`xtask/src/verify_release.rs:208-211`] — Bei `locate_workspace_root() == None`, `read_to_string` mit `NotFound`/`InvalidData`/Permission-Fehler entsteht eine `failures.push(msg)`-Zeile, die für den Operator nicht von einer echten PII-Regression unterscheidbar ist (Release wird trotzdem geblockt). Fix: `ErrorKind::NotFound` separat behandeln (mit Rename-Hinweis auf logging.rs-Pfad), `from_utf8_lossy` als Decode-Fallback, dedizierter Unit-Test der None-Branch. Defer-Reason: Niedrig — kein Code-Defekt, Diagnose-Schliff. Re-eval bei erstem CI-Flake-Report.
- **6.2-W3 — W7 empirisches Timestamp-Format-Artifact fehlt** [Story-Spec Debug Log References-Block, leer] — Story 6.2 markiert Task "Timestamp-Format empirisch prüfen (W7)" als done; Completion-Notes behaupten `tracing-subscriber 0.3.23 SystemTime-Timer gibt YYYY-MM-DDTHH:MM:SS.ffffffZ aus`, aber kein Beleg-Log, kein Debug-Log-Reference, kein Test asserts das Format. Per `feedback_spike_rigor`: Spike-Claims brauchen Messwerte. Fix: Eine konkrete Log-Zeile aus dem empirischen Run unter "Debug Log References" oder Completion-Notes #7 einfügen. Defer-Reason: Doc-only, blockiert nichts.

## Deferred from: code review of story-6.3 (2026-05-02)

- **6.3-W1 — `extract_panic_message` deckt nicht `Box<dyn Error>` / `anyhow::Error` / non-static `&str`** [`klarvo-core/src/telemetry/logging.rs:84-90`] — Spec-AC-1 mandatet nur `&str`/`String`/Fallback. Erweiterung sobald anyhow/eyre in klarvo-core landet (Phase-2-Pattern für Result-Propagation). Defer-Reason: aktuell keine non-string Panic-Sites im Code; Diagnose-Loss erst bei Library-Adoption.
- **6.3-W2 — Concurrent `export_debug_zip` calls — kein Single-Flight-Guard** [`klarvo-core/src/telemetry/export.rs:17`] — Stub returnt immer `Err`, Race ist heute irrelevant. Phase-2-Real-Impl muss Mutex/AtomicBool wrappen + Settings-UI-Button-Disable während laufendem Export. Defer-Reason: erst mit Real-Impl in Epic 9 lösbar.
- **6.3-W3 — `process::exit(1)` in `main.rs` droppt `_tracing_guard` während Hook noch installed** [`shells/windows/src-tauri/src/main.rs:489-499`] — Pre-existing Pattern aus Story 6.1; nach `exit(1)` kann ein atexit/destructor-Panic einen toten Subscriber treffen → kein Log + potenziell Abort. Defer-Reason: außerhalb Story-6.3-Scope; gehört zu Story-6.1-Boot-Error-UX-Carry-Over.
- **6.3-W4 — OOM-Panic → Backtrace-Allocation-Failure → Hook-Abort** [`klarvo-core/src/telemetry/logging.rs:108`] — `Backtrace::force_capture` allokiert beim Symbol-Resolving; bei OOM-induced Panic erzeugt das eine zweite Alloc-Failure → Hook-Abort ohne Log. Defer-Reason: OOM ist niedrig-priorisiertes Failure-Mode für Klarvo (Audio-Buffers sind klein); Lösung wäre `cap_panic_alloc`-Strategie in Phase-2.
- **6.3-W5 — `init_tracing`→None silent-failure: Hook fires gegen No-Op-Subscriber** [`shells/windows/src-tauri/src/main.rs:22-23`] — Wenn `init_tracing` `None` liefert (log_dir nicht beschreibbar / globaler Default schon gesetzt), wird `_tracing_guard = None` lautlos gesetzt und der Panic-Hook verschickt `tracing::error!`-Events ins Leere. Pre-existing aus Story 6.1. Defer-Reason: gehört zu Story-6.1-Boot-Error-UX-Carry-Over (deferred-work-Epic-6.1-Liste hat verwandtes Item); konsolidierter Fix mit Boot-Error-Bridge sinnvoller als Story-6.3-lokaler Workaround.
- **6.3-W6 — `install_panic_hook` Integration-Test fehlt** [`klarvo-core/src/telemetry/logging.rs:install_panic_hook`] — Die 3 Headless-Tests covern nur den `extract_panic_message`-Helper; der eigentliche Hook (load-bearing API) ist nicht durch automatisierte Tests gedeckt (set_hook ist global → braucht serial_test-Coordination). Phase-1-Mitigation: AC-7 Manual-Smoke (Windows). Phase-2: `serial_test`-dev-dep + Test mit `take_hook`-Roundtrip + simulierter Panic via `catch_unwind`. Defer-Reason: aktuell einziger global-state-Test-Use-Case; serial_test-Setup lohnt erst wenn weitere ähnliche Cases auftauchen.

## Deferred from: code review of epic-9-return-focus (2026-05-03)

- **9.1-W1 — WaitAndType Restore-Timing-Race mit Pill-Bar** [`klarvo-shell-orchestrator/src/session.rs:274-282`] — Restore vor `RecordingDelivered`-Emit gemäß Spec, aber Pill-Bar (Story 9.6) holt Fokus möglicherweise direkt zurück → Race-Window. Defer-Reason: Cross-Story-Concern, gehört in Epic-9.6-Scope wenn Pill-Bar-Focus-Verhalten implementiert wird.
- **9.1-W2 — `SetForegroundWindow` Foreground-Lock-Limit** [`shells/windows/src-tauri/src/focus.rs:27`] — Win32-API ist rate-limited / privilege-gated; `SetForegroundWindow` schlägt silent fehl, wenn der Caller-Thread nicht bereits Foreground-Privilege hat. Spec acknowledged "best-effort, silent". Defer-Reason: Workaround `AllowSetForegroundWindow` evaluieren in Folge-Story; aktuell kein User-sichtbarer Diagnose-Pfad nötig.
- **9.1-W3 — `MockFocusCapture`-Sentinel = 42 ist tautologisch für Multi-Press-Szenarien** [`klarvo-test-fixtures/src/focus.rs:38`] — Mock returnt immer `Some(42)`; bei mehreren Presses werden Capture-Restore-Pairing-Bugs nicht erfasst. Defer-Reason: Nice-to-have; aktuell single-press-Tests adäquat. Bei Toggle-Multi-Press-Test (siehe 9.1-W4) Mock auf monoton-zählend umstellen.
- **9.1-W4 — Test-Coverage-Gaps** [`klarvo-shell-orchestrator/tests/session_tests.rs`] — Fehlende Tests (nach Review-Closure 2026-05-03 reduziert; paste-failure-restore wurde als `focus_restored_after_paste_error` mit-implementiert): empty-result-restore (text_to_deliver = None), capture-during-key-repeat, output-target-not-found-Pfad, None-from-capture-Pfad, WaitAndType-Restore-vor-Emit-Ordering. Defer-Reason: D1=A bereits implementiert + 3 Happy/Failure-Paths covered; verbleibende Gaps sind Coverage-Polish, kein Blocker.
## Deferred from: code review of story-9.2 (2026-05-03)

- **9.2-W1 — `manifest_stt_plugin` returns None silent when no Stt stage** [`klarvo-shell-orchestrator/src/session.rs:822-829`] — Helper iteriert über manifest-stages und returnt erste `PipelineStageType::Stt`-`plugin_id`; bei Pipeline ohne Stt-Stage `None` → history-Eintrag mit `plugin_id=NULL`. Manifest-Validierung müsste Stt-Stage ohnehin garantieren (Pipeline ohne STT macht keinen Sinn). Defer-Reason: defensive log nice-to-have, kein Phase-1-Blocker; gehört zu Manifest-Validation-Härtung wenn das auftaucht.
- **9.2-W2 — `history_max_entries()` non-numeric-string silent-default** [`klarvo-core/src/settings/mod.rs:695-700`] — `.parse::<i64>().ok().unwrap_or(500)` schluckt parse-Fehler stumm. User mit `history.max_entries="foo"` config-typo bekommt 500 ohne Hinweis. Phase-1 hat keine Settings-UI-Slider → User editiert nur via SQL-Tool. Defer-Reason: silent-default akzeptabel solange UI fehlt; warn-log kommt mit Settings-UI-Story (Epic-Settings).
- **9.2-W3 — history.db corrupt-Recovery (test-query at boot, `.corrupt`-rename)** [`shells/windows/src-tauri/src/main.rs:269-291`] — Aktueller Boot: `SqliteHistoryStore::open()` returnt `Ok(_)` wenn File öffnbar aber corrupt; jeder append/list failt downstream silent → fail-soft fallback NIE getriggert. Robust-Variante: Test-Query `SELECT 1 FROM history LIMIT 1` post-open; bei Error `.corrupt`-Rename + reopen fresh. Defer-Reason: Recovery-Flow gehört zu Phase-2-Story "DB-Recovery" (übergreifend für settings.db + history.db).
- **9.2-W4 — Migration crash-mid-tx Recovery-Test fehlt** [`klarvo-core/src/history/sqlite.rs:338-364`] — Currently OK weil `pragma_update(user_version)` inside tx + tx atomic. Aber kein Test deckt: tx-body crash (z.B. SQL-Error mid-migration) → tx-rollback → `user_version` bleibt auf alter Version → next-boot retry. Crash-Simulation via injected-failing-statement nötig. Defer-Reason: Test-Coverage-Gap, kein Code-Defekt; Pattern teilbar mit settings-Migrations.
- **9.2-W5 — lokale Telemetry-counter für persistente History-Failures** [`klarvo-shell-orchestrator/src/session.rs:299-300`] — `tracing::warn!` ist single-shot; bei persistentem Failure (DB locked, full disk) wird jeder Write geloggt aber nichts aggregiert → kein "5 von 5 letzten Writes failed" Signal. Memory `project_no_remote_telemetry` blockt Sentry. Lokaler Counter mit "Show diagnostic info"-Settings-UI denkbar. Defer-Reason: keine metrics-infra in Klarvo v2; warten bis logging.rs/diag-export-Story die Infrastruktur liefert.

## Deferred from: code review of 9-3-history-panel (2026-05-03)

- **F25 — Tabs ohne ARIA-Tablist-Semantik** [`shells/windows/src/index.html:288-298`] — `<button>` mit className-Toggle, kein `role="tablist"/"tab"`, kein `aria-selected`, keine Arrow-Key-Navigation. Defer-Reason: Spec adressiert Accessibility nicht; Phase-1-Polish, sinnvoll mit Vite/React-Migration zu ergänzen.
- **F26 — Kein Retry-Button im Error-State der HistoryPanel** [`shells/windows/src/index.html:267-273`] — Bei `loadHistory`-Failure ist der Error-State terminal bis Tab-Switch. Defer-Reason: UX-Polish, spec-out-of-scope; Toast erlaubt aktuell schon manuelles Retry via Tab-Wechsel.
- **F27 — `toLocaleString()` ignoriert `ui.language`-Setting** [`shells/windows/src/index.html:247`] — Date-Format folgt Browser-Default-Locale, nicht der gewählten UI-Sprache. Defer-Reason: Phase-1 ohne i18n-Resolver konsistent mit hardcoded-EN-UI-Strings; Phase-2-B Vite-Migration bringt Resolver inkl. Locale-Aware-Format.
- **F28 — Visuelle Hierarchie fehlt: „Clear All" vs. Per-Row-Delete identisches Styling** [`shells/windows/src/index.html` CSS .btn-danger + Toolbar-Render] — Bulk-Destruktiv und Per-Row-Destruktiv stehen kanten-nah und sehen identisch aus → Misclick-Risiko. Defer-Reason: UX-Polish; Designentscheidung (Outline-Variante / Spacing / Confirmation-Pattern-Differenzierung) gehört in Phase-2-Settings-Refresh.
- **F29 — UTF-16-Slice schneidet Surrogate Pairs (Emojis am 120er-Index)** [`shells/windows/src/index.html:243-245`] — `text.slice(0, 120)` operiert auf Code-Units, Emoji am Schnitt ergibt Replacement-Char. Defer-Reason: Spec ohne Grapheme-Mandat; tritt erst bei sehr spezifischem Diktat-Inhalt auf.
- **F30 — Kein Live-Update bei neuem Diktat (`history.appended`-Listener fehlt)** [`shells/windows/src/index.html` HistoryPanel] — User diktiert während Panel offen ist; neuer Eintrag wird erst nach Tab-Switch sichtbar. Defer-Reason: Spec ohne Live-Update-Mandat; Phase-2-Backlog. Erfordert neues Backend-Event aus `OrchestratorImpl::pipeline_completed`.

## Deferred from: code review of 9-4-toast-notifications (2026-05-03)

- **F31 — 60-char Preview cuttet Grapheme-Cluster** [`shells/windows/src-tauri/src/notification.rs:64-67`] — `text.chars().take(60)` arbeitet auf Unicode-Scalar-Values, nicht auf Extended-Grapheme-Clusters. Emoji-/ZWJ-/Combining-Mark-Boundary kann mojibake erzeugen. Defer-Reason: Char-Slicing ist explizit Spec-konform (AC-1 L45 "Char-basiert"); cosmetic für User-Diktat. Migration auf `unicode-segmentation` möglich wenn Preview-Quality-Issue auftritt.
- **F32 — `show()` blockiert den Broadcast-Loop synchron** [`shells/windows/src-tauri/src/notification.rs:88-99`] — `notification().builder().show()` ist sync; bei OS-Toast-Hänger stallt der `recv()`-Loop und Risk steigt für `RecvError::Lagged` auf den anderen Events. Defer-Reason: Win-Toast-COM-Call typischerweise <1ms; spekulativ. Phase-3-Hardening wenn beobachtbar (`spawn_blocking` oder eigener Worker-Thread).
- **F33 — Keine Unit-Tests für `in_session`-Lifecycle in `notification.rs`** [`shells/windows/src-tauri/src/notification.rs`] — Doc-Comment verspricht `MockRuntime`-Test-Pfad, aber kein `#[cfg(test)] mod tests`. Defer-Reason: AC-2-Architektur (M1-Decision oben) bestimmt erst, was getestet werden muss; Tests gemeinsam mit M1-Fix.
- **F34 — App-Exit-Shutdown emittiert kein `RecordingCompleted` → `in_session`-Leak latent** [`klarvo-shell-orchestrator/src/session.rs:361-378`] — Pre-existing aus Story 3.x. Aktuell harmlos weil M1-Pfad (Event::ErrorEmitted auf Bus) dead ist; wird relevant sobald M1-Decision umgesetzt ist. Defer coupled to M1-Resolution.

## Deferred from: code review of story-9.5 (2026-05-04)

- **9.5-DF1 — Error-Path-Tests für `make_export_err`** [`klarvo-core/src/telemetry/export.rs` tests-mod] — Alle 3 neuen Tests sind happy-path; weder `AppErrorKind::ExportFailed` noch `error.telemetry.export.failed`-Key werden assertiert. Failure-Injection (read-only Target-Dir, locked log-file, simulated full disk) braucht entweder `tempfile`-Permissions-Setup oder einen Mock-Filesystem-Layer. Defer-Reason: Coverage-Gap; Test-Infrastructure-Investment lohnt erst wenn weitere I/O-Stories ähnliche Patterns brauchen (vgl. 6.3-W6).
- **9.5-DF2 — `handleExport` Component-Unmount-Race** [`shells/windows/src/index.html:304-315`] — `setExporting(false)`/`setToast(...)` im `finally` werden gegen unmounted Component aufgerufen, falls SettingsPanel während laufendem `await invoke(...)` unmountet wird. React warnt + State-Leak. Defer-Reason: SettingsPanel ist heute permanent gemountet (Tab-Switch toggelt Display, nicht Mount); wird real wenn Settings zur Route wird.
- **9.5-DF3 — `log_dir`-Subdir-Recursion** [`klarvo-core/src/telemetry/export.rs:63-65`] — `if !path.is_file() { continue; }` skippt Sub-Verzeichnisse silent. Spec mandatet explizit non-recursive (Story L79). Defer-Reason: spec-konform; relevant wenn rolling-appender ein Subdir-Layout produziert (kein bekannter Trigger heute). Doc-Note "non-recursive contract" im Header reicht.
- **9.5-DF4 — `USERPROFILE`/Downloads-ist-Datei-statt-Dir** [`shells/windows/src-tauri/src/commands/telemetry.rs:43-47`] — `resolve_export_path`-Filter prüft `parent().exists()`; bei Downloads-als-File passiert `is_file=true`, Filter passes, `File::create` failt unten mit IO-Error. Defer-Reason: extremer Edge; `temp_dir`-Fallback würde im selben Szenario auch nicht greifen ohne weitere Logik. Akzeptabel als IO-Error mit `error.telemetry.export.failed`.
- **9.5-DF5 — `debug_assert!(app.manage(...))` droppt Result in Release** [`shells/windows/src-tauri/src/main.rs:344-358`] — Projektweit-pre-existing-Pattern (orch/config/keystore/emitter/clock/settings/Bus + neu ExportState). Bei doppelter Registrierung (Refactor/Hot-Reload) silent-no-op; Release verwendet die ältere Instanz, Debug paniced. Defer-Reason: Out-of-Scope-Refactor mit 7+ Touch-Sites; eigene Story "manage-result-handling-policy" sinnvoll.
- **9.5-DF6 — `exported_at: 0` Fallback versteckt Clock-Errors** [`klarvo-core/src/telemetry/export.rs:41-44`] — `SystemTime::now().duration_since(UNIX_EPOCH).unwrap_or(0)` ergibt `exported_at: 0` bei Misconfigured Clock. Defer-Reason: minor; Bug-Reporter mit `exported_at: 0` als Diagnose-Signal akzeptabel. Alternative wäre Err-propagation (vermutlich overkill für sysinfo-Header).
- **9.5-DF7 — AC-Coverage für `tempfile`-Dev-Dep fehlt** [Spec AC-2] — Spec AC-2 enumeriert nur `zip = "2"`; `tempfile = "3"` ist Test-Voraussetzung der drei neuen Unit-Tests, aber nicht in einem AC verankert. Defer-Reason: Spec-Amendment-Kosmetik; im Closure-Commit-Body erwähnen reicht. Memory `feedback_adr_amendment_convention`-analog: Amendment-Anhang oder schlicht akzeptiert.
- **9.5-DF8 — Spec-Prosa-Inkonsistenz "einzeiliges Text-File"** [Spec L72] — Prose sagt "einzeiliges Text-File", Format-Block listet 4 Zeilen. Code folgt Format-Block (load-bearing). Defer-Reason: Spec-Cleanup; Code stimmt mit Format-Block überein.

## Deferred from: code review of 9-6-pill-bar (2026-05-04)

- **9.6-DF1 — `pill_bar.show` vs. `pill_bar.waveform_tick` Listener-Ordering** [`shells/windows/src/pill-bar.html:71-75`] — `bins.fill(0)` im show-Listener kann nachträglich einen schon angekommenen waveform_tick überschreiben. JS-EventLoop ist single-threaded → praktisch OK, aber kein Ordering-Contract zwischen den beiden Tauri-Emit-Calls auf Rust-Seite. Defer-Reason: First-frame visual cosmetic; harte Lösung wäre Sequence-Number am Payload + `if seq < lastSeq drop`.
- **9.6-DF2 — First-Press WebView-Listener-Registration-Race** [`shells/windows/src-tauri/src/overlay/pill_bar.rs:88-92`] — `pill_bar.show` kann theoretisch emittiert werden, bevor `<script type="module">` der Pill-Bar-Seite die Listener registriert hat. `visible: false` in `tauri.conf.json` lädt die Seite at-boot; in der Praxis bis zum ersten Hotkey-Press hinreichend lange registriert, aber kein Ready-Handshake. Defer-Reason: Hard to test deterministisch; Phase-3-Hardening wenn observable.
- **9.6-DF4 — `300ms` Fade-Duration als duplizierte Magic-Number** [`shells/windows/src-tauri/src/overlay/pill_bar.rs` `Duration::from_millis(300)` + `shells/windows/src/pill-bar.html` CSS `transition: opacity 300ms`] — Kein shared Constant; Future-Maintainer-Drift produziert visuellen Pop (Hide vor Fade-Ende) oder transparenten Click-Blocker. Defer-Reason: Cosmetic; sauber via `tauri::Builder.manage(FadeConfig)` lösbar wenn Animation-Tunability eine Story wird.
- **9.6-DF5 — Title "Klarvo" auf transparentem `decorations: false`-Window** [`shells/windows/src-tauri/tauri.conf.json` pill-bar.title] — Window pollutet Win32 EnumWindows + Accessibility-Tree-Reads, obwohl Title nirgendwo sichtbar ist. Defer-Reason: Harmless; Tauri verlangt `title` als Pflichtfeld in der Window-Config (oder default-leer setzen ginge auch).
- **9.6-DF6 — `ts_ms` im Ring-Buffer-Push ohne Anti-Jitter** [`shells/windows/src-tauri/src/overlay/pill_bar.rs:866-874`] — Out-of-order broadcast-Delivery (rare) führt dazu, dass ältere Samples neuere überschreiben. Defer-Reason: Visualization-Wonkiness, nicht Safety; `tokio::sync::broadcast` liefert FIFO solange Lag nicht auftritt.
- **9.6-DF7 — Spec-Impl-Divergenz `handle_event` async vs. sync** [`shells/windows/src-tauri/src/overlay/pill_bar.rs:843`] — Spec hat `async fn handle_event(...).await`, Impl ist sync. Body hat kein `.await` (300ms-Sleep läuft im separat gespawnten Task) → funktional äquivalent. Defer-Reason: Spec-Prosa-Cleanup; Code stimmt.
- **9.6-DF8 — `tauriEvent.listen()`-Unlisten-Functions werden nicht gespeichert** [`shells/windows/src/pill-bar.html:55, 71, 79`] — Pill-Bar-WebView lebt App-Lifetime → keine Leak-Sites in Production. Defer-Reason: Dev-Hot-Reload kann Orphans akkumulieren; bei Webview-Recycle-Story (Theme-Wechsel etc.) sauber zu lösen.
- **9.6-DF9 — `PillBar::new()` → `start(self)` API-Smell** [`shells/windows/src-tauri/src/overlay/pill_bar.rs:807-825`] — `new()` returnt `Self`, das sofort von `start(self)` konsumiert wird; effektiv eine Free-Function mit Init-Position-Side-Effect. Defer-Reason: Cosmetic; Refactor sinnvoll wenn `PillBar` jemals State hält (z. B. fade-epoch nach P3).
