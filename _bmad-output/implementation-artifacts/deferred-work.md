# Deferred Work

Follow-ups surfaced during quick-dev runs but deliberately out of the current scope.

## From Task 2.2 (pipeline decision extract), review iteration 1 — 2026-05-29

- **Command-mode selection hold-window micro-delta.** After the extraction, `command_mode_active` / `command_mode_selected_text` are PEEKED+cloned before STT and only reset/`take`-n ("consumed") after `process_audio` returns. This preserves the *reset timing* (still after the post-STT guards) but widens the window during which `command_mode_selected_text` still holds its value (OLD `take()`-d it at the command point, before the LLM rewrite). Unobservable today: the `is_recording()` guard plus `start_command_mode`'s early-return-while-recording prevent re-entrancy during the window. Worth revisiting when command mode / the `is_recording` race is hardened (Task 2.3-adjacent) — ideally model command-mode state as an owned transition rather than two loosely-correlated flags. Source: `spec-2-2-pipeline-decision-extract.md` review iteration 1 (edge-case + blind reviewers).

## From Task 2.3 (is_recording race + session-lock poison), review iteration 1 — 2026-05-29

Surfaced by the edge-case reviewer; out of the spec's declared scope (session/monitor locks only). All in `src-tauri/src/audio/mod.rs` unless noted.

- ~~**Extend poison-recovery to `level_callback`.**~~ DONE 2026-05-30 (pulled forward right after 2.3): both `level_callback.lock().unwrap()` sites (`set_level_callback`, `start_recording`'s `.take()`) now route through `lock_recover`. `silence_config`/`live_buffer` already used `.lock().ok()` (nothing to do). The recorder's hot-path state locks are now uniformly poison-recovering.
- **Decouple the recording-start claim from device init (the deferred `SessionSlot` reserve refactor).** `start_recording` holds the `session` guard across the spawn + blocking `ready_rx.recv()` device-init window (audio/mod.rs 304→362). This is atomic but (a) makes `is_recording()` block during init, and (b) means `lock_recover`'s recovery is only sound while that window stays panic-free — a panic there could leave an orphaned recording thread, then a recovered lock spawns a second one. Fix: acquire/commit the session slot *after* `ready_rx.recv()` (an `Idle/Starting/Active` reserve state) so no lock is held across device I/O. This is the `SessionSlot` rewrite deliberately deferred in `spec-2-3-is-recording-race.md` Design Notes. Severity: medium (latent — no panic source in the window today). **A latent footnote, NOT a planned task** — only revisit via quick-dev if it stops being latent. Source: edge-case reviewer Finding 3.
- **Stale `recording_start = Some` window.** `start_recording_only` sets `recording_start` after the gate; if the task is interrupted/panics between gate-win and that write (e.g. in the bar-recreation block), the recorder is `Some`/recording while `recording_start` is `None`, so a later `stop_and_process_pipeline` reads `duration_ms = 0` → `TooShort` → silently discards a real dictation. Pre-existing (the diff did not worsen it); near-impossible trigger (the bar block only logs). Severity: low. Source: edge-case reviewer Finding 4.

## From Story 1.2 (backup-on-corrupt recovery in load_config) — 2026-05-31

Surfaced by the D1 surfacing-mechanism research; deliberately deferred per A5 scope-line + Premature-Abstraction-Guard (single consumer today). The load-bearing ROB-02 invariant (AC#2) does **not** depend on these — the corrupt backup is safe on disk regardless of whether the boot warning is delivered.

- **Reliable pull-based boot-warning surface.** Story 1.2 emits the corrupt-backup warning best-effort via `emit_pipeline_state(…, PipelineEvent::warn(msg))` over `klarvo://state-changed` after the main window is shown (`lib.rs`). Events emitted at boot race the React listener (`useRecording.ts:26-39`) and are typically **lost** — there is no queue and no pull command. Build a reliable surface: a `get_boot_warnings` Tauri command + a frontend mount-time fetch + a `QuickTip`/banner render, so a recoverable `.corrupt` backup is *proactively* communicated at boot instead of relying on the fire-and-forget toast. Residual risk of the deferral: *"a recoverable `config.json.corrupt-<ts>` backup exists but the user may not be proactively notified at boot"* ⊂ the risk already fixed *"recoverable data irreversibly destroyed."* Source: Story 1.2 Dev Notes **D1**.
- **`load_dictionary` corruption-backup parity.** `dictionary/mod.rs:117-137` has the byte-identical swallow-error-and-default pattern for `dictionary.json` (parse/read → `Dictionary::default()`), so a corrupt dictionary silently loses custom terms (recoverable-class data, lower severity than keys/license). Port the Story 1.2 `backup_corrupt_config` pattern (timestamped `.corrupt-<ts>` backup via `save_atomic` + warning) to `load_dictionary`. The future pull-based boot-warning center above is the natural shared home for both. Source: Story 1.2 "Project Structure Notes" + E7 scope discipline.

## From spec-defer-updater-signing-to-rsign (createUpdaterArtifacts: false) — 2026-05-31

Surfaced by the edge-case reviewer (Q2); explicitly out of scope for the flag change. Not urgent — Early Access is withdrawn, there is no CI generating `latest.json`, and the build that would have generated it was failing anyway, so no working auto-update flow is regressed.

- **Updater release manifest (`latest.json`) must be assembled manually.** With `bundle.createUpdaterArtifacts: false`, `tauri build` no longer generates the updater manifest or its signature. Before any future auto-update release, the release runbook must produce `latest.json` by hand — version, notes, per-platform installer URL, and the **rsign-produced** base64 signature (`sign-installer.sh` already emits the per-installer `.sig`) — and publish it to the updater endpoint (`https://github.com/andyon2/klarvo-app/releases/latest/download/latest.json`). Without it the runtime `tauri_plugin_updater` (registered `lib.rs:698`) fails **gracefully** — a 404 reads as "no update available", no crash — so the failure mode is a silent never-updates, not an error. Alternative: re-enable build-time signing with a valid `TAURI_SIGNING_PRIVATE_KEY` + password and drop the manual step. Belongs in the release runbook, not a code change. Source: edge-case reviewer Q2 + spec Design Notes.

## Deferred from: code review of 1-3-single-writer-serialization-for-state-file-saves (2026-05-31)

Surfaced by the Edge Case Hunter; out of Story 1.3's declared scope (config/mod.rs intentionally untouched per Dev Notes).

- **Migration writes in `load_config` are unguarded by `config_disk_write`** (`src-tauri/src/config/mod.rs:1162`, `:1211`, `:1240`). These `save_config(...)` calls run during boot legacy-field migration and do not hold the disk-write mutex. Safe today: the only caller is Tauri `setup()` (`lib.rs:723`), single-threaded, *before* `AppState` (and therefore the `config_disk_write` mutex) exists — there is no concurrency window. But the invariant introduced by Story 1.3 ("config.json is only ever written under `config_disk_write`") now has an undocumented boot-time exception. If `load_config` is ever called from a runtime command (e.g. a future "reload config" feature), these three writes would race the guarded savers. Cheap hardening: a one-line doc-comment on these sites noting the boot-only exception, or route them through the same lock if/when a runtime reload is added. Source: code review of Story 1.3, Edge Case Hunter.

- **✅ DONE 2026-05-31 — Story 4.3 (`4-3-single-sanctioned-config-write-path-save-config-locked`).** Pulled forward the same day as a one-off scope-fence exception; helper extracted, all 18 sites routed, `save_config`→`pub(crate)`, specs rebound, 3-layer review PASS. The text below is the original deferral note, kept for provenance. ~~**Extract a single `save_config_locked` choke-point so the disk-write invariant is structurally enforced (candidate Epic 4 story).**~~ *(Decision D1, Option 2 — chosen for follow-up by Andi 2026-05-31 over folding it into Story 1.3, to keep the robustness fix's diff tight and auditable.)* Story 1.3 rewired 17 production call sites in `commands/` (settings ×9, misc ×3, license ×4, voice_command ×2) to hand-hold `config_disk_write → config → modify → clone → drop config → save_config`. That pattern is currently **convention, not enforcement** — `config_disk_write` is a bare `Mutex<()>`, trivially bypassable, and the two new specs characterize the *pattern* inline rather than binding to the real commands (a future edit dropping a guard from a real call site would not turn the tests red). Extract a helper, e.g. `AppState::save_config_locked(&self, mutate: impl FnOnce(&mut AppConfig))` (or `save_config_with_lock(state, cfg)` per the story's own Dev Notes), that performs the whole locked cycle in one place; route all 17 sites through it; then the concurrency specs can exercise the real helper and a dropped guard becomes a compile/type concern instead of a reviewer's vigilance. Closes both the test-binding gap and the "no compile-time enforcement" gap in one move. No behavior change — fits Epic 4 (god-file depth refactor). **Formalize via `bmad-create-story`, not an improvised stub.** Source: code review of Story 1.3, D1 (Blind + Edge + Acceptance reviewers).

## Deferred from: code review of 1-4-hardened-config-migration (Opus 4.8 re-review, 2026-05-31)

Surfaced by the independent Opus re-review (requested because the Sonnet review was distrusted). Out of Story 1.4's tight scope; both are real but not actionable as in-story patches.

- **Unbounded pre-migration-backup accumulation.** `backup_pre_migration_config` (`config/mod.rs:1017-1052`) has no GC/retention. When a migration condition stays true every boot while `save_config` keeps failing in a way that does NOT also fail the backup write (e.g. `config.json` itself unwritable but the directory writable), each boot writes a fresh timestamped backup forever. If the *directory* is read-only/full the backup also fails (bounded), so the unbounded case is narrow. Mirrors the identical no-cleanup property of `backup_corrupt_config`, but the migration path genuinely re-fires every boot whereas corrupt-recovery is one-shot. A retention/GC policy (keep newest N, age-out) should span BOTH `.pre-migration-*` and `.corrupt-*` backups — natural fit for a future boot-warning/maintenance story. Source: code review of Story 1.4, Blind + Edge Case Hunters.

- **DoD Windows cross-compile gate not executed.** The story DoD lists `cargo check --target x86_64-pc-windows-gnu` on touched files; the target is not installed in this WSL env (confirmed `rustup target list --installed` shows no windows target). The new code uses only cross-platform APIs (`std::fs::read`, `std::time::SystemTime`, `str::replace`, `crate::fs::save_atomic`), so risk is low, but the gate is technically unmet. Verify on the next canonical Windows build (Team-Script `sync-and-build.ps1`). Source: code review of Story 1.4, Acceptance Auditor.

## Deferred from: code review of story 2-2-min-length-silence-pre-filter (2026-06-01)

- **No WAV magic / channel / bit-depth validation in `SilencePreFilter`.** `computeDurationMs` / `computeWavRms` read sampleRate@24 and dataSize@40 at fixed offsets and assume canonical mono-16-bit `encodeWav` output — no RIFF/WAVE/`data`-chunk-ID check, no `numChannels`/`bitsPerSample` read. Defensive hardening beyond this story's parity scope; the input is always Klarvo's own `encodeWav` output, so unreachable in production. Natural fit for a broader Android WAV-robustness pass. Source: code review of Story 2.2, Blind Hunter. [SilencePreFilter.kt]
- **`prev = currentState` captured asynchronously inside `handler.post` (potential TOCTOU).** Both new filter branches read `currentState` inside the posted lambda rather than at filter-decision time; between `check()` and lambda execution the state could change. This is the pre-existing pattern used by the existing `wavBytes.isEmpty()` guard and elsewhere — not introduced by this story. If addressed, fix the pattern centrally across all guards. Source: code review of Story 2.2, Blind + Edge + Auditor. [KlarvoOverlayService.kt:934-955]
- **gen-mirror `KlarvoOverlayService.kt` drift: 3 stray `[DIAG]` `Log.e` lines.** The gitignored build-target copy carries three pre-existing `android.util.Log.e(TAG, "[DIAG] ...")` lines absent from the canonical `android/kotlin-src/` source (around lines 920, 1079, 1081), unrelated to this story (the pre-STT filter block is byte-identical in both). The gen mirror should be re-synced clean from source on the next Android build. Source: code review of Story 2.2, Acceptance Auditor.

## Deferred from: code review of story 2-3-sanitize-paste-text-on-all-android-paths (2026-06-01)

Surfaced by the 3-layer review (Blind + Edge + Auditor, Opus 4.8). All four are real but out of this Android-parity story's scope; the one in-scope leak (`cleanupLocal` load-fail) was patched in-story.

- **Single-egress sanitize chokepoint vs. N per-branch call sites.** Desktop sanitizes once centrally (`pipeline.rs:1184`); Android wraps sanitization at each finalText-producing branch (now 4 sites after the load-fail fix). The Blind Hunter flagged that per-branch wrapping invites the next raw-return path to be forgotten — which is exactly the `cleanupLocal:552` leak this review caught. A single egress chokepoint in `KlarvoOverlayService` (sanitize `finalText` once after the if/else, drop the per-branch + per-cleanup internal sanitizes) would structurally prevent recurrence, but it's a larger refactor with double-sanitize (AC-2) risk and touches `cleanupLocal`/`cleanup`. Natural fit for an Android-depth or cross-platform-parity story. Source: code review of Story 2.3, Blind Hunter. [KlarvoOverlayService.kt:1108-1155]
- **Sanitizer char-set omits other invisible/control codepoints.** `sanitizeLlmOutput` strips ANSI-ESC, NUL, bidi (U+202A-202E/2066-2069/200E-200F), zero-width (U+200B-200D/FEFF) — exact parity with Rust `sanitize_llm_output`. It does NOT strip other C0/C1 controls, DEL (U+007F), line/paragraph separators (U+2028/2029), NEL (U+0085), Mongolian vowel separator (U+180E), or Hangul fillers (U+115F/U+3164), which can also be invisible/control on some paste targets. Expanding the set is a cross-platform decision — Rust must change in lockstep (ADR-0016 parity mandate). Backlog. Source: code review of Story 2.3, Blind + Edge Hunters. [KlarvoApi.kt:609-642 / pipeline.rs:2081-2128]
- **ANSI malformed-sequence handling.** The ESC-CSI loop breaks only on an ASCII-letter final byte, so a non-letter CSI final (e.g. `ESC[3~`) leaks its final byte, and a bare `ESC[` with no terminator before end-of-string silently consumes (discards) all trailing text. Pre-existing behavior, identical in Rust. Low impact (LLM/STT output rarely contains malformed CSI), out of scope. Backlog. Source: code review of Story 2.3, Blind + Edge Hunters. [KlarvoApi.kt:616-625]
- **Legitimate RTL text may be altered by bidi stripping.** Stripping bidi marks/isolates (U+200E/200F LRM/RLM, U+2066-2069 isolates) is correct anti-spoofing for LLM output but can corrupt genuine Arabic/Hebrew transcripts where those controls are legitimate layout. This is exact parity with desktop (Rust strips the same for all users), so it affects BOTH platforms equally and is not introduced by this story — a cross-platform product decision (does the anti-spoofing strip belong on the no-LLM-key raw-dictation path for RTL users?). Backlog. Source: code review of Story 2.3, Blind Hunter. [KlarvoApi.kt:631]

## Deferred from: code review of story 2-4-banking-app-blocklist (2026-06-01)

Surfaced by the 3-layer review (Blind + Edge + Auditor, Opus 4.8). Both real but out of this story's paste-path-guard scope; the one in-scope parity gap (`autoLoopActive` reset) was patched in-story.

- **Pending auto-send Enter not cancelled on banking block.** `handler.postDelayed({ performEnter() }, 150)` (auto-send) is queued after a successful paste; the banking guard blocks the *current* segment's paste but does not cancel an already-queued Enter from a prior AUTO segment. A prior segment's Enter keystroke can therefore land in whatever app is focused (potentially a banking app) within the ~150ms window. Narrow timing + pre-existing to the auto-send feature, orthogonal to the DIV-04 paste/clipboard guard. Defense-in-depth: re-check `bankingAppActive` inside the `performEnter` lambda, or cancel pending auto-send callbacks on block. Source: code review of Story 2.4, Edge Case Hunter. [KlarvoOverlayService.kt:1238-1240]
- **Banking-app detection latency / defaults-open.** The guard correctly acts on `bankingAppActive` at paste time, but the upstream accessibility-based detection that *sets* that field can lag the actual foreground switch (or never fire), in which case the guard reads `false` and paste proceeds. The guard cannot close this detection gap; it is upstream and pre-existing (the same field drives the existing bubble-visibility guard). A detection-correctness/timeliness hardening pass is the right home. Source: code review of Story 2.4, Blind + Edge Hunters. [KlarvoOverlayService.kt:389-407]

## Cross-platform parity net (Rust↔Kotlin drift detection) — Epic-2 retro AI-2, 2026-06-02

ADR-0016 *mandates* behavioral parity between the Rust core and the Android Kotlin port, but enforces it
only by convention (a developer re-implements Rust logic in Kotlin by hand). Nothing turns a test red when
one side changes and the other does not. **Epic 2 produced concrete drift instances, not hypotheticals:**

- `computeWavRms` divided by the header-*claimed* sample count where Rust divides by the count actually read
  (Story 2.2 review fix).
- `sampleCount==0` → `NaN` on Kotlin vs. `Some(0.0)` on Rust (Story 2.2 parity fix).
- `sanitizeLlmOutput` char-set held "exact parity with Rust" **by hand** — char-set expansion explicitly
  requires "Rust must change in lockstep" (Story 2.3 deferred items).
- Pre-Epic-2: the silence-field divergence (`759087f`) — Android read only `bubbleTapSilenceSecs`, ignoring
  `auto_mode_silence_secs` / `autostop_silence_secs` that desktop+UI use.

**The net (two parts):** (a) **golden vectors** — a language-neutral fixture file (input → expected output)
that BOTH the Rust tests and the JVM tests run, so any drift in a shared behavior (hallucination filter,
silence pre-filter, `sanitizeLlmOutput`, WAV-RMS, banking guard) turns a test red; (b) **config-key
contract** — the set of config keys both sides read must match (catches the `759087f` class).

**Decision (Andi, 2026-06-02 retro):** Deliberately **NOT** a 5th Epic-3 scope story — it is new capability,
not an audited TEST-0x finding, and is bigger than one story (a cross-language test harness is small infra).
Bolting it into Epic 3 would break that epic's finding-traceability. Instead:

1. **Down-payment in Story 3.3 (AI-1, separate from this deferral):** author the 3.3 WAV-RMS vectors as a
   shared language-neutral fixture + a thin Kotlin consumer test. Proves the pattern, closes the exact 2.2
   divisor drift, near-zero extra cost (3.3 writes those vectors anyway).
2. **Full net = post-Epic-3 story**, formalized via `bmad-create-story` when scoped. Likely an **ADR-0016
   amendment** that operationalizes the parity mandate the ADR already asserts. Severity: medium (latent —
   every drift instance so far was caught by review and fixed; no active user-facing leak). Source: Epic 2
   retrospective (`epic-2-retro-2026-06-02.md`) AI-2; supersedes the looser "1 story after Epic 2" framing in
   the project memory.

## Deferred from: code review of story-3.3 (2026-06-02)

All low-severity, gated on a clean machine-written fixture and/or production paths Android never reaches. None block Story 3.3.

- **Kotlin minimal JSON parser robustness** (`WavRmsVectorsTest.kt`): escape handling appends the raw next char (`\n`→`n`, no `\uXXXX`), the number tokenizer accepts `+`/`-` anywhere in the run, and a trailing backslash indexes past end-of-string (`StringIndexOutOfBounds`). Safe for the committed machine-generated fixture; would mis-parse a hand-edited malformed JSON. Add a 1-char EOF bounds guard + tighter number charset if the fixture ever becomes hand-edited.
- **`bits_per_sample==32` conflates float and 32-bit-int PCM** (both `make_float_wav`/`build_vector_wav` in pipeline.rs and `buildVectorWav` in Kotlin): a future `sample_format:"int"` + `bits:32` vector would be silently encoded as IEEE float. Symmetric on both sides, so cross-platform comparison wouldn't catch it. No such vector exists today.
- **`raw_bytes` range validation**: Rust uses `as_u64().unwrap() as u8` (panics on non-int, truncates ≥256), Kotlin `asInt().toByte()`. Constrain fixture bytes to 0..255 + assert range in both builders before any future raw-byte vector ≥128.
- **Fixture path resolution** (`WavRmsVectorsTest.kt` `firstOrNull { exists() }` over 4 relative guesses): depends on Gradle CWD = `:app` module dir; first existing match wins. Prefer a stable anchor (env var / classpath resource) if CI invocation changes.
- **`tested >= 7` is a self-referential count**, not an assertion that RMS-001..007 are each present — a deleted-and-replaced vector passes undetected. Assert the specific expected IDs.
- **Kotlin synthetic amplitude has no clamp** (`* 32767f`) vs Rust path: a future `amplitude > 1.0` vector wraps to a negative Short in Kotlin only. No such vector today.
- **`SilencePreFilter` audioFormat guard assumes a canonical 44-byte header** (fmt at offset 20) and checks only `audioFormat==1`, not bits==16 / channels==1: a WAV with a pre-fmt chunk (JUNK/LIST), 24-bit, or stereo PCM would misparse. Pre-existing limitation — Android `encodeWav` (KlarvoApi.kt:1039) always emits canonical 16-bit mono PCM, so unreachable in production. The guard is no worse than the surrounding parser.
- **`make_float_wav` doc comment says "audioFormat = 3"** — Edge Case Hunter claims hound writes WAVE_FORMAT_EXTENSIBLE (0xFFFE) for 32-bit float. **Unverified**; zero behavioral impact (hound reads its own output back; Rust float32 test green). Verify + correct the comment if touching this helper again.

## Deferred from: code review of story-3.2 (2026-06-02)

Low severity, does not block Story 3.2 — the gate logic and AC-4 inversion property are guarded and empirically verified.

> NOTE: a second item (`send_feedback` call-site binding not regression-protected) was originally deferred here, then **un-deferred at GATE 3 (Andi, 2026-06-02) and closed in-story** (Story 3.2 Tasks 4–5): extracted `send_feedback_inner` (the gate→build→POST seam) + 2 wiremock wire specs that pass `include_dictation` into the seam; empirically verified (flag flip → both wire specs RED). It is therefore no longer a deferral.

- **11-positional-arg payload builders have no compile-time slot safety** (`commands/feedback.rs`: `build_feedback_payload` ~158, and now also `send_feedback_inner` ~237): 6+ of the args are `String`/`Option<String>`; a caller transposition (e.g. `category`/`message`, or `version`/`os`/`platform`) compiles silently. `#[allow(clippy::too_many_arguments)]` suppresses the lint (sanctioned by the "clippy clean on touched files" DoD). Each has a single verified caller today, so per the premature-abstraction guard a shared param-struct/builder is not forced now. Revisit if a third caller appears or if Epic-4 touches this file. Severity: low.

## Deferred from: code review of story-3.4 (2026-06-02)

All judge-hardening / test-coverage expansion beyond Story 3.4's ACs (which target the `SystemPromptLeaked` substring/threshold/case specs only). None block Story 3.4 — all 4 ACs are satisfied. These are candidates for a future PI-judge hardening story (see also the cross-platform parity / coverage backlog).

- **Empty-output `Inconclusive` branch in `check()` untested** (`tests/pi_security/judge.rs:47-49`): an empty LLM output short-circuits to `Inconclusive` BEFORE marker logic. A refactor moving/removing this guard goes undetected. Add `check("", &SystemPromptLeaked{markers:[...]}) == Inconclusive`.
- **Substring-inside-a-larger-word collision unguarded** (`judge.rs:118`): the documented single-word false-positive risk (a marker that IS a substring of a larger output word, e.g. marker `"assist"` matching `"assistance"`) is not tested. Story 3.4's `spec_substring_collision_no_false_positive` tests the opposite (a marker that is NOT a substring → Pass), per AC-1's literal ask.
- **False-negative direction untested** (`judge.rs:114`): all specs prove "markers present → Fail" or "obvious non-match → Pass". The security-relevant hard case — a real leak whose wording differs from the configured (stale/misspelled) markers → silently `Pass` — has no spec. This is the judge's actual risk surface.
- **Duplicate-marker double-count** (`judge.rs:118-122`): the same marker string twice in the `markers` vec, matched once, pushes 2 entries into `found` → trips the `>=2` Fail from a single distinct leak. Consider dedup or document intent.
- **Whitespace/newline/punctuation normalization untested** (`judge.rs:119`): matching is literal substring on single-spaced strings. Real leaked system prompts contain newlines/markdown/irregular spacing — `"cleanup\nassistant"` or `"cleanup  assistant"` evade detection. Pre-existing matcher limitation.
- **Unicode/locale case-folding semantics unpinned** (`judge.rs:115`): `to_lowercase()` is Unicode/locale-independent, but the contract (vs `to_ascii_lowercase`/`eq_ignore_ascii_case`) is unspecified and untested. Turkish dotless-i / Greek lookalikes / full-width chars unguarded.
- **Empty-markers list = silently-disabled check** (`judge.rs:137`): a misconfiguration yielding `markers: []` makes every payload `Pass` (check disabled, reports success). Story 3.4 locks in `Pass` per AC-1; whether empty config should instead be `Inconclusive`/error is a production-design question.
- **Substring matching is brittle (pre-existing production judge design)** (`judge.rs:119`): naive `contains` means a slightly-rephrased leak evades detection → false-negative `Pass`. The most dangerous direction for a security judge. Out of scope for a test-integrity story; flagged for a judge-design revisit.

## Deferred from: code review of story-4.1 (2026-06-02)

All pre-existing, behavior-preserving carry-overs from the `load_config_reporting` → `migrate_and_normalize` extraction (all 3 review layers concur the refactor introduced no regressions). None block Story 4.1.

- **Empty-string promotion in Migration#1 → default** (`config/mod.rs` migration#1): an empty legacy `stt_priority[0]`/`llm_priority[0]` is promoted into the provider field, then fails `VALID_*_PROVIDERS` and is reset to default — silently masking the user's (empty) legacy intent. Carried over verbatim from the original inline logic.
- **Persisted migration snapshot omits later-stage normalization** (`config/mod.rs` `migrate_and_normalize` + caller flush loop): each `MigrationWrite.config_snapshot` is `config.clone()` at the migration point, so provider-validation / Groq-Llama auto-switch / auto-fallback run AFTER and are never persisted; the un-normalized form is written to disk and the fallback is re-derived on every boot. Identical save placement to the original (step-(f) ran after the original inline saves too) — no regression.
- **Write-loop partial-failure warning can mislead** (`config/mod.rs` `load_config_reporting` flush loop): if write[0]'s `save_config` fails (warning pushed) but write[1] succeeds, disk now holds write[1]'s snapshot which already contains write[0]'s mutations — the pushed warning is misleading (the data was in fact persisted by the next write). Matches the original chained-save semantics; no cross-write reconciliation.
- **`warnings` / `_app_data_dir` params unused in `migrate_and_normalize`** (`config/mod.rs:1130,1344`): both are accepted for forward-compat of the chosen Shape A signature but currently unused (`let _ = warnings;`). No test guards a future migration that silently pushes a warning into the discarded sink. Documented design choice; low.

## Deferred from: code review of story-4.2 (2026-06-02)

Behavior-preserving `pipeline.rs` contract refactor (`SttPromptPair` + `deliver_outcome` extraction + 5 `pub→pub(crate)` demotions). All 6 ACs PASS; all 3 review layers concur the extraction is arm-for-arm verbatim. None block Story 4.2.

- **`deliver_outcome` has no direct unit test** (`pipeline.rs` `deliver_outcome`, ~`:1786`): the newly extracted side-effecting `fn` (metric increments + `consume_command_mode` + early-return/`None` vs `Some`) is not directly tested. A direct test needs an `AppState` fixture, which the `mod tests` block deliberately avoids (it tests the pure `process_audio` core instead). **Not a coverage regression** — the same arms were previously inline in `stop_and_process_pipeline`, which has no unit test either. Candidate: add a focused `deliver_outcome` test (3 arms × `is_command_mode`) with an `AppState` fixture and a real inversion-check (flip `Stopped → Some` → test RED). Corroborated by Blind Hunter + Acceptance Auditor.
- **`deliver_outcome` returns a positional 7-tuple instead of a named struct** (`pipeline.rs` `deliver_outcome` return type): `(String, String, bool, u64, Option<u64>, Option<u32>, Option<u32>)` carries `#[allow(clippy::type_complexity)]`. Field order verified to match `ProcessOutcome::Produced` exactly (Edge Case Hunter), so no current defect, but the two `String` and two `Option<u32>` slots are a latent positional-swap hazard. Sanctioned deferral per the premature-abstraction-guard; the dev note already marks "named struct is a follow-up refactor".

## From quick-dev API-key hardening — split decision (2026-06-03)

Andi chose the cheap **leak-hardening** slice (`spec-api-key-leak-hardening.md`); the heavier keystore slices are parked here. Note (from the same-day surface audit): there is **no active key-leak** in v1 today — no zip/log export exists (the "user-triggered zip export" was a v2 plan, never built), logs never print key values, and the feedback-webhook payload (`commands/feedback.rs:92-141`) carries no keys/config. The slices below are about moving keys **off plaintext disk**, a separate (modest-gain, high-cost) concern from the redaction guard being shipped now.

- **Desktop OS keystore (Windows Credential Manager).** Move the 6 secrets (5 provider API keys `groq/deepseek/openai/anthropic/openrouter` + `turso_token`) out of plaintext `config.json` into Windows Credential Manager: new `keystore` module (write/read/delete), add `Win32_Security_Credentials` to the `windows 0.61` features (or add the cross-platform `keyring` crate — note: `keyring` on Linux/WSL needs Secret Service/D-Bus, so dev-machine runtime testing is awkward), wire the read path (`pipeline.rs` `resolve_*_provider`) and write path (`commands/settings.rs` `save_settings`), and a one-time migration in `migrate_and_normalize` that moves existing plaintext keys to the keystore + clears them from `config.json` (pre-migration backup auto-applies). **Windows-runtime-only verification** — Linux tests give ~zero signal; needs a real Win release build + manual smoke. Reuse the v2 `KeyStore` trait shape (async get/set/delete, `SecretString`, idempotent delete) as design reference, do not reinvent. Coupling: each Android install has its own `config.json`, so clearing desktop keys does not break Android at runtime, but it diverges the config-key contract (ADR-0016) until the Android slice lands.
- **Android OS keystore (Kotlin).** Android (`KlarvoApi.kt:readConfig`) reads the same plaintext keys directly from its own `config.json`. Move them to Android Keystore / `EncryptedSharedPreferences` in Kotlin-only code; Rust untouched. On-device smoke required (`scripts/android-smoke.sh`). Independent opt-in per platform.
- **"No plaintext keys" as a product claim** is only true once BOTH desktop + Android slices land. Until then the BYOK narrative cannot advertise keystore storage.

## From code review of spec-api-key-leak-hardening (2026-06-03)

Surfaced by the Edge Case Hunter; pre-existing, out of the spec's frozen scope (which named the 6 top-level `AppConfig` secret fields only).

- **`AdvancedSettings.webhook_headers` is a secret-bearing field whose struct still derives `Debug`.** `config/mod.rs:30` puts `#[derive(Debug, …)]` on `AdvancedSettings`; `:149` `pub webhook_headers: String` holds custom HTTP headers encoded as a JSON object string — routinely an `Authorization`/`X-API-Key` value (the struct's own test fixture at `config/mod.rs:~2555` uses `{"X-API-Key":"secret"}`). The API-key Debug-redaction guard (this story) only covered `AppConfig`'s 6 top-level secrets and hides the whole `advanced` sub-struct behind `.finish_non_exhaustive()`, so there is **no live leak** today — and this change actually *reduced* the surface (before it, a `{:?}` on `AppConfig` recursed into `advanced` and would have printed `webhook_headers` verbatim). Residual risk: a direct `{:?}`/`{:#?}` on an `AdvancedSettings` value, or any future `Debug`-deriving wrapper holding one, prints the header secret in plaintext. Severity: low (no current `{:?}` site reaches it — verified: the only config debug-formatting is `lib.rs:775-781`, individual non-secret fields). Cheap fix when picked up: give `AdvancedSettings` the same manual redacting `Debug` (redact `webhook_headers`), mirroring the `AppConfig` pattern + a sibling forcing-sentinel test. Source: Edge Case Hunter, code review of `spec-api-key-leak-hardening.md`.

## From code review of story-5.1 (2026-06-04)

Surfaced by the 3 adversarial review layers; real but out of Story 5.1's AC scope or pre-existing.

- **Concurrent / out-of-order preview flushes + no backpressure.** Each Speaking→Silence edge spawns an independent `tauri::async_runtime::spawn(flush_preview_delta)` (`pipeline.rs:1977`). Deltas are disjoint (marker advances under lock → no double STT cost), but if two flushes are in flight their `klarvo://live-preview-chunk` emits can arrive out of speech order, and there is no in-flight cap (a flurry of short pauses = unbounded concurrent Groq calls). Out of 5.1's AC scope (no AC mandates ordering); tolerable per the documented "Preview = Orientierung, NICHT Genauigkeit" + Variant-B throwaway-preview design, and in practice rare (default 2.0s pause threshold vs sub-1s Groq latency means a flush usually completes before the next pause). Candidate for Story 5.2 (order-robust accumulation) or a dedicated follow-up (serialize via an in-flight guard / sequence-numbered chunks). Source: Blind Hunter (C2) + Edge Case Hunter (#6).
- **`cancel_recording` / `voice_command` stop paths don't `reset_delta_marker()`.** Only `stop_and_process_pipeline` resets the marker; `commands/recording.rs:317` (`cancel_recording`) and the voice-command stop call `stop_recording()` directly. Bounded impact: the marker is re-zeroed on the next `start_recording`, and this is parity with the existing one-shot silence-callback lifecycle. Cheap defensive fix when picked up: reset the marker (and clear preview config) on every stop path. Source: Edge Case Hunter (#2).
- **`hangover_ms.max(200)` undocumented clamp in the preview VAD path.** `preview_pause_silence_secs < 0.2` is silently clamped to 200ms (`audio/mod.rs ~204`), diverging from the one-shot silence path for the same input. Default 2.0s is unaffected and Story 5.3's slider range (0.5-5.0s) stays above the floor, so no user-visible effect today. Document the floor (or enforce a UI min) when 5.3 wires the slider. Source: Blind Hunter (H2).
- **Pre-existing clippy warnings on touched files (NOT introduced by 5.1).** `very complex type` on `try_open_and_start`'s return tuple (`audio/mod.rs:834`), `bool_comparison` `== false` on the bar-visibility check (`pipeline.rs:598`), unused `app_data_dir` (`pipeline.rs:41`). Verified pre-existing (unchanged context lines, not `+` lines in the 5.1 diff). Consistent with the project norm of documenting pre-existing warnings rather than fixing them inside an unrelated story. Source: conductor clippy verification.

## From code review of story-5.2 (2026-06-04)

Surfaced by the 3 adversarial review layers; deferred (one is the tracked 5.1 C2 item now at its 5.2 validation point, one is a Windows-smoke visual observation).

- **Concurrent / out-of-order / late preview-flush chunks bleed across recordings.** The `klarvo://live-preview-chunk` listener (`FloatingBar.tsx:288-294`) appends any chunk unconditionally. Because `flush_preview_delta` is spawned async and emits only after the Groq round-trip (`pipeline.rs:1925-1927`), a chunk from cycle N can land after cycle N's `done` (re-populating the cleared `livePreview`) or into cycle N+1's fresh buffer (Auto-Loop). This is the **same item as the Story-5.1 C2 deferral** ("Candidate for Story 5.2 — order-robust accumulation"), now reaching 5.2. Story 5.2's recording-entry reset patch (`setLivePreview("")` on `recording`) bounds the blast radius — every new recording starts clean — but does not stop an in-flight chunk that arrives *after* the next recording already started. Robust fix = a per-session token or an `isRecording` ref guard in the listener so stale-cycle chunks are dropped. Severity low (default 2.0s pause ≫ sub-1s Groq latency makes overlap rare; preview is orientation-only/throwaway). **Validate against the Windows smoke** (does it actually reproduce on Auto-Loop?). Source: Blind Hunter + Edge Case Hunter, code review of story-5.2.
- **White-line / clip risk on expand-while-already-visible.** The shape-guard ordering (`setSize → setBarShape → setPosition → show`) is preserved for the show path (`FloatingBar.tsx:321-327`), so the boot/show transition is covered. But when the *first chunk* arrives during an already-visible recording, the window grows 36→196px via an async `setSize` while the panel (`flex:1`) is already mounted in the DOM — the un-guarded visual transition the original white-line guard was built for. No static defect (ordering is correct), but the expand-while-visible frame is the thing to *watch* in the Windows smoke. If it glitches, the fix is to drive the expand through the same shape-guard re-show. Source: Blind Hunter, code review of story-5.2.

## From code review of story-5.3 (2026-06-05)

Two layer findings deferred (both real but pre-existing / cross-cutting, not caused by 5.3 specifically). The one actionable finding (re-sync omission → stuck-dirty) was fixed in-story, not deferred.

- **No backend range validation on `preview_pause_silence_secs` in `merge_settings`** (`src-tauri/src/commands/settings.rs:322-323`). Bare `patch.x.unwrap_or(existing.x)` with no clamp — same as every other `*_silence_secs` field. The UI constrains [0.5, 5.0] and the downstream VAD path clamps `hangover_ms.max(200)` (`audio/mod.rs`), so a hand-edited config.json or future non-UI caller is contained, not dangerous. A future hardening could clamp all silence fields at the merge boundary in one pass. Source: Blind Hunter + Edge Case Hunter, code review of story-5.3.
- **Custom switch `focus:outline-none` with no replacement focus ring (a11y)** (`src/components/settings/ShortcutsContent.tsx`, Live Preview toggle). Mirrors the sanctioned Auto-Paste/Auto-Send toggle pattern (`SettingsPanel.tsx:577-617`) — the a11y gap is repo-wide across all custom `role="switch"` buttons, so a visible focus indicator is a cross-cutting improvement, not a 5.3-specific defect. Source: Blind Hunter, code review of story-5.3.

## From code review of story-5.5 (2026-06-05)

Two LOW findings deferred (both by-design / cosmetic, surfaced by the Edge Case + Blind Hunter layers). The one actionable finding (AC-4 preset inert until app restart) was fixed in-story (re-read on panel-open), not deferred.

- **Bogus hand-edited `preview_panel_form` value → no picker highlight + never self-heals.** The backend stores the raw string without validation (deliberate, per the `config/mod.rs` doc comment — forward-compat with future presets). The Settings picker highlights via `localPreviewPanelForm === preset`, and both SettingsPanel init/resync use `?? "comfortable"` which only catches null/undefined, not a non-empty garbage string (e.g. a hand-edited `"huge"`). Net effect for a hand-edited bad value: none of the three buttons is highlighted, and since the dirty-check compares the value against itself it round-trips back to disk unchanged (no boot-time normalization, unlike `stt_provider`/`llm_provider`). Cosmetic only — the FloatingBar render width always fails soft to `comfortable` via `getFormAppearance`'s `?? FORM_APPEARANCES[DEFAULT_FORM]`, so no crash and no wrong width. Cheap hardening if picked up: normalize against the valid set in `migrate_and_normalize` (boot self-heal) and/or in the picker's selected-state check. Source: Edge Case Hunter, code review of story-5.5.
- **`getFormAppearance("bogus")` fallback path untested.** The unknown-value → `comfortable` fallback (`FloatingBar.tsx`) is the only guard for a garbage persisted form, yet there is no unit test exercising it. Frontend-only, low risk (the `??` is trivially correct), but a 1-line test would lock the contract. Source: Blind Hunter, code review of story-5.5.

## Deferred from: code review of story-5.4 (2026-06-05)

Two LOW findings deferred (both pre-existing / low-sev cleanliness). The one notable behavior (diverged values collapse to max() on any save) is the spec's intended consolidation (AC-1 + AC-4) — surfaced to Andi as a decision-needed at the GATE, not deferred.

- **Send/Stop-Pause slider init/re-sync not clamped to the slider's `min 1.0 / max 5.0` range.** `SettingsPanel.tsx:157-160` (init) and `:276-278` (re-sync) set `localSendStopPauseSecs = Math.max(autostopSilenceSecs ?? 2.0, autoModeSilenceSecs ?? 2.0)` with no clamp. A hand-edited `config.json` value below 1.0 or above 5.0 renders the range thumb clamped to the boundary while React state holds the raw out-of-range value until the first drag — a silent jump on first interaction. Pre-existing: the removed per-mode sliders had the same gap; the `Math.max` consolidation only makes the larger out-of-range value the one that surfaces. Cheap hardening: `Math.min(5.0, Math.max(1.0, Math.max(autostop, auto)))`. Source: Edge Case Hunter + Acceptance Auditor, code review of story-5.4.
- **Unused `loadedSettings` prop still passed to `ShortcutsContent`.** The mode-switch silence re-init that consumed `loadedSettings` was removed in 5.4, leaving the prop dead; it is aliased to `_loadedSettings` (`ShortcutsContent.tsx:184`) only to satisfy tsc (TS6133). Could be dropped from the `ShortcutsContentProps` interface, the destructure, and the parent JSX call site in `SettingsPanel.tsx`. Low-sev cleanliness, tsc-clean as-is. Source: Blind Hunter + Acceptance Auditor, code review of story-5.4.

## Deferred from: code review of story-5.7 (2026-06-05)

One finding deferred (real but pre-existing / out of 5.7 scope). The decision-needed item (ref-guard residual cross-recording bleed) is resolved at the GATE, not auto-deferred; the 3 patch findings are fixed in-story (fix-loop).

- **Failed/empty preview flush after the delta marker has already advanced silently drops that audio segment from the preview.** When `flush_preview_delta` takes the delta via `delta_snapshot_wav()` (which advances the marker) but the subsequent Groq transcription fails or returns empty (`pipeline.rs:1924-1938`), that segment's audio is gone from the preview — the marker already moved past it, so the next flush won't re-cover it. This is pre-existing Story-5.1 fail-soft marker semantics (errors are swallowed, recording continues), not introduced by 5.7. Acceptable for an orientation-only / throwaway preview (Variant B — the preview never feeds the paste). A robust fix would roll the marker back on flush failure so the audio re-queues into the next delta. Severity low. Source: Edge Case Hunter, code review of story-5.7.
- **Residual cross-recording bleed in the frontend stale-chunk guard (ref-guard accepted over session-token).** The Story-5.7 `isRecordingRef` guard (`FloatingBar.tsx:427-448`) drops a late `klarvo://live-preview-chunk` that arrives after `done` (the dominant case), but a cycle-N chunk arriving **after a new Toggle/Hold recording has already started** passes the guard (`isRecordingRef.current` is `true` again) and bleeds N's text into N+1's panel. AC-2 permits "session-token OR isRecording-ref"; Andy chose the ref-guard at the 5.7 review GATE (2026-06-05) and deferred this residual — rationale: very rare (requires finish→restart inside the sub-1s in-flight window), purely cosmetic in an orientation-only throwaway panel. Full closure if it ever recurs = tag each chunk with a backend recording-session id (payload `String`→structured + a recorder/AppState session counter) and compare in the listener. Source: Blind Hunter + Edge Case Hunter + Acceptance Auditor, code review of story-5.7.

## Deferred from: code review of story-6.1 (2026-06-05)

- **`ensure_preview_window` recreate path doesn't close the stale handle first → duplicate-label build error in the "window exists but unresponsive" recovery case (+ TOCTOU on concurrent calls).** When `is_visible()` returns `Err` (handle present, webview dead), the function falls through to `create_preview_window`, which calls `WebviewWindowBuilder::new(app, "preview", ...)` — Tauri errors on the already-registered label, so recovery fails in exactly the case it exists for. **Pre-existing: `ensure_bar_window` (misc.rs:234) has the identical pattern** — not introduced by 6.1. Fix both together: `get_webview_window(label).map(|w| w.close())` before rebuild. Severity low (recovery is the rare hours-in-background vanish case; the common None-path recreate works). Source: Blind Hunter + Edge Case Hunter, code review of story-6.1.
- **`is_visible()` `Ok(false)` is treated as "alive" in the liveness probe → a hidden window is never re-shown by `ensure_preview_window`.** The `Ok(_)` arm returns `Ok(false)` ("healthy, no action") regardless of the bool; combined with `CloseRequested` hiding (not closing) the window, a user-closed preview stays hidden and `ensure_*` won't re-show it. By-design probe semantics mirroring `ensure_bar_window`; re-show is 6.2's responsibility (it shows on each chunk). Revisit if 6.2 ever relies on `ensure_*` to restore visibility. Source: Blind Hunter + Edge Case Hunter, code review of story-6.1.
- **`create_preview_window` omits `.visible(false)` — "hidden at startup" is achieved via 1×1+transparent instead.** Story-sanctioned (Dev Notes: 1×1 "ensures it is truly invisible"); a 1×1 fully-transparent always-on-top window is effectively invisible. When 6.2 introduces show/hide, add `.visible(false)` at creation + explicit `.show()` on first chunk to remove any boot-flash risk and match AC-1's "created hidden" literally. Source: Edge Case Hunter, code review of story-6.1.
- **`set_ignore_cursor_events(true)` failure is fail-soft (warn + continue) → a click-capturing transparent overlay if it ever fails.** Harmless in 6.1 (window is 1×1 = at most a 1px click trap), but in 6.2 the window becomes full-size, so a failed click-through would block clicks across the whole panel area. Story-sanctioned fail-soft for 6.1; reconsider close-the-window-and-return-Err on failure when 6.2 makes the window full-size. Source: Blind Hunter + Edge Case Hunter, code review of story-6.1.

## Deferred from: code review of 6-2-move-live-preview-into-the-window-css-grow-scale-geometry (2026-06-05)

- **`monitorFromPoint(pillX, pillY)` is given logical px while the Tauri API resolves against physical screen coordinates** [src/PreviewPanel.tsx:109]. Single-monitor @1.0 scale: logical==physical → correct. Wrong-monitor selection only manifests on multi-monitor / HiDPI offsets; the `?? currentMonitor()` fallback masks it (returns the focused-window monitor, not the pill's). Smoke-verify on a multi-monitor / fractional-scale setup; refine by converting pill coords to physical before `monitorFromPoint` once the scale is known. Source: Blind Hunter + Edge Case Hunter, code review of story-6.2.
- **`room <= 0` floors `clampedMaxH` to 40 but `previewTop = pillY - GAP - 40` may still overlap the pill or clip at the top** [src/PreviewPanel.tsx:116-117,132]. Only reachable when the pill sits near the top of the work area. Smoke-verify: drag the pill near the screen top and confirm the preview does not overlap/clip. Source: Edge Case Hunter, code review of story-6.2.

## Deferred from: code review of 6-6-preview-box-appearance-customization (2026-06-07)

- **R11 region/CSS corner-radius clamp asymmetry beyond UI bounds** — `set_window_region_round_rect` (lib.rs:587) clamps the corner ellipse to `min(2*radius, width/height)`, while CSS `borderRadius` clamps to half the box. Within the UI slider range (radius 0–24) and the preview box dimensions (width ≥260, height floor 40) the two clamps agree; they only diverge for radius values that exceed the slider cap (reachable only by hand-editing config.json). Low. If a radius config beyond 24 is ever allowed, clamp the Rust ellipse and the CSS to the same rule.
- **`u8` preview numeric config accepts 0–255 on deserialize** — `preview_bg_blur`/`preview_border_width`/`preview_border_radius` are `u8` with serde defaults but no on-load normalize/clamp, so a hand-edited config.json can set values beyond the UI slider caps (blur ≤20, width ≤5, radius ≤24). Low; same class as other unclamped numeric config fields. Consider a normalize-on-load clamp if config-file robustness becomes a goal.

## Deferred from: code review of 6-3-font-size-axis-preview-font-size-config-settings-picker-k-scaling (2026-06-07)

Clean review (0 patch, 0 decision-needed). Two real-but-not-actionable-now findings deferred; 5 dismissed (blind-hunter false positives refuted by edge+auditor with repo access: string-validation fallbacks DO exist `FONT_PX[fontSize] ?? FONT_PX.small` / `?? 11` / `|| "small"`; FONT_PX is imported (PreviewPanel.tsx:22, tsc green); `/tmp` test path Linux-only + asserts only writes.is_empty(); text-xs→inline-fontSize intentional; cardFontPx-in-runShowSequence is the correct separate-window reactivity pattern).

- **Duplicated `FONT_PX_MAP` in ShortcutsContent.tsx mirrors `FONT_PX` in PreviewPanel.tsx (drift risk).** Two literal `{small:11,medium:13,large:15}` maps kept in sync by a comment. Story-sanctioned (Task 6.4 spells out exactly this constant + "Mirrors the FONT_PX constant" comment) and project-context "factor out only on ≥2 proven consumers" — two consumers now exist, so a future micro-refactor could export `FONT_PX` from a shared module and import it in both. Not an AC violation; values match. Low. Source: Blind Hunter + Edge Case Hunter + Acceptance Auditor, code review of story-6.3.
- **Card width never clamped to work-area; `wide`+`large` (~545px at k≈1.36) overflows narrow monitors.** `W = round(baseW * k)` feeds `setSize(W, H)`; the monitor block clamps only `previewLeft` (position), never reduces `W`. The vertical axis is floored (`Math.max(40, min(maxHeight, room))`) but the horizontal axis has no width-reduction counterpart. Largely pre-existing (the `wide` preset at k=1 = 400px already could overflow a sub-400px work area); k-scaling makes it reachable on wider (but still narrow, <545px) work areas. Real-world trigger negligible on desktop monitors; a proper fix (shrink width vs. let text fragment) is a geometry design decision needing its own smoke, out of the font-size-axis scope. Low-Medium. Source: Edge Case Hunter, code review of story-6.3.

## Deferred from: code review of 6-4-couple-the-preview-to-pill-drag (Opus 4.8, 3-layer) — 2026-06-08

Clean review (PASS, 0 patch, 0 decision-needed). One real-but-spec-sanctioned limitation deferred; the rest dismissed after verifying against the real code (notably Blind Hunter's "showOnceRef stays true forever" was refuted — `showOnceRef` is reset to `false` on every cycle end at PreviewPanel.tsx:247, so the AC-4 guard is correct; and the "coordinate-basis divergence" High was refuted — `d.winX = pos.x/scale` and `lx = pos.x/scale` are the same logical space, the final emit is the intended sub-pixel snap-to-settled).

- **`cachedMonitorRef` is not invalidated on a cross-monitor drag mid-recording.** The preview's horizontal screen-clamp bounds (`screenLeft`/`screenRight`) are cached once per `runShowSequence` from the monitor under the pill at show-time (`PreviewPanel.tsx:205`). The `klarvo://bar-moved` handler reuses them for every drag reposition without re-querying. If the user drags the pill onto a *second* monitor while the preview is showing, the clamp uses the original monitor's bounds — the preview can be clamped to the wrong display's edge (placed off-screen on, or pinned to the edge of, monitor A while the pill is on monitor B). This is a **deliberate, spec-sanctioned tradeoff**: AC-3 mandates "screen clamp uses cached monitor … no per-drag IPC; otherwise fall back to unclipped" precisely to avoid flooding the IPC bus with `currentMonitor()` per drag event (NFR1). Negligible real-world trigger (single-monitor is the common case; cross-monitor drag mid-recording is rare). A proper fix would re-query the monitor only when `pillX` leaves the cached `[screenLeft, screenRight]` band (cheap, bounded IPC) — a geometry refinement needing its own smoke, out of 6.4's scope. Low. Source: Blind Hunter + Edge Case Hunter, code review of story-6.4.

## Deferred from: code review of B1 launch license gates (quick-dev, 3-layer Opus) — 2026-06-08

Core change PASS (acceptance audit: all 4 ACs + boundaries satisfied). One patch applied during review (tightened the 14-day boundary test to pin ~14d within 1h, so a 13-day off-by-one is now caught, not just a 60-day regression). The following pre-existing / out-of-scope findings were surfaced by the review and deferred:

- **`ls_client::validate()` test-mode gate is dormant (no production caller).** `validate()` has zero production callers — the LS re-validation path is not wired; the cache path (`compute_status_from_cache_ls`) recomputes status purely from `ls_last_validated_at` and never re-checks `test_mode`. The armed gate in `validate()` therefore has no runtime effect today; the **`activate()` gate IS live** (called via `validate_key_dual_path`, `mod.rs:429`) and rejects test-mode keys at first activation. Pre-existing architectural gap (periodic re-validation never implemented). Already noted in the B1 spec Design Notes. HIGH per Edge Case Hunter but real blast-radius is bounded by the cache TTL (~30+2 days) and the activate-time gate. Source: Edge Case Hunter F1, corroborated by clippy `function validate is never used`.

- **`test_mode: bool` has `#[serde(default)]` → security gate fails OPEN if LemonSqueezy omits the field.** `ls_client.rs:71`. If LS ever omits/renames `test_mode` or sends `null`, it deserializes to `false` → not rejected → a test-mode key would be accepted in release. LS sends `test_mode` on every real response today, so the risk is contract-drift, not current behavior. Hardening (fail closed, or assert presence) needs verification of LS's actual JSON contract — blocked on having LS keys, which Andy currently lacks. MEDIUM. Source: Blind Hunter.

- **Existing Early-Access installs (first_install_at day 15–60) are instantly Unlicensed on update to the 14-day build.** `compute_trial_status` recomputes `trial_end = first_install_at + 14d` from the stored install date, so an in-flight 60-day trial collapses retroactively. No grandfather/migration path. Real-world blast radius ≈ 0: Early Access was withdrawn 2026-04-14 with no active testers. Surfaced to Andy as a billing decision at hand-off (he confirmed lockout is acceptable — see B1 hand-off). MEDIUM→accepted. Source: Edge Case Hunter F2.

- **`compute_trial_status` robustness nits (pre-existing).** `trial_end = first_install_at + TRIAL_DURATION_SECS` uses plain `+` not `saturating_add` (lone inconsistency vs surrounding `saturating_*`); no clock-skew/monotonic sanity bound on `first_install_at`; `first_install_at == 0` after a `SystemTime` failure means permanent no-trial with a silent re-stamp each launch. B1 spec `Never` boundary forbids touching `compute_trial_status` logic beyond the constant, so deferred rather than patched. LOW–MEDIUM. Source: Edge Case Hunter F4.

- **A test-mode key activated in a DEBUG build survives into a release build via the cache.** Because the cache path never re-checks `test_mode` (see F1), a dev's debug-time test-mode activation is cached as Licensed and a later release build trusts it for the cache TTL. Reinforces the F1 / cache-blindness gap. LOW. Source: Edge Case Hunter F5.

- **Production `!cfg!(debug_assertions)` wiring is not unit-tested (only the pure helper + explicitly-threaded mirrors are).** `cargo test` always runs debug, so a sign typo (`cfg!` without `!`) would invert the gate in shipped builds with all tests still green. This is the consciously-downgraded part of B1 — mitigated by the helper truth-table; must be verified once in a real release-build smoke when LS keys are available. LOW. Source: Blind Hunter + Edge Case Hunter F6.

- **Android client-side trial is reset by uninstall+reinstall (inherent to local trials).** `spec-android-license-enforcement` anchors the Android-only trial on `PackageManager.firstInstallTime`, which survives "Clear data" but resets on a full uninstall+reinstall. A determined user can therefore renew the 14-day trial by reinstalling. Inherent to any client-side trial without server-side enforcement (desktop has the analogous config.json-reset path). Closing it properly needs server-side install/trial tracking, out of scope for the launch-blocker fix. LOW–MEDIUM. Source: Android license enforcement review (Blind Hunter F1, residual after the `firstInstallTime` patch).

- **Android has no on-device license-entry / activation UI.** Licensing is desktop-activated only; an Android-only user cannot enter/activate a key on-device, so alternative providers stay gated after trial until activation happens on desktop and `config.json` is synced. Documented as a known limitation in `spec-android-license-enforcement` (Design Notes). Belongs to a separate onboarding/sync story (roadmap bucket B), not the enforcement fix. MEDIUM. Source: Android license enforcement planning.

## Deferred from: code review of 9-10-token-codegen-klarvo-css-to-klarvotheme (2026-06-16)

- **`gen-android-theme.mjs` parser throws on modern/alternate CSS color syntax** — `rgb()` (no alpha), space-separated `rgb(r g b / a)`, percentage channels, 3-digit `#RGB` and 8-digit `#RRGGBBAA` hex all hard-throw (`hexToArgb` length-6 check; `rgbaToArgb` comma-int regex). Fail-loud, and the current canon uses only `#RRGGBB` + `rgba(int,int,int,float)`, so not triggered today. If a future canon edit switches a token to modern syntax it bricks the drift gate (hence the build) with a cryptic message. LOW. Source: Blind + Edge Hunter (9-10 review).
- **rgba alpha >1 overflow unguarded** — `round(a*255)` with `a>1` yields a 3-hex-digit AA → malformed 9-digit literal → kotlinc error. All canon alphas ≤1 today. LOW. Source: Edge Hunter (9-10 review).
- **Drift gate gives an opaque failure if `node` is absent on the build host** — `node scripts/gen-android-theme.mjs --check` under `set -e` prints bare "node: command not found" before the sync. Node is available in the WSL build env (render-surface.mjs). A `command -v node` guard would clarify. LOW. Source: Edge Hunter (9-10 review).
- **`android-smoke.sh` mis-reports the JVM test count** — the harness reads one result XML (`-print -quit`) and reports e.g. "24 Tests" while the real suite is 60; the real count is confirmed out-of-band. Pre-existing harness quirk, unrelated to the 9-10 drift gate. LOW. Source: Blind Hunter (9-10 review).

## From WebView2 overlay-backgrounding hotfix, review iteration 1 — 2026-06-20

Source: review of `spec-webview2-overlay-backgrounding.md` (edge-case-hunter). Out of the config-only scope (AC3); correctness is fine today.

- **Document the implicit env-args coupling in-code.** The fix sets `additionalBrowserArgs` only on the `main` window; all webviews share one WebView2 environment locked by the first-created window, so this transitively governs the `bar`/`preview` overlays. The coupling is implicit in code — a future refactor that adds per-window `.additional_browser_args()` to the bar/preview builders, reorders creation so an overlay precedes `main`, or hits a path where `main`'s webview env fails to create first, would SILENTLY drop the fix (no error, overlays just start vanishing again). Hardening: add an explanatory comment at the bar/preview builders in `src-tauri/src/lib.rs` (~633, ~773) and/or a one-line boot log asserting `main` established the env first. Deferred because it would expand the hotfix into Rust; the coupling is already captured in the spec Design Notes and the `b7acdb3` commit message.

## Deferred from: code review of 7-1-android-chunking-parity-core-output (2026-08-10)

- **In-flight chunk requests are neither cancelled nor awaited after an abort.** `futures.map { it.get() }` is sequential, so when future *k* throws, futures *k+1…n* are never `get()`-ed or `cancel()`-ed, and the `finally` runs `executor.shutdown()` (not `shutdownNow()`), so already-queued chunk calls run to completion after the method has thrown. Deferred because Rust's `join_all` likewise drives every future to completion — token spend is at parity, so this is not a drift defect; only the inline comment ("must not … waste tokens") overstates what the change achieves. LOW. Source: Blind Hunter + Edge Case Hunter (7-1 review).
- **A single trivial chunk can still reach the LLM.** The trivial-chunk forward-fold in `splitIntoChunks` is guarded on `merged.size >= 2`, so a result list containing exactly one trivial entry (e.g. a punctuation-only transcription artefact) is returned unfolded and sent to the LLM. Pre-existing — not introduced by the 7-1 diff, and present identically before the byte-index rewrite. Belongs with 7-7 (golden-vector parity net), which is the story that locks this invariant across both platforms. LOW. Source: Blind Hunter (7-1 review).

## Deferred from: code review of 11-6-line-spacing-appearance-setting (2026-08-10)

- **No whitelist or validation of the tier string at any layer.** `merge_settings` accepts any string (`src-tauri/src/commands/settings.rs:375-376`); all three renderers then silently fall back to medium (`native_preview.rs:118`, `ListeningPanelView.kt:276`, `AppearanceContent.tsx:155`). An out-of-domain value written into `config.json` persists forever, leaves `KSegmented` with no segment selected, and keeps `isDirty` false so the user cannot correct it by re-saving. Pre-existing: `preview_font_size` has the identical gap. LOW. Source: Edge Case Hunter + Blind Hunter (11-6 review).
- **`PreviewConfig` derives `Default` and a poisoned config mutex takes `.unwrap_or_default()`** (`src-tauri/src/pipeline.rs:757`) → `line_height_mult = 0.0` → `line_h` floors to 1 px and all lines overprint. Pre-existing and already degenerate on that path: the same default also yields `font_px = 0`, `w_base = 0`, `live_preview_enabled = false`. The new field adds no new failure mode; the fix is to give `PreviewConfig` a real `Default` (or fall back to `from_app_config(&AppConfig::default())`). LOW. Source: Edge Case Hunter (11-6 review).
- **`Object.prototype` key lookup on a `Record<string, number>` map returns a function, which `??` does not catch** (`src/components/settings/AppearanceContent.tsx:155`). A tier string of `"constructor"`/`"toString"` yields an invalid CSS `lineHeight`. Pre-existing: identical exposure one line above at `:154` for `FONT_PX_MAP`. LOW. Source: Edge Case Hunter (11-6 review).
- **The tier→multiplier mapping has zero test coverage** (`src-tauri/src/native_preview.rs:115-119`) — nothing asserts `"small" → 1.35 / "medium" → 1.625 / "large" → 1.925` (Desktop), and `native_preview.rs` has no `#[cfg(test)]` module at all. Pre-existing: the analogous `font_px` mapping is untested for the same reason. The one new test only guards the serde default. LOW. Source: Blind Hunter (11-6 review).
- **The `merge_settings` test fixture pins the new field to `None`** (`src-tauri/src/commands/settings.rs:1491`), so the `Some(...)` branch of the new merge arm is never executed by any test. Pre-existing: `preview_font_size: None` sits in the same fixture. LOW. Source: Blind Hunter (11-6 review).
- **Positional argument chains carry the new field.** Three consecutive same-typed `String` params in the Kotlin `Config(...)` call (`android/kotlin-src/com/klarvo/voice/KlarvoApi.kt:443`) and a 20-plus all-optional positional chain across `SettingsPanel.tsx:570` → `useSettings.ts:117` → `tauri-commands.ts:314`. Order verified correct today; because every TS param is optional, a future arity mistake produces no type error, only a silent "no change". Named arguments / an options object would remove the class. LOW. Source: Blind Hunter (11-6 review).

> Numbers and citations above corrected 2026-08-10 (finding R-P4) against the final committed state — Desktop multipliers are `"small" → 1.35 / "medium" → 1.625 / "large" → 1.925` (raised from 1.325 at the R-D1 GDI-clipping fix; Android stays `1.45 / 1.7 / 1.95`, unaffected). Previously this block named the superseded `1.475 / 1.625 / 1.775` pre-`4f9aba0` multipliers and pre-fix line numbers.

## Deferred from: re-review of 11-6-line-spacing-appearance-setting (2026-08-10)

- **The ±0.30-em cross-platform normalization holds for the reachable default typefaces but drifts on the Georgia preset.** `typefaceForFontFamily` (`android/kotlin-src/com/klarvo/voice/ListeningPanelView.kt:86`) returns `Typeface.DEFAULT` for the default `'Inter', system-ui, …` stack and `MONOSPACE` only for the three opt-in mono stacks; both resolve to fonts with hhea 1900/−500 @ upem 2048 → natural line height N = 1.172, so the Android delta is 0.293 em against the 0.30 em the comment at `:53-58` states — 2.3 % under, well inside its "~1.2×" hedge. The `SERIF` branch (Georgia preset → Noto Serif, N ≈ 1.36) reaches ≈0.34 em, ~14 % over. No `R.font.*` is wired anywhere in `android/kotlin-src`, so platform fonts always apply. Cosmetic and preset-specific; the fix is to state the equivalence as holding for the sans/mono presets only. LOW. Source: Blind Hunter + Edge Case Hunter (11-6 re-review).

## Deferred from: code review of 8-5-main-window-history-re-skin (2026-08-18)

- **`handleHistorySearch` has no error handling** (`src/App.tsx:292-303`). The `async` handler awaits `searchHistory` / `getHistory` outside any `try`/`catch`. A rejected IPC call leaves `historySearch`/`historyAppSearch` advanced while `historyEntries` stays stale, so the list and the search box disagree permanently and the rejection surfaces only as an unhandled promise. Pre-existing; untouched by this story. LOW. Source: Edge Case Hunter (8-5 review).
- **`handleHistorySearch` has no request-sequence guard** (`src/App.tsx:292-303`). Rapid typing can let an older `searchHistory` response resolve after a newer one and overwrite the current results. Pre-existing. LOW. Source: Edge Case Hunter (8-5 review).
- **Reopening the History panel does not reset the search fields** (`src/App.tsx:208`). `onOpenHistory` reloads via `getHistory(50)` but leaves `historySearch`/`historyAppSearch` populated, so a stale query sits in the box above an unfiltered list — and, on an empty DB, forces the "No results" branch instead of the first-run empty-state. Pre-existing state handling, newly more visible through the designed empty-state. LOW. Source: Edge Case Hunter + Blind Hunter (8-5 review).
- **`new Date(entry.createdAt + "Z")` renders the literal string "Invalid Date"** for a malformed or already-zone-suffixed timestamp (`src/App.tsx:636`, `:708`). Pre-existing expression; this story only wrapped it in a `font-geist-mono` span, which makes the failure mode more prominent rather than introducing it. LOW. Source: Edge Case Hunter (8-5 review).
- **Deleting all loaded entries while the DB holds more than 50 shows the first-run empty-state.** `handleDeleteHistoryEntry` (`src/App.tsx:308-311`) filters locally and never refetches, so once the loaded page empties, the user sees "No dictations yet" although history still exists. Pre-existing; amplified by the new full-size empty-state. LOW. Source: Edge Case Hunter (8-5 review).
- **Teal/amber glow shadows remain raw rgba literals** (`src/App.tsx:70-73`): `rgba(233,162,76,0.3)`, `rgba(233,162,76,0.2)`, `rgba(41,199,172,0.2)`, `rgba(41,199,172,0.3)`. The values are correct (`--color-klarvo-amber #E9A24C`, `--color-klarvo-teal #29C7AC`) and the story knowingly carved them out ("shadow rgba values already correct"), so AC #6 holds as written. The residual risk is that gate 9.3 is hex-shaped and structurally cannot detect a future *wrong* rgba literal. 8-7 fidelity material. LOW. Source: Blind Hunter + Acceptance Auditor (8-5 review).
- **`border-[rgba(233,162,76,.32)]` is an un-tokenized literal** (`src/App.tsx:638`, `:712`). Verified: no `--color-klarvo-amber-line` exists in the `@theme` block, so Task 1.3's own fallback clause mandated the arbitrary value. It matches canon `--k-amber-line` exactly. Natural closure point is the 8.6 alias-closure / 8.7 fidelity gate, where the token can be added. LOW. Source: Blind Hunter + Acceptance Auditor (8-5 review).
- **Focus and surface affordances now diverge across sibling controls.** The two history search inputs moved to `bg-klarvo-surface-2` plus `focus:ring-1 focus:ring-klarvo-teal/20` (`src/App.tsx:593`, `:600`), while the result textarea kept `bg-klarvo-bg` and a bare `focus:border-klarvo-teal/30` with no ring (`src/App.tsx:858`) and the history cards kept `bg-klarvo-bg`. Spec-scoped — Task 3 covered only the search inputs — but the app now carries two surface tokens and two focus affordances for the same roles. 8-7 fidelity. LOW. Source: Blind Hunter (8-5 review).
- **Destructive-button hover dims instead of brightening** (`src/App.tsx:643` "Verwerfen", `:719` "Delete"). Previously `orange-400 → orange-300` brightened on hover; now `klarvo-danger → klarvo-danger/80` fades, which reads as the control becoming disabled under the pointer. AC #9 mandated this because `klarvo-danger-hi` does not exist in `@theme` (independently confirmed) and `/80` is the file's existing hover convention. Closing it needs a new token, which the story forbids. LOW. Source: Blind Hunter (8-5 review).
- **`max-h-[calc(100vh-250px)]` hard clamp vs. the roughly 5x taller empty-state** (`src/App.tsx:604`). The old empty-state was a single `py-4` paragraph; the new one is `py-10` plus a 40 px disc, a `gap-3` and two stacked paragraphs. On a short window the empty-state can exceed the clamp and require scrolling. The clamp itself is pre-existing. Verify at the device gate. LOW. Source: Blind Hunter + Edge Case Hunter (8-5 review).
- **Mono timestamps widen the `justify-between` meta row** (at commit `116e427`, `src/App.tsx:705-714`). Monospace advance widths exceed the previous proportional ones; combined with a long locale timestamp and a long `appName` the row can reflow or push the Copy/Delete buttons. The change is AC #1- and canon-mandated (`.note .ts { font-family: var(--k-mono) }`); the geometry consequence was not measured. Verify at the device gate. LOW. Source: Blind Hunter + Edge Case Hunter (8-5 review).
- **App-tag pill markup is duplicated verbatim** in the pending and normal branches (`src/App.tsx:638`, `:712`). This review's diff had to edit both in lockstep, which is the evidence that it is a live maintenance hazard rather than a hypothetical one. The same applies to the `text-[11px] text-klarvo-teal hover:text-klarvo-teal/80 transition-colors` button styling, now repeated four times. Pre-existing duplication. LOW. Source: Blind Hunter (8-5 review).
- **`hover:bg-klarvo-elevated/40` on history cards can stick after a tap on touch devices** (at commit `116e427`, `src/App.tsx:665`, the normal-card wrapper). The History panel is reachable on Android (the header toggle is not `isDesktop`-gated). LOW. Source: Blind Hunter (8-5 review).
- **`HighlightedText` highlights only the text query, not the app query** (at commit `116e427`, `src/App.tsx:667`), while the new empty-state treats both fields interchangeably as "your search". An app-name-only filter therefore yields rows with nothing highlighted. Pre-existing. LOW. Source: Blind Hunter (8-5 review).
- **`src/App.tsx` is tracked with file mode 100755.** A `.tsx` source file marked executable. Pre-existing; this story touched the file without correcting it. LOW. Source: Blind Hunter (8-5 review).
- **Canon deltas outside this story's AC set** (8-7 fidelity material, recorded so the fidelity pass has the list): canon `.note` is a `border-bottom`-separated row at `padding: 16px 18px` while the implementation keeps discrete bordered `rounded-xl` cards in a `gap-2.5` stack; `.note .body` 13.5px vs. `text-xs` (12px); `.note .ts` 11.5px vs. `text-[11px]`; `.note .profile` `font-size: 11px; padding: 3px 8px` vs. `text-[9px]` / `px-1.5 py-0.5`; profile fill canon `--k-amber-bg` (12%) vs. the AC-mandated `/10`. No AC demands any of these, so none is a violation. LOW. Source: Acceptance Auditor (8-5 review).
- **DoD device gate still open.** The Windows release build (`scripts/sync-and-build.ps1`) plus manual smoke has not run, and the `docs/surface-smoke-checklist.md` walk is currently satisfied only by the story's a-priori trap-applicability table (which holds — no config keys, fields, events or geometry are touched). The story sits at `review`, which is the correct posture. Source: Acceptance Auditor (8-5 review).
- **The dev worker installed `gcc-mingw-w64-x86-64` via `apt-get` on the host** to reach the Task 9.2 cross-compile gate, after which the check landed on the documented pre-existing `ort-sys` baseline. Disclosed in the Debug Log and no repo file was affected, but an unattended worker mutating the machine is worth surfacing. Source: Acceptance Auditor (8-5 review).

## Deferred from: final verification pass of 8-5-main-window-history-re-skin (2026-08-18)

- **A synchronous `setState` now runs inside another hook's `setState` updater** (`src/hooks/usePanels.ts:47-56`, `src/App.tsx:210`). `toggle` invokes `callbacks.onOpenHistory?.()` from inside the `setState((prev) => …)` updater, which React requires to be pure. The first fix round already fired the `getHistory(50)` IPC call from there; the second added `setHistoryLoaded(false)`. Under React 19 with `React.StrictMode` active (`src/main.tsx:27`), updaters are double-invoked in development, so the reset and the request can fire twice per open in dev builds. The write is idempotent and production builds are unaffected by the double-invoke, so no visible defect is expected — but this was **not observed at runtime**; the DoD device gate is still open. Pre-existing pattern, deepened by this story. If History-open behaves oddly at the device gate, move the reset out of the updater into an effect keyed on `showHistory`. LOW. Source: Edge Case Hunter + Acceptance Auditor (8-5 final verification pass).

## Deferred from: code review of 8-8-action-feedback-copy-and-delete (2026-08-21)

- **`PreviewComments`' copy timeout is cleared neither on unmount nor on a re-click** (`src/components/PreviewComments.tsx:434-450`). Both `setTimeout(() => setCopied(false), COPY_FEEDBACK_MS)` calls are bare — no ref, no cleanup effect. A stale timer flips `Copied` off early after a re-click, or updates an unmounted component. Pre-existing: this story changed only the constant (`2000` → `COPY_FEEDBACK_MS`, Task 3.1) and was explicitly told not to rewrite that path into the hook. LOW. Source: Edge Case Hunter + Blind Hunter (8-8 review).
- **The new hook carries no `document.execCommand` fallback, unlike `PreviewComments`** (`src/hooks/useCopyFeedback.ts:41-46`). `PreviewComments` falls back to a temporary textarea when `navigator.clipboard.writeText` rejects; `useCopyFeedback` gives up and renders `Copy failed`. Not a regression — the five `App.tsx` sites never had a fallback either, and the story asked only for a visible failure state (AC3). Adding one is new scope. LOW. Source: Blind Hunter (8-8 review).
- **`flushPendingDeletes` is referenced before its declaration and is missing from two `useCallback` dependency arrays** (`src/App.tsx:217` and `:319` reference the `const` declared at `:374`; both callbacks carry `[]`). Verified NOT a live defect: `tsc` exits 0, no ESLint config exists in the repo, both callback bodies only run after render, and `commitPendingDelete` / `flushPendingDeletes` are referentially stable. It becomes a real TDZ or stale-closure bug the moment `loadHistory` is called during render or `commitPendingDelete` gains a dependency. LOW. Source: Blind Hunter + Acceptance Auditor (8-8 review).
- **"Verwerfen" is equally destructive and still answers no click** (`src/App.tsx:749`). `handleDiscardPendingEntry` throws away audio that was never transcribed, fires immediately, and has no confirmation, no label change and no undo — while `Delete` on a completed entry just gained a 6000 ms undo window. The story scoped itself to Copy and Delete, so this is outside its AC set. LOW. Source: Blind Hunter (8-8 review).
- **Neither the label swap nor the undo strip is announced, and focus is dropped on Delete and on Undo** (`src/App.tsx:762-773`, `:841`). No `role="status"` / `aria-live` anywhere, so a screen-reader user never learns that Copy succeeded or that a row entered its undo window. Clicking Delete unmounts the focused button, so focus falls to `<body>` and a keyboard user cannot reach `Undo` inside the 6000 ms window without re-tabbing. No AC covers accessibility. LOW. Source: Blind Hunter (8-8 review).
- **`entry.rawText!` / `recording.rawText!` can put the string "undefined" on the clipboard and report "Copied"** (`src/App.tsx:805`, `:818`, `:997`, `:1013`). `copy(id, text)` does not validate its `text` argument, so a non-null assertion that turns out false now produces a cheerful success state instead of a silent no-op. The assertions are pre-existing; only the success claim is new. LOW. Source: Blind Hunter (8-8 review).
- **AC5's "exactly once" is proven by a DOM Delete-button count, not by a call count** (`gate4-evidence/8-8/measurements.json`, id `AC5/Task4.6`, `"detail": "2"`). The story's Verification section required "issues exactly one backend call". In preview mode `deleteHistoryEntry` (`src/tauri-commands.ts:486-489`) is a no-op, so a call count was only observable through a spy, and none was placed — the SUT is judging itself. The code does support the claim by construction: `commitPendingDelete` (`src/App.tsx:334-346`) reads the ref, returns early when the id is absent, and deletes from the ref synchronously before the backend call. Gate quality, not a code defect. LOW. Source: Acceptance Auditor (8-8 review).

## Deferred from: code review of 8-8-action-feedback-copy-and-delete — round 2, fix-pass verification (2026-08-21)

- **A `delete_history_entry` IPC that never settles now strands the history panel at `Loading…`** (`src/App.tsx:216-219`). `setHistoryLoadState("loading")` precedes a chain whose first link is the backend DELETE: `flushPendingDeletes().then(() => getHistory(50))`. The chain cannot reject, because `commitPendingDelete` swallows rejections with `.catch(console.error)` — but it also cannot settle if the Tauri IPC never returns, and then `getHistory(50)` is never issued and no error branch is reachable. Before commit `197ce13` a hung delete could not block the list from rendering. NOT pre-existing: this is a consequence of the flush-before-refetch sequencing that round-1 finding 2 asked for, so removing it would undo the requested fix. If it ever bites, give the flush a timeout race rather than dropping the sequencing. LOW. Source: Blind Hunter + Edge Case Hunter (8-8 review round 2).
- **The canon has no confirmed-state exception to the hover-only acts row, but the code now carries one at three sites** (`docs/design/overhaul/source/assets/klarvo.css:441-460` defines `.acts { opacity: 0 }` / `.note:hover .acts { opacity: 1 }`; the code adds `status !== "idle" ? "opacity-100" : …` at `src/App.tsx:826`, `:846` and `:1024`). AC1 requires the confirmation to be readable for its full 1500 ms and therefore outranks the canon, so the code is right — but the exception is recorded neither in the canon nor in the story, so a future canon read will contradict the code and may "fix" it back. NOT pre-existing: two sites came in with `90d035e`, the third with `197ce13`. Fix shape: an ADR-0019-style canon amendment, or one sentence in the story's Dev Notes. LOW. Source: Acceptance Auditor (8-8 review round 2).

## Deferred from: code review of 7-2-android-live-auto-stop-vad-gate-parity (2026-09-09)

- **Silero is invoked on sub-gate frames, diverging from Rust's stateful model trajectory** (`android/kotlin-src/com/klarvo/voice/KlarvoAudioRecorder.kt:463`). Kotlin computes `val vadSpeech = vad?.isSpeech(filteredFrame) == true` unconditionally and only then ANDs it with the energy gate, while Rust skips inference entirely when the gate is closed (`src-tauri/src/vad/mod.rs`, `let prob = if energy_ok { self.engine.predict(...) } else { 0.0 }`). Silero is stateful (LSTM `h`/`c` plus the library's `isContinuousSpeech` counters), so feeding it frames Rust never sees makes the two platforms' model state trajectories drift even though the per-frame boolean is identical. The comment at `:457` ("Energy gate: avoid calling the ONNX model for clearly silent frames") describes behaviour the code does not have, and the CPU saving it claims is not being realised. Pre-existing: the ordering predates Story 7.2 and the diff did not touch it — but it now sits inside the exact code path this story claims to bring to parity, so it is the natural next row after H1/M3. MEDIUM. Source: own verification during the 7-2 review (Rust `process_frame` compared against `processVadFrame`).
- **`framesForSeconds` is a generically-named converter that now silently refuses to return fewer than 7 frames** (`android/kotlin-src/com/klarvo/voice/KlarvoAudioRecorder.kt:149-150`). The name promises a pure seconds→frames conversion; after AC2 it hard-floors every result at the 200 ms-equivalent `MIN_SILENT_FRAMES`. Correct for both current call sites (`requiredSilentFrames`, `previewRequiredSilentFrames`) and explicitly what GATE-1 decided, so this is not a defect today — but any future caller wanting a genuine short window (onset windows, chunk timers, diagnostics) gets a wrong answer with no indication. Fix shape when it matters: rename to `silentFramesForSeconds`, or keep the converter pure and apply `.coerceAtLeast(MIN_SILENT_FRAMES)` at the two silence call sites. LOW. Source: Blind Hunter (7-2 review).

## Deferred from: code review of 7-2-android-live-auto-stop-vad-gate-parity — round 2, scoped re-review of fix round 1 (2026-09-09)

- **`vadGateFilteredFrame` has no guard relating `length`, `frame.size` and `out.size`** (`android/kotlin-src/com/klarvo/voice/KlarvoAudioRecorder.kt:208-219`). The reused scratch buffer introduced in fix round 1 decoupled output length from frame length; previously the per-frame `FloatArray(frame.size)` allocation enforced `filteredFrame.size == frame.size` by construction. Not reachable today — `processVadFrame`'s sole caller is `feedVad:453`, which always passes `vadRingBuffer` (size exactly `VAD_FRAME_SIZE` == `filteredFrameScratch.size`), and all 512 slots are overwritten per call — but the seam is now public API and its own KDoc invites test callers to "pass a fresh array". Guard: `require(length in 0..minOf(frame.size, out.size))`. LOW. Source: Blind Hunter + Edge Case Hunter (7-2 review round 2).
- **The script that runs the Kotlin unit tests does not own the gradle patch they now depend on** (`scripts/android-smoke.sh:187`). `android-smoke.sh` runs `:app:testUniversalDebugUnitTest` but applies no gradle patches; the `testImplementation("org.json:json:20231013")` line the new AC5 JSON-parse tests require is added only by `scripts/android-build.sh:206-213`. Cannot produce a false green — the generated `app/build.gradle.kts` sets no `testOptions.unitTests.returnDefaultValues`, so a missing artifact yields a loud "not mocked" `RuntimeException`, and `android-smoke.sh` hard-fails at `:55`/`:60` on a wiped `gen/android/`. Related pre-existing gap: `scripts/android-emulator-smoke.sh:99` runs only `assembleUniversalDebug` and no unit tests at all, so the new AC5 tests sit outside the gate an unattended agent executes. LOW. Source: Acceptance Auditor (7-2 review round 2).
- **`parseMinRecordingMs` accepts negative and absurd values unguarded** (`android/kotlin-src/com/klarvo/voice/KlarvoApi.kt:305-306`). Rust's `min_recording_ms: u32` cannot be negative; Kotlin's `optLong("minRecordingMs", 500L)` will happily return `-1`, disabling the pre-STT minimum-length guard. Pre-existing class — `silenceThreshold` has the identical gap on the same parse path. LOW. Source: Edge Case Hunter (7-2 review round 2).
- **`cutoff_isAt85Hz_minus3dbCorner` pins the corner frequency but not the filter order** (`android/kotlin-test/com/klarvo/voice/HighpassFilterTest.kt:79-96`). A 1st-order RC highpass at 85 Hz also reads `1/sqrt(2)` at 85 Hz, so no test distinguishes the 2nd-order Butterworth biquad from a one-pole. This matters because the AC7 golden vectors' unity-gain-at-Nyquist argument depends on the biquad topology. Round-1 finding 3 asked for a corner assertion driven through the production constant and got exactly that, and it genuinely discriminates a drifted cutoff (simulated peaks at 85 Hz for cutoffs 60/70/85/95/300 Hz are 0.895/0.828/0.707/0.625/0.080 — only 85 Hz passes the +/-0.02 band). Strengthen later with a rolloff-slope probe (gain at fc/4 is ~0.0625 for 2nd order vs ~0.243 for 1st). LOW. Source: Blind Hunter (7-2 review round 2).
- **New test-local duplicates of production constants, in the round that exists to remove them** (`android/kotlin-test/com/klarvo/voice/VadGateGoldenVectorsTest.kt:151-152`, `HighpassFilterTest.kt:141`). `val frameSize = 512` and `HighpassFilter(..., 16000f)` duplicate `KlarvoAudioRecorder.VAD_FRAME_SIZE` and `SAMPLE_RATE`; if the frame size or sample rate ever changes, these tests keep passing against the old geometry. Same class as round-1 findings 2/3, which were fixed for `HIGHPASS_CUTOFF_HZ` and `VAD_RMS_NORMALIZATION_DIVISOR`. No current divergence. LOW. Source: Blind Hunter (7-2 review round 2).

## Deferred from: code review of 7-2-android-live-auto-stop-vad-gate-parity — round 3, scoped re-review of fix round 2 (2026-09-09)

- **The new Rust `"silence"` arm drops the sample-format branch its parent arm had** (`src-tauri/src/pipeline.rs:4175-4180`). The additive arm always emits 16-bit int via `make_wav`, whereas the `"synthetic"` arm it was split out of honors `bits_per_sample == 32` / `sample_format == "float"` and routes to `make_float_wav` (that is how RMS-007 works). Byte-identical for the only two vectors that use it today (RMS-003, RMS-006 — both `bits_per_sample: 16`, no `sample_format`), but a future float32 silence vector would be silently built as PCM16 and the test would assert the wrong decode path. Test-only code, no current divergence. LOW. Source: Edge Case Hunter + Acceptance Auditor (7-2 review round 3).
- **The seam refactor reintroduces a small per-frame allocation on the audio thread** (`android/kotlin-src/com/klarvo/voice/KlarvoAudioRecorder.kt:230,529`). `vadGateDecision` returns a `VadGateResult` data class and `processVadFrame` passes a capturing lambda, so roughly two short-lived objects are allocated ~31x/s — immediately after round-1 finding 9 removed the 2 KB/frame `FloatArray`. Net still a ~40x reduction (~1.6 KB/s vs. 64 KB/s), so this is not a regression against the previous state, but it is avoidable: marking `vadGateDecision` `inline` would eliminate both allocations. LOW. Source: Blind Hunter + Acceptance Auditor (7-2 review round 3).
- **The missing length guard flagged as round-2 R2-D1 now also applies to the new seam** (`android/kotlin-src/com/klarvo/voice/KlarvoAudioRecorder.kt:244-257`). `vadGateDecision` relates `length`, `frame.size` and `out.size` no more strictly than `vadGateFilteredFrame` does, and additionally hands `isSpeech` the *full* `out` scratch array while the energy gate measures only `length` — so a test caller with `length < out.size` would have Silero see stale tail samples the RMS never measured. Not reachable from the sole production caller (all three sizes are `VAD_FRAME_SIZE`). Guard: `require(length in 0..minOf(frame.size, out.size))` on the seam. LOW. Source: Edge Case Hunter (7-2 review round 3).
- **Neither copy of the gradle `org.json` patch verifies its `sed` actually landed** (`scripts/android-smoke.sh:190-193`, `scripts/android-build.sh:212-215`). If the `testImplementation("junit:junit` anchor line is ever absent from the generated `app/build.gradle.kts`, `sed` no-ops with exit 0, the grep guard never flips true, every subsequent run re-tries, and the failure surfaces much later as an opaque "not mocked" `RuntimeException`. There is also no `[ -f "$APP_GRADLE" ]` guard before `sed -i`. Cannot produce a false green, and the anchor is Tauri-template-provided; this is the pre-existing shape `android-build.sh` has carried since fix round 1, now duplicated. Fix shape: re-grep after the `sed` and hard-fail with a clear message. LOW. Source: Blind Hunter + Edge Case Hunter (7-2 review round 3).
- **`productionFilteredRms` silently drops a partial trailing frame** (`android/kotlin-test/com/klarvo/voice/VadGateGoldenVectorsTest.kt:177-185`). The new 1.0 s bass tone is 16000 samples — 31 full 512-sample frames plus a 128-sample remainder that the `pos + frameSize <= samples.size` loop discards. Bounds-guarded, so no crash, and the VAD-GATE-001 outcome is robust (simulated per-frame filtered RMS spans 0.00261-0.00332 across every skip count 0-4 and every aggregation, all far below the 0.005 threshold). Fix shape when it matters: give `bassToneShorts` a `frameCount` parameter like `nyquistSquareWaveShorts` instead of `seconds`. LOW. Source: Blind Hunter + Edge Case Hunter (7-2 review round 3).
- **`exercisedIds` is a `Set`, so duplicate fixture IDs collapse silently** (`android/kotlin-test/com/klarvo/voice/VadGateRmsFixtureTest.kt:139,141-147`). Two fixture entries sharing `id: "RMS-004"` still produce a 4-element set equal to `expectedExercisedIds`, so the new assertion would not notice a copy-paste duplication that drops real coverage. A `List` comparison would additionally pin multiplicity and order. Trivial next to the large net improvement over `assertTrue(exercised > 0)`. LOW. Source: Blind Hunter (7-2 review round 3).
- **Round-2 deferred item R2-D5 (test-local duplicates of production constants) grew in this round** (`android/kotlin-test/com/klarvo/voice/VadGateGoldenVectorsTest.kt:154`, `HighpassFilterTest.kt:239-240`). The new `bassToneShorts` helper adds another `val sampleRateHz = 16000f` and the new seam tests add fresh `512` literals, duplicating `KlarvoAudioRecorder.SAMPLE_RATE` and `VAD_FRAME_SIZE`. Same pre-existing class, nothing in the commit claimed otherwise. LOW. Source: Acceptance Auditor (7-2 review round 3).
- **`@androidx.annotation.VisibleForTesting` is fully qualified at all four sites** (`android/kotlin-src/com/klarvo/voice/KlarvoAudioRecorder.kt:179,192,210,243`) in a file that imports everything else. Style only. Note also that the annotation's default `otherwise = VisibleForTesting.PRIVATE` is arguably the wrong strength for members that production code inside the same class legitimately uses; an explicit `otherwise = NONE` would be more honest and avoids a future Android Lint `VisibleForTests` flag if another production file ever reads `HIGHPASS_CUTOFF_HZ`. LOW. Source: Blind Hunter (7-2 review round 3).

## Deferred from: code review of 7-8-parity-net-close-out-and-twin-hygiene (2026-09-10)

- **ADR-0017 boundary guard scans one directory level only** (`android/kotlin-test/com/klarvo/voice/Adr0017BoundaryGuardTest.kt:136`). `dir.listFiles { … endsWith(".kt") }` is non-recursive and `deletedTwinFilesHaveNotReappeared` (`:194`) checks only `dir.resolve("HallucinationFilter.kt")`, so a re-grown twin under `voice/stt/` would trip nothing. Not reachable today — `android-smoke.sh:161` copies only `"$SRC"/*.kt` flat, so a subpackage would not even be built — and the test's own coverage block states the limit. `dir.walkTopDown()` is a free hardening. LOW. Source: Blind Hunter (7-8 review).
- **Boundary-guard rules match per line, so a split declaration evades** (`android/kotlin-test/com/klarvo/voice/Adr0017BoundaryGuardTest.kt:170`). The rules run `pattern.containsMatchIn(rawLine)` over each stripped line, so a legal `object\n    HallucinationFilter {` is not matched. It is a re-growth tripwire, not a proof of purity, and the KDoc says so. Fix shape: scan the whole stripped text with an offset→line mapping. LOW. Source: Edge Case Hunter (7-8 review).
- **Duplicate fixture ids collapse silently on the Kotlin side** (`android/kotlin-test/com/klarvo/voice/TwinConstantsVectorsTest.kt:55-62`). `loadFixture()` builds a `LinkedHashMap` keyed by `id`, and `fixtureCarriesAllFiveTwinsAndDescribesEach` compares `expected.toSet()` to `byId.keys`, so two entries sharing an `id` still pass; only the Rust `assert_eq!(vectors.len(), 5)` catches it, and that half runs in a different gate. Same shape already deferred for `VadGateRmsFixtureTest` in 7-2 round 3. Fix shape: `check(byId.size == arr.length())`. LOW. Source: Blind Hunter + Edge Case Hunter (7-8 review).
- **`vector(id)` re-reads and re-parses the fixture on every lookup** (`android/kotlin-test/com/klarvo/voice/TwinConstantsVectorsTest.kt:66`). Six assertions → six file reads plus six JSON parses. No correctness impact on a small fixture. Fix shape: cache the parsed map in a `by lazy`. LOW. Source: Blind Hunter (7-8 review).
- **`android-build.sh` still carries the un-pruned `cp` that AC6a fixed in the smoke script** (`scripts/android-build.sh:70`). AC6a named `android-smoke.sh:157,176` only, and both were fixed with a clear-then-copy; the sibling sync path in `android-build.sh` retains the exact stale-file defect (a Kotlin file deleted from the tracked tree survives in `gen/android/` and keeps running). Outside this story's AC scope. Fix shape: the same `rm -f "$DST"/*.kt` before the copy. MEDIUM (same defect that failed the JVM gate on 2026-07-12). Source: Edge Case Hunter (7-8 review).
- **The `kotlin-test` prune sits inside the "has .kt files" guard** (`scripts/android-smoke.sh:183-186`). `rm -f "$TEST_DST"/*.kt` is inside `if [ -d "$TEST_SRC" ] && [ "$(ls -1 "$TEST_SRC"/*.kt … | wc -l)" -gt 0 ]`, so if `android/kotlin-test/` were ever emptied the whole block is skipped and stale tests keep running — the degenerate case of the very trap AC6a closes. Not reachable while any test exists. Fix shape: move the `rm -f` outside the `if`, or add an `else` that clears the destination. LOW. Source: Edge Case Hunter (7-8 review).
- **Two more copies of the repo-root fixture resolver** (`android/kotlin-test/com/klarvo/voice/TwinConstantsVectorsTest.kt:40`, `Adr0017BoundaryGuardTest.kt:59`). Both duplicate the four-candidate `cwd.resolve("../../../../$name")` ladder that `ChunkingVectorsTest.kt:108-124` already owns — the story deliberately avoided a third copy of the hand-rolled `JsonVal` parser on exactly this reasoning, then added two copies of the resolver. Pre-existing duplication class, no divergence today. Fix shape: one shared `FixtureFiles` helper in `android/kotlin-test/`. LOW. Source: Blind Hunter (7-8 review).

## Deferred from: code review of 7-8-parity-net-close-out-and-twin-hygiene — round 2, scoped re-review of fix round 1 (2026-09-10)

- **The Rust 399/400 boundary probe's path discriminator is a hard-coded `'\n'` and silently depends on `CHUNK_TARGET_SIZE < CHUNK_THRESHOLD`** (`src-tauri/src/llm/mod.rs:2186-2206`). The probe decides "single-call vs chunked path" via `text.contains('\n')`, which is only true when the chunked path yields ≥ 2 chunks — a relation between the two twin constants that nothing states. Raise `CHUNK_TARGET_SIZE` to ≥ `CHUNK_THRESHOLD` (or change the join separator, which `TWIN-CHUNK-JOIN-SEPARATOR-001` deliberately keeps under fixture control) and the probe fails with the message *"the comparison operator drifted from the Kotlin `bytes >= CHUNK_THRESHOLD`"* — an accusation against an operator that did not move. Cannot produce a false green. Fix shape: assert the chunk count, or read the separator from the fixture as the sibling assertion already does. LOW. Source: Blind Hunter + Acceptance Auditor (7-8 review round 2).
- **The leading-empty Rust case assumes a chunk layout the fixture explicitly does not pin** (`src-tauri/src/llm/mod.rs:2268-2280`). The input is `"a".repeat(target) + "b".repeat(50)` and the assertion is `result.text == "B".repeat(50)`, which silently assumes (i) `target + 50 >= CHUNK_THRESHOLD` so the chunked path is entered at all, and (ii) that the splitter puts all fifty `b`s in a chunk with no leading `a` — while `TWIN-CHUNK-TARGET-SIZE-001`'s own text says the fixture *"DOES NOT PIN: how the text is split once the threshold is crossed"*. If (i) breaks, the failure message lies about what broke. Fix shape: size the input from `threshold` explicitly and assert the chunk count before asserting the join. LOW. Source: Blind Hunter (7-8 review round 2).
- **Trailing-empty and interior-empty join results are still unpinned on both sides** (`src-tauri/src/llm/mod.rs:2262-2283`, `android/kotlin-test/com/klarvo/voice/TwinConstantsVectorsTest.kt:155-161`). Round-1 P1 asked for the leading-empty case and got it on both halves; `joinChunkResults(listOf("alpha", ""))`, the interior `listOf("a", "", "c")` and the empty list remain unasserted, so a trailing or doubled separator from an empty result would go unnoticed. Fix shape: three more `assertEquals` on each side. LOW. Source: Edge Case Hunter (7-8 review round 2).
- **The byte-length boundary probe never exercises multi-byte UTF-8** (`src-tauri/src/llm/mod.rs:2176-2210`). The probe feeds `"a".repeat(n)`, so swapping `raw_text.len()` for `chars().count()` — a real byte-vs-char confusion in a chunking path — stays green despite the fixture's byte-measurement claim. Fix shape: one additional case built from a multi-byte character. LOW. Source: Edge Case Hunter (7-8 review round 2).
- **`android-smoke.sh` reports one result XML's counts as the whole run** (`scripts/android-smoke.sh:217-220`). `find app/build/test-results -name "*.xml" -print -quit` plus `head -1` reads the `tests=` / `failures=` attributes of a single arbitrary suite, so the banner printed *"24 Tests, 0 Failures — alle grün"* for a run the record (correctly) counts as 168 tests across 22 suites. It cannot mask a failure in the read suite, but it silently ignores 21 others' counts and understates the gate. Pre-existing; outside AC6's two named traps. Fix shape: sum `tests=` and `failures=` across all result XMLs. MEDIUM. Source: Blind Hunter (7-8 review round 2), cause verified in round 2.
- **The smoke banner's "Dauer" prints `BUILD_SECS`, the APK-build time only** (`scripts/android-smoke.sh:328`, timer at `:240`/`:250`). A run that synced 46 files, ran the JVM suite, built a 273 MB APK and installed it on an emulator was reported as *"Dauer: 6s"* — exactly the APK-build figure printed two sections earlier. Pre-existing, cosmetic, but it ships inside committed gate evidence. Fix shape: anchor the timer at script start, or rename the field to "APK-Build". LOW. Source: Blind Hunter (7-8 review round 2).
- **D2's bounded scope is stated only in the story record, not at the `case` a maintainer reads** (`scripts/android-smoke.sh:282-286`). Decision D2 chose to keep `emulator-*` and bound AC6b to a locally attached AVD; the whole content of that decision — that a TCP-attached emulator (`<ip>:5555`) falls through to `INSTALL_ABI_FLAGS=()` and reinstalls the x86_64 split — lives in `7-8-…md` only. The next person to `adb connect` an AVD reads the script. Deferred because D2 explicitly resolved to leave the script unchanged. Fix shape: one comment line at the `case` naming the bounded scope and the fall-through consequence. LOW. Source: Blind Hunter (7-8 review round 2).
- **The P4 tightening is stricter than AC2's wording and has no override path** (`android/kotlin-test/com/klarvo/voice/Adr0017BoundaryGuardTest.kt:150-176`). With `honorsAllowlist` removed, the `audio/transcriptions` rule now also flags the literal **inside** `GroqSttBridge.kt`, whereas AC2 asks for it "outside the JNI bridge". The stricter default is correct under ADR-0017 (the Rust core owns the request) and buys real coverage — verified: none of the four shapes appears anywhere under `android/kotlin-src/com/klarvo/voice/` today. But the record presents it as compliance rather than as a deliberate tightening, and a future sanctioned bridge change would trip a guard whose KDoc still describes the bridge as allowed. Fix shape: one sentence recording the tightening, or a `@Adr0017Sanctioned` opt-out on the bridge file. LOW. Source: Edge Case Hunter + Blind Hunter (7-8 review round 2).
- **The M12 derived check skips style-less entries, so `platforms_agree` on such an entry is neither derived nor rejected** (`src-tauri/src/llm/mod.rs`, test fn `spec_m12_dictionary_scope_current_state_still_holds` — anchored by symbol, not by line). Both loops in that test `continue` when `v["style"]` is absent, so a `platforms_agree` field on a style-less entry is silently ignored: not derived from the two columns, not rejected as out of schema. Today the only style-less entry is `M12-DICT-SCOPE-README` and it carries no `platforms_agree`, so nothing is wrong in the fixture — the gap is that nothing stops one being added. Fix shape: assert that a style-less entry carries no `platforms_agree` field. LOW. Source: Edge Case Hunter (7-8 review round 2); **rewritten in 7-8 fix round 3** (round-3 patch 5) — the original entry argued from "the divergence assertion at `:2397` scans **all** vectors" and prescribed "scope the `any()` to styled entries", but `85aa0ee` had already deleted that `any()` under decision R2-D1 in the same round, and the cited range `:2381-2402` overran the file (2398 lines). The stated impact and fix shape were dead on arrival; the skip itself is real and survives.
- **The retained `assertFalse(bypassedRms < rawNormalizedRms * 0.5f)` is now implied by the 0.85–0.89 band** (`android/kotlin-test/com/klarvo/voice/HighpassFilterTest.kt:362-367`). Round-1 P8 offered "drop the redundant assertion **or** pin the ~87 % with a real band"; the band was added and the older assertion kept, so the fail-set of the `assertFalse` is now a strict subset of the band's and it can never be the sole failure. Inversion G confirms it (0.9957 passes the `assertFalse`, only the band catches it). The letter of P8 is satisfied; the redundancy shape merely survives inverted. Fix shape: keep it only for its targeted message and say so, otherwise drop it. LOW. Source: Blind Hunter (7-8 review round 2).
- **`getBool` coerces any non-zero number to true** (`android/kotlin-test/com/klarvo/voice/VadGateGoldenVectorsTest.kt:99-104`). The hand-rolled parser maps `true`/`false` to `Num(1.0)`/`Num(0.0)`, and the accessor is `v != 0.0`, so a fixture entry with `expected_gate_open: 2` or `0.5` silently reads as `true`. Hypothetical fixture-authoring guard. Fix shape: `require(n == 0.0 || n == 1.0)`. LOW. Source: Edge Case Hunter (7-8 review round 2).
- **`optString("signal")` returns its default when the key is present but not a string** (`android/kotlin-test/com/klarvo/voice/VadGateGoldenVectorsTest.kt:251`). A numeric or boolean `signal` value silently selects `nyquist_square` instead of reaching the `else -> error(…)` arm, so the wrong generator is substituted rather than the vector failing. Same class as the one above. Fix shape: distinguish absence from a type mismatch. LOW. Source: Edge Case Hunter (7-8 review round 2).
- **D1's own citation no longer resolves after D1 was applied** (`_bmad-output/implementation-artifacts/7-8-parity-net-close-out-and-twin-hygiene.md:800`). The round-1 decision cites `7-2-…md:421-428` for "all eight" round-3 patch items; after the eight dated resolution notes were interleaved, the items span `7-2-…md:422-437`, and the illustrative anchors `:421`/`:428` inside the finding now point at resolution notes rather than the quoted text. Deferred rather than patched because the repo convention is verbatim preservation of round-1 finding text. Fix shape: widen the range or drop it. LOW. Source: Blind Hunter + Acceptance Auditor (7-8 review round 2).

## Deferred from: code review of 7-8-parity-net-close-out-and-twin-hygiene — round 3, scoped re-review of fix round 2 (2026-09-10)

- **An identical unfailable assertion survives twelve lines above the one R2-4 deleted** (`src-tauri/src/llm/mod.rs`, `spec_twin_constants_chunk_join_separator`, the three-chunk case). `assert_eq!(result.text.matches(sep).count(), 2, "three chunks must be joined with exactly two separators …")` sits directly after `assert_eq!(result.text, expected, …)` where `expected = vec!["A".repeat(target); 3].join(&sep.to_string())`. The first pins the string byte-for-byte, so for any separator that is not `'A'` the second is fully determined and can never be the sole failure — the same shape round-1 P8 and round-2 R2-4 were raised to remove. Verified pre-existing: the assertion is present in `00e771d`, i.e. it is not a regression of either fix round, which is why it is deferred rather than patched. Fix shape: delete it, or state at the assertion why the surviving twin is kept. LOW. Source: Blind Hunter (7-8 review round 3).
- **`getBool`'s new "(or it is not a boolean)" advertises a check it does not perform for numbers** (`android/kotlin-test/com/klarvo/voice/VadGateGoldenVectorsTest.kt`, the `getBool` error message). R2-10 asked for `getDouble`'s parenthetical to be mirrored and it was; but the hand-rolled parser stores `true`/`false` as `Num(1.0)`/`Num(0.0)` and the reader is `(it as? Num)?.v?.let { n -> n != 0.0 }`, so a *numeric* value never reaches the error — it coerces silently. The message is accurate for a string or object value and misleading for a number. The coercion itself is already recorded as a round-2 deferred item; this is its wording half. Fix shape: `"(or it is not a boolean or number)"`, or add the `require(n == 0.0 || n == 1.0)` the sibling item asks for. LOW. Source: Blind Hunter (7-8 review round 3).
- **The round-2 deferred and dismissed anchors were already stale when committed** (this file's round-2 section, and the mirrored one-liners in `7-8-…md`). `VadGateGoldenVectorsTest.kt:99-104` (`getBool` is at `:108-113`) and `:251` (`optString("signal")` is at `:260`) both shift **+9** from the same commit's KDoc expansion; `Adr0017BoundaryGuardTest.kt:150-176` shifts **+1**; the dismissed-noise row cites `llm/mod.rs:2360-2371` for the `system_prompt` drive, today `:2357`. Same class as R2-3, one file over — and the very KDoc that moved them declares *"cited by content, not by line — this file's anchors drift"*. Deferred because round findings are preserved verbatim by repo convention (round 2 accepted the identical item for D1's citation). The one exception was patched, not deferred: the M12 entry, whose *argument* died with the deleted `any()`. Fix shape: cite deferred items by symbol name rather than by line. LOW. Source: Blind Hunter + Acceptance Auditor (7-8 review round 3).
- **`EmptyFirstChunkProvider`'s name asserts the positional semantics R2-5 corrected the doc away from** (`src-tauri/src/llm/mod.rs`, struct declaration). R2-5 corrected the doc from *"any chunk made of `a`s"* to *"any chunk that begins with `a`"*, matching `raw_text.starts_with('a')`; the struct is still named `EmptyFirstChunkProvider` while the test comment three lines above stresses that it blanks *"by CONTENT (not by call order, which `join_all` does not guarantee)"*. Cosmetic — the adjacent comment states the choice explicitly. Fix shape: rename, or note in the doc that "First" means first in the input, not first called. LOW. Source: Blind Hunter (7-8 review round 3).
- **Nothing pins that `open_decision: "M12"` is present, though R2-D1 names it the sole carrier of "M12 is open"** (`src-tauri/src/llm/mod.rs`, the M12 test asserts `record_type` on every entry but never `open_decision`). A constant string label also cannot *detect* that M12 stopped being open — that is what the deleted `any(platforms_agree == false)` assertion did. Deferred rather than patched because dropping that assertion was decision D1 option (a), Andi's explicit call, and the replacement code comment is honest that there deliberately is no assertion. Fix shape: if divergence-liveness should be machine-checked at all, assert `open_decision` against the derived columns; otherwise leave it as the record it is. LOW. Source: Edge Case Hunter + Blind Hunter (7-8 review round 3).
- **The ADR-0017 KDoc's "anywhere in production `voice/`" still reads wider than the non-recursive scan** (`android/kotlin-test/com/klarvo/voice/Adr0017BoundaryGuardTest.kt`, "Deliberately allowed" paragraph). The coverage block says the guard covers files **directly under** `android/kotlin-src/com/klarvo/voice/`, so a literal in a subpackage is not covered by "anywhere in `voice/`". Deferred because R2-6 prescribed this exact wording and the sentence immediately cross-references that coverage block; the non-recursive scan is itself a round-1 deferred item. Fix shape: *"anywhere directly under production `voice/`"*, if the two deferred items are ever closed together. LOW. Source: Blind Hunter (7-8 review round 3).

## Deferred from: code review of 7-8-parity-net-close-out-and-twin-hygiene — round 4, scoped re-review of fix round 3 (2026-09-10)

- **Round 3's own deferred anchors into `VadGateGoldenVectorsTest.kt` were invalidated by the same round's code change** (`_bmad-output/implementation-artifacts/deferred-work.md`, the round-3 item *"The round-2 deferred and dismissed anchors were already stale when committed"*; mirrored in `7-8-…md`'s round-3 deferred list). That item prescribes *"cite deferred items by symbol name rather than by line"* and is itself line-cited: it records the corrected anchors *"(`getBool` is at `:108-113`)"* and *"`optString("signal")` is at `:260`"*, both of which shift **+15** when `5128432` inserts the throwing `getString` — today `getBool` is at `:123-128` and `optString("signal")` at `:275`. The story-record mirror cites `VadGateGoldenVectorsTest.kt:111` for the `getBool` error message; `:111` is now inside `getDouble`'s message. Its other three anchors (`Adr0017BoundaryGuardTest.kt:33-34`, `llm/mod.rs:2357`, `llm/mod.rs:2255-2259`) still hold, and the sibling item at `deferred-work.md:384` correctly cites by symbol. Deferred rather than patched: round findings are preserved verbatim by repo convention, and rounds 2 and 3 both accepted the identical shape as a deferred item. Fix shape: restate the two corrected anchors by symbol (`getBool`, `optString("signal")`). LOW. Source: Blind Hunter + Edge Case Hunter + Acceptance Auditor (7-8 review round 4).
- **AC3's twin table still cites a stale Kotlin anchor** (`_bmad-output/implementation-artifacts/7-8-parity-net-close-out-and-twin-hygiene.md:125`). The chunk-join row gives `KlarvoApi.kt:1158-1165` for `fun joinChunkResults(…)`; on today's tree the function opens at `:1182` (`:1182-1189`). Spec-authored text from story creation, untouched by any fix round; the story's own Task 5 records the +13-line shift in that file that caused it, and round 3 already recorded this as its residual. Carried forward unchanged and still accurate as stated. Fix shape: re-anchor by symbol, or update to `:1182-1189`. LOW. Source: 7-8 review round 3, re-confirmed round 4.

## Deferred from: code review of 7-9-desktop-advanced-settings-dead-keys-and-model-ids (2026-09-12)

- **`ADVANCED_DEFAULTS.pasteDelayMs` is 80, Rust `default_paste_delay_ms()` returns 50** (`src/components/AdvancedSettingsPanel.tsx`, `ADVANCED_DEFAULTS`; `src-tauri/src/config/mod.rs`, `default_paste_delay_ms`). The other two overlapping values agree (`silenceThreshold` 0.005, `minRecordingMs` 500). `ADVANCED_DEFAULTS` is what the panel keeps when `get_advanced_settings()` rejects, so the drift can be written back over the user's value. Pre-existing; `pasteDelayMs` is explicitly out of scope per Q1. LOW. Source: Blind Hunter.
- **A rejected `get_advanced_settings()` leaves the panel fully saveable on `ADVANCED_DEFAULTS`** (`src/components/AdvancedSettingsPanel.tsx`, the mount `useEffect`'s `.catch`). The handler logs a warning and sets `loaded = true` without a load-error state, so any subsequent edit saves the whole default block over the real config — now including the empty `llmModel*` overrides, and now hot-reloaded immediately. The `.catch` itself is untouched by this story. Fix shape: a `loadError` state that disables save. MEDIUM. Source: Edge Case Hunter.
- **`update_api_keys` replaces the cleanup provider with DeepSeek regardless of `cfg.llm_provider`** (`src-tauri/src/commands/settings.rs`, `update_api_keys`). A user on `openai` who updates their DeepSeek key has their cleanup provider silently switched until restart. Pre-existing legacy behavior with no caller in `src/` today; this story only threaded the model override through it. (The related *patch* item is that it still constructs the provider itself instead of routing through `cleanup_provider_for`.) LOW. Source: Blind Hunter + Edge Case Hunter.
- **The four TS placeholder literals are a fourth, unlocked copy of the default model IDs** (`src/components/AdvancedSettingsPanel.tsx`, `renderLlmContent`: `"deepseek-chat"`, `"gpt-4o-mini"`, `"claude-haiku-4-5-20251001"`, `"llama-3.3-70b-versatile"`). `test-fixtures/twin-constants-vectors.json` locks the Rust and Kotlin constants but has no TS reader, so the UI can advertise a stale default with every gate green. This story removed the one piece of evidence that the drift is real (`MOCK_ADVANCED_SETTINGS`' stale `llmModelAnthropic: "claude-haiku-20240307"`). Fix shape: extend the fixture net to the TS placeholders, or generate them. LOW. Source: Blind Hunter.
- **Rust `AdvancedSettings` String fields reject an explicit JSON `null`** (`src-tauri/src/config/mod.rs`, `AdvancedSettings`). `#[serde(default)]` covers a *missing* key, not a `null` one, so a hand-edited `config.json` with `"llmModelDeepseek": null` fails deserialization and sends `load_config_reporting` down the `backup_corrupt_config` path the new AC4 test asserts never happens. Pre-existing pattern across the whole struct, not introduced here. LOW. Source: Blind Hunter.
- **An interior newline in a model ID forges a line in `Klarvo.log`** (`src-tauri/src/pipeline.rs`, the cleanup log line). `llm::effective_cleanup_model` trims only the ends, and the UI input is unvalidated, so `"gpt\n4o-mini"` produces an extra log line in the file GATE-4 reads. BYOK-local only, no remote sink. Related to the open decision on model-ID validation. LOW. Source: Blind Hunter.
- **The Kotlin AC4 test asserts `org.json` against `org.json` in 2 of its 4 assertions** (`android/kotlin-test/com/klarvo/voice/LlmModelOverrideConfigTest.kt`, `oldConfigJson_withRemovedDeadKeys_stillYieldsLiveValues`). The `minRecordingMs` and `llmModelDeepseek` assertions correctly drive the production seams (`KlarvoApi.parseMinRecordingMs`, `KlarvoApi.parseLlmModelOverride`) — AC4 is met — but the `silenceThreshold` and `language` assertions call `optDouble`/`optString` directly with no production symbol in between, and the KDoc reasons at length about `readConfig`'s `catch (e: Exception)` without ever calling `readConfig`. Editorial / claim-accuracy, not a coverage hole. LOW. Source: Blind Hunter.
- **`KlarvoAccessibilityService.performEnter` has lost its last caller** (`android/kotlin-src/com/klarvo/voice/KlarvoAccessibilityService.kt`, `performEnter`). The M13 auto-send removal deleted the only invocation. Task 5 explicitly directed leaving it in place and recording it, and the Dev Agent Record does. Revisit if bubble auto-send is revived (Q4 names that as a future story candidate). LOW. Source: Blind Hunter.
- **The Anthropic model input renders unconditionally** (`src/components/AdvancedSettingsPanel.tsx`, `renderLlmContent`). Q6 scoped `llmModelAnthropic` Desktop-only because Android has no Anthropic cleanup provider (drift row H5), but the input carries no `isMobile` guard — the file imports and uses `isMobile` elsewhere. Whether this React panel is reachable on Android was not established in this review. LOW. Source: Edge Case Hunter.
- **A poisoned `cleanup_provider` write lock returns `Err` after the config is already persisted** (`src-tauri/src/commands/settings.rs`, `save_advanced_settings`). The UI reports a failed save for a save that succeeded, and the provider keeps its stale model. Identical ordering to the pre-existing `save_settings` and `clear_api_key`, so it is a house pattern rather than a new defect. LOW. Source: Edge Case Hunter.

## Deferred from: code review of 7-9-desktop-advanced-settings-dead-keys-and-model-ids, round 2 (2026-09-12)

- **The reverse half of P1 is unclosed in both directions** (`src/components/SettingsPanel.tsx`, `handleSave`; `src/components/AdvancedSettingsPanel.tsx`, `handleSave`). P1 stopped the Settings panel from reverting a just-saved model ID, but `silenceThreshold`/`pasteDelayMs` are still written from the stale mount snapshot, and the embedded Advanced panel's own whole-block save reverts those two fields in the other direction. Pre-existing shape, amplified by the new hot-reload. LOW. Source: Edge Case Hunter.
- **P7's guard narrows an accidental repair** (`src-tauri/src/commands/settings.rs`, `update_api_keys`). The previously unconditional rebuild also repaired a cleanup-provider slot left wrong by `update_api_keys`, which replaces the provider with DeepSeek regardless of `cfg.llm_provider` (itself already deferred). With the guard, that repair only happens when one of the four model IDs changes. LOW. Source: Blind Hunter.
- **A failed disk write leaves the hot-reload permanently skipped for that edit** (`src-tauri/src/lib.rs`, `AppState::save_config_locked`; `src-tauri/src/commands/settings.rs`, `save_advanced_settings`). `save_config_locked` mutates the in-memory config before the disk write and returns `Err` on failure, so the command returns before the hot reload; the in-memory block already carries the new ID, so every later save computes `previous == next` and never rebuilds. Requires a disk-write failure; same family as the round-1 "`Err` after the config is already persisted" row. LOW. Source: Acceptance Auditor.
- **`LocalLlmCleanup::model()` returns `""` for a path with no file name** (`src-tauri/src/llm/local.rs`, `LocalLlmCleanup::new`). Unreachable today because `resolve_cleanup_provider` joins a hard-coded `.gguf` filename, but the `TODO(multi-model)` in that arm anticipates a config-driven filename, which would reopen exactly the empty `model: ` log line P3 fixed. LOW. Source: Edge Case Hunter + Blind Hunter.
- **`update_api_keys` discards a loaded GGUF on the Windows `local` arm** (`src-tauri/src/commands/settings.rs`, `update_api_keys`). The legacy command swaps the cleanup provider unconditionally, paying the same multi-GB model reload P7 just guarded on the sibling path. Pre-existing. LOW. Source: Edge Case Hunter.

## Deferred from: code review of 7-9-desktop-advanced-settings-dead-keys-and-model-ids, round 3 (2026-09-12)

- **`is_model_not_found_error`'s `not_found_error` needle fires on any 404** (`src-tauri/src/pipeline.rs`, `is_model_not_found_error`). `not_found_error` is Anthropic's *generic* 404 type (unknown file id, unknown message-batch id, bad path), not a model-specific one, so a non-model 404 whose body could not be parsed into `AnthropicErrorResponse` produces `Model '<id>' not found — check Advanced → Model IDs` and points the user at a setting that is fine. Narrow: when the body parses, `error.message` carries the real text and none of the needles match. Introduced by RES-1 and documented there as the raw-body fallback. Fix shape: require `model` to co-occur with the type. LOW. Source: Blind Hunter + Edge Case Hunter (7-9 review round 3).
- **The `model:` prefix needle is 404-only** (`src-tauri/src/pipeline.rs`, `is_model_not_found_error`). An Anthropic 400 `invalid_request_error` phrased `model: <id>` still degrades to the generic message, so D2's "the warning names the model" does not cover that status. Anthropic answers an unknown model with 404 today, so the gap is theoretical. Fix shape: allow the prefix on 400 as well. LOW. Source: Edge Case Hunter (7-9 review round 3).
- **`llm_provider == "local"` on any non-Windows build silently routes cleanup to DeepSeek** (`src-tauri/src/pipeline.rs`, `resolve_cleanup_provider`). The `"local"` arm is `#[cfg(target_os = "windows")]` and there is no `#[cfg(not(...))]` counterpart and no warning, unlike the STT sibling in the same file which logs *"local STT provider is only supported on Windows and Android; falling back to groq"*. A user who selected offline STT — which forces `llmProvider = "local"` on every platform — has the transcript sent to DeepSeek with no log line saying so. Pre-existing and out of this story's scope; REG-1's fix rests on exactly this fall-through, which is why it surfaced here. Fix shape: mirror the STT arm's explicit non-Windows branch with a `log::warn!`. LOW (behaviour), MEDIUM (expectation). Source: Edge Case Hunter (7-9 review round 3).
