# Conductor Run — 2026-09-19 · Story 13-1 (Epic 13, Parity line over audit #2)

`bmad-conductor` (BMAD 6.13) on branch `conductor/13-1`, cut from `v1-ship` at `f2f3f14`.
Machine: `bmad-build-auto` in a `bmad-story-session` subagent (Opus, xhigh). Run guard acquired;
real device detached. This stage runs no surface smoke and no follow-up review.

## Plan
| Story | Spec checkpoint | Done checkpoint | Surface |
|---|---|---|---|
| 13-1-debug-test-provider-both-twins | **yes** (Andi at Go, 2026-09-19) | no | yes |

Pre-flight notes:
- Epic 13 lives in `planning-artifacts/epics-parity-line-audit-2.md`, not `epics.md`. Its headings
  were moved to the BMAD standard form in `f2f3f14` before the run (generator dry-run: Epic 13
  0 orphans, 0 new keys).
- `adb` / `ssh` / `scp` are not in `permissions.deny` (contract: accepted residual risk). The
  transcript detector runs after every dispatch.
- First epic-story run of `bmad-build-auto` on klarvo: the spec gate also checks that the machine
  compiled the context of the right epic.

## Stories
| Story | Status | Range | Review loops | Patched | Deferred (high) | Follow-up | Warnings | Note |
|---|---|---|---|---|---|---|---|---|
| 13-1-debug-test-provider-both-twins | **parked** — spec `blocked` (`intent gap`) at planning halt | `3980b70..4ff857c` (spec + `epic-13-context.md` only, no code) | 0 | – | 0 (0) | no | `oversized` | see below |

## Parked — 13-1, open questions for Andi
Detectors: no `Unknown skill`; no executed host mutation (the five grep hits are read text in docs,
checked per Bash tool call). `check-head` exit 4 = the machine's own planning commit `4ff857c`,
paths under `implementation-artifacts` only — not a foreign commit.
Spec gate (conductor read): `epic-13-context.md` is the right epic; the intent contract matches the
epic text. The machine's own decisions are recorded in the spec (`### Decisions`): provider value
`debug`, two scenario keys, wire-level injection, STT debug Rust-only (ADR-0017), surface =
Settings → Advanced behind `expertMode`, D6 not served (Weg 2 goes to 13-2), `debug` needs a
licensed/trial device.
The machine halted on two design questions (spec `## Auto Run Result`):
1. May `src/components/settings/FormControls.tsx` (`KSelect`/`KToggle`, Story 8.2) be the binding
   source for hover/focus/pressed/open/disabled, because the canon `klarvo.css` defines none?
2. Is the `.focus-klarvo` always-on focus ring (app-wide, pre-existing) in scope for 13-1 or deferred?
Resume: re-dispatch with both answers in the prompt (the machine opens a `-2` spec).
