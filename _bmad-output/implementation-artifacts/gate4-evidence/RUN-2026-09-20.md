# RUN 2026-09-20 — conductor/13-1 (Fortsetzung von RUN-2026-09-19)

## Plan

| Story | Spec-Checkpoint | Done-Checkpoint | Surface | Notiz |
|---|---|---|---|---|
| 13-1 Debug test provider, both twins | ja (Andi, 2026-09-19) | nein | ja | Re-Dispatch nach geparktem Spec-Gate; die Maschine oeffnet eine `-2`-Spec |

Branch `conductor/13-1` (v1-ship eingemergt: eine `epics.md`, generate-Sperre aufgehoben). Run-Guard: ja.

## Pre-flight

- Tree sauber. Konformitaets-Probelauf: `in_sync true`, 0 Orphans. `Story 13.1:` steht in genau einer Datei (`epics.md`).
- preflight_facts, Canon-Sonde: `klarvo.css` definiert `.select` nur im Ruhezustand und `.toggle` / `.toggle.on`. Die uebrigen Zustaende deckt das ausgelieferte Bauteil `src/components/settings/FormControls.tsx` (KSelect, KToggle, Story 8.2) -> *In order* nach der Response-State-Regel vom 2026-09-20.
- Antwort auf die zwei offenen Fragen aus RUN-2026-09-19 (Andi, 2026-09-20, ueber die Regel-Aenderung b01980b): (1) ausgeliefertes Bauteil verbatim = Quelle; (2) `.focus-klarvo`-Defekt = Vorbestand, deferred (Backlog-Eintrag besteht, Teil von 8-7).

## Stories

| Story | Status | Range | Review-Loops | Patches | Deferred (high) | Follow-up | Warnings |
|---|---|---|---|---|---|---|---|
| 13-1 | **blocked — intent gap** (geparkt) | Basis `d36401c`, Code zurueckgesetzt | 1 | Bau gruen (cargo 737/0, JVM 208/0, npm build, Proxy-Gate 23/23, Inversion rot), dann Revert; Patch `13-1-attempted-implementation.patch` (2634 Zeilen) | 2 (0 high) | nein | oversized |

## Geparkt: 13-1 — zwei Fragen an Andi

1. **Wie stellt ein Geraet den Anbieter auf `debug`?** Die Spec baut zwei Auswahlfelder fuer das *Szenario*, aber keinen Schalter fuer den *Anbieter*. Wege: (a) eigene Anbieter-Zeile(n) in Advanced -> System hinter `expertMode`; (b) `debug` im normalen Anbieter-Picker hinter `expertMode` (widerspricht AC "unsichtbar im normalen Picker"); (c) nur Desktop per `config.json`, Android-H+ fuer 13-2 auf "nur agenten-verifiziert" herabstufen.
2. **`malformed`:** soll das Szenario den Desktop-Mechanismus von D-M2 nachstellen (nicht dekodierbarer Body -> retryable -> Leiter feuert) oder das `ResponseFormat`-Ergebnis der Matrix?

## Conductor-Fehler (festgehalten)

Das Spec-Gate 2a hat die Luecke nicht gefunden, obwohl sie im `<intent-contract>` lesbar war ("selectable on both devices" gegen "the only interactive control this story adds is KSelect, twice"). Pruef-Frage, die gefehlt hat: *Welches Bedienelement loest jedes Akzeptanz-Kriterium aus?* Kosten: ein voller Bau-Lauf (60 min).

## Close

Run-Guard released. 13-1 bleibt `backlog` in sprint-status.yaml. Deferred aus der Spec (beide pre-existing, nicht high): `.focus-klarvo`-Ring (Backlog-Eintrag besteht, Teil von 8-7); Log-Level-Zeile nutzt rohes `<select>`.

## Andis Antworten (2026-09-20, abends) — 13-1 wieder aufgenommen

1. **Weg (a), zwei Zeilen:** zwei eigene Anbieter-Zeilen in Settings -> Advanced -> System hinter `expertMode`, eine fuer `llmProvider`, eine fuer `sttProvider`. `debug` bleibt im normalen Anbieter-Picker unsichtbar. Beschriftung abgeleitet wie bei "Log Level", kein neuer Text.
2. **`malformed` = nicht dekodierbarer Body:** das Szenario stellt den Desktop-Mechanismus von D-M2 nach (retryable, die Ausweich-Leiter feuert), nicht das `ResponseFormat`-Ergebnis.

Re-Dispatch mit Spec-Checkpoint. Spec-Gate prueft diesmal je Akzeptanz-Kriterium den Ausloeser.

## 13-1 — zweiter Lauf (Spec `-2`)

| Story | Status | Range | Review-Loops | Patches | Deferred (high) | Follow-up | Warnings |
|---|---|---|---|---|---|---|---|
| 13-1 | Spec **done**; sprint-status **review** bis zu Andis H+-Check | `11f8381..385bd8e` (4 Commits) | 0 (eine Review-Runde, 36 Findings: 0 high, 11 medium) | cargo 743/0, JVM 209/0, npm build, Proxy-Gate 66/0, Inversion 3/3 rot | 2 (0 high) | **empfohlen, nicht gefahren (Stufe)** | oversized |

- Spec-Gate 2a: je Akzeptanz-Kriterium den Ausloeser geprueft (vier KSelect-Zeilen in Advanced -> System hinter expertMode). Detektoren sauber (305 Bash-Aufrufe, nur Temp-Schreibzugriffe).
- Conductor-Entscheidung (technisch, umkehrbar): sprint-status `review` statt `done`. Grund: die DoD der Story ist Andis Geraete-Check auf Windows und Xiaomi; der Geraete-Test IST der Liefergegenstand eines Enablers. Nach dem Check: `done`.
- Follow-up-Review, benanntes Risiko: zwei der drei zuletzt gepatchten Stellen sind Quelltext-Tripwires per Regex (Kotlin-Reihenfolge, React-Arrays); ein harmloses Umformatieren faerbt sie rot. Keine der drei Stellen lief auf einem echten Geraet.
- Deferred nach docs/backlog.md: JNI-Signatur-Gate (medium), nie gelaufenes groq_jni-Testmodul (low).
- Residual: Windows-Release-Build und Geraete-Smoke nicht gefahren. Run-Guard released.

## 13-1 — H+-Check, Zwischenstand (2026-09-20 abends)

- Builds: Windows `klarvo.exe` = `42b2e10` (frisch bewiesen). Android: Release-APK passt nicht zur
  Debug-Signatur auf dem Xiaomi; statt Deinstallation (Datenverlust) Debug-APK mit der frischen
  `libklarvo_lib.so` ueber die alte App installiert.
- Android, 23:24: `[debug-provider] LLM cleanup scenario=ok`, cleanup 16 ms `(debug)` -- Anbieter
  feuert, Log nennt das Szenario. Voraussetzung war ein Lizenz-Key: ohne Lizenz zwingt der Gate in
  `KlarvoApi.readConfig` jeden Anbieter auf `groq`, auch `debug` (Handy war unlizenziert).
- Offen: Szenario `empty` auf beiden Geraeten (D2 beobachtbar); Windows noch ohne Lauf mit
  `llmProvider=debug`. `debugLlmScenario=empty` kam auf Android nie in der Config an -- Andi weiss
  nicht, ob er den Advanced-Knopf gedrueckt hat; Speicher-Fehler weder belegt noch ausgeschlossen.
- Bedien-Mangel (zweimal Testkosten): zwei Speicher-Knoepfe fuer vier Zeilen (`Save Settings` fuer
  die Anbieter, Advanced-Footer fuer die Szenarien); `Log Level = debug` direkt ueber
  `LLM Provider` wird mit dem Test-Anbieter verwechselt. Design-Frage -> Andi.
- sprint-status bleibt `review`.
