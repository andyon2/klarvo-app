# RUN 2026-09-21 — conductor/13-1b

## Plan

| Story | Spec-Checkpoint | Done-Checkpoint | Surface | Notiz |
|---|---|---|---|---|
| 13-1b Test provider operability | ja (Andi, Go 2026-09-21) | nein | ja | Spec-Gate prueft Config-Form + Migration und den Save-Footer |

Branch `conductor/13-1b` ab `v1-ship` `8ff1a32`. Run-Guard: ja.

## Pre-flight

- Tree sauber. Konformitaets-Probelauf: `in_sync true`, 0 Orphans, 0 neue Eintraege. `Story 13.1b` steht in genau einer Datei (`epics.md`).
- Vorgaenger 13-1: Spec `-2` = `done`.
- Canon-Sonde vor dem Lauf gestrichen (`5e4c92c`, Andi). Ausgelieferte Quellen fuer das Spec-Gate: `KSelect` (`FormControls.tsx`), klebender Save-Footer (`SettingsPanel.tsx`).
- `permissions.deny` ohne `ssh`/`adb` (Vertrag: akzeptiertes Restrisiko); Detektor nach jedem Dispatch.

## Stories

| Story | Status | Range | Review-Loops | Patches | Deferred (high) | Follow-up | Warnings |
|---|---|---|---|---|---|---|---|
| 13-1b | **done** | `1b3621f..cdf8745` | 0 | 38 Findings: 15 patch (5 medium, 10 low), 1 defer, 8 reject, 3 widerlegt; cargo 749/0, JVM 217/0, npm build, Proxy-Gate 59/0, Inversionen RED | 1 (0 high) | **empfohlen, nicht gefahren (Stufe)** | multiple-goals, oversized |

## Spec-Gate 13-1b (Conductor, 2026-09-21)

Geprueft gegen den Epic-Text, nur technisch + umkehrbar -> Re-Entry ohne Mensch: Config-Form (ein Schluessel pro Kette in `advanced`), alte Config ignoriert statt migriert (Epic erlaubt beides; kein toter Anbieter in beiden Zwillingen), ein Save = Advanced-Footer inkl. STT-Slot, Footer `sticky bottom-0` mit unveraendertem Button, FAB per Konstante aus. Jedes Kriterium hat einen Ausloeser.

## Detektoren

Beide Dispatches sauber. Detektor 1 (Text-Grep) schlug bei der Planung auf 5 Mitschriften an: alles geladener Text (Memory-Index, Vertrag), kein ausgefuehrter Befehl. Geprueft ueber die `tool_use`-Bash-Befehle.

## Residuals

- **Follow-up-Review empfohlen, nicht gefahren.** Risiko: der G2-Fix (Reload-Reihenfolge) ist auf Linux nicht falsifizierbar (`cfg!(target_os = "windows")`); nur ein Quelltext-Tripwire haelt ihn. Das Verhalten liegt auf dem Windows-H+-Ziel.
- **Deferred (medium):** JNI-Aritaet 9 -> 8 prueft nichts Ausfuehrendes (kein NDK auf powerhouse). Android-Install nur mit `android-install-debug.sh <ip:port> --full`.
- **`hover` ist im Proxy-Gate nicht ausgeuebt** (Tailwind v4 `hover:` greift in headless Chromium nicht).
- **Surface nicht gesmoked (Stufe).** H+ offen: Andi, Windows + Xiaomi, `Test provider (LLM)` = `empty` in einem Versuch.
- Epic 13 bleibt `in-progress`. Naechste Story: 13-2.
