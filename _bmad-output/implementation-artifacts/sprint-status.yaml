# generated: 2026-05-30
# last_updated: 2026-05-30
# project: klarvo
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
#
# REMEDIATION NOTES (this sprint):
# ================================
# - Source: docs/robustness-audit-2026-05-30.md (Heavy Track only). See epics.md.
# - Sprint order: Epic 1 and Epic 2 are independent surfaces (Rust desktop vs. Kotlin Android),
#   parallelizable. Then Epic 3 (strengthen test net). Then Epic 4 (god-file depth — fenced, last;
#   story 4-1 sequenced after Epic 1, story 4-2 independently deferrable).
# - Heavy-Track epics run with the Test Architect (*risk / *design / *trace).
# - Epic 2 stories are surface-class: real Android build + manual on-device test in the DoD.
# - quick-dev-track findings (ROB-03/06/07/08/10, TEST-06..10, low-sev polish, contested) are NOT
#   pre-populated here. They land in THIS same ledger via bmad-quick-dev's sync-sprint-status step.

generated: 2026-05-30
last_updated: 2026-06-05 # 5-5 code-review PASS (Opus, 3 layers, fix-loop 1 round): AC-4 gap caught+fixed (preset was inert until app restart → now re-reads on panel open). Stays review — Windows smoke DoD open.
project: klarvo
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # Epic 1 — Config & State Persistence Hardening [gated ADR-0015]
  epic-1: in-progress
  1-1-atomic-state-file-writes-via-a-shared-save-atomic-helper: done # NFR-W verified on Windows 2026-05-30: live license-activation replaced existing config.json atomically (2967→3006B, valid JSON), zero temp leak, source=hmac
  1-2-backup-on-corrupt-recovery-in-load-config: done # NFR-W verified on real Windows 2026-05-31: corrupt config.json → app boots on defaults, config.json.corrupt-<ts> created in %APPDATA%\com.klarvo.voice\ with ALL data intact (keys/license recoverable). Build chain had to be unblocked first (silent build failure: .env-injected TAURI_SIGNING_PRIVATE_KEY + createUpdaterArtifacts:true → no key/wrong-pw abort; fixed via createUpdaterArtifacts:false + build-script gating; earlier smoke ran a STALE binary). Code-review PASS (0 crit/high), 10 Linux specs, 528 lib tests
  1-3-single-writer-serialization-for-state-file-saves: done # Code-review PASS 2026-05-31 (3 adversarial layers): all 6 ACs verified, 18 guard-sites + lock-ordering (config_disk_write→config, no inversion) + production write-coverage confirmed clean. 2 patches applied (save_settings guard-scope contention fix; voice_command poison no longer silent), tests hardened (Spec B de-tautologized to observe the disk lock + reverse-order race added). 531 lib tests / 0 fail, clippy clean on touched files. 1 dismissed (false-positive divergence). D1 helper-extraction (compile-enforce the invariant + bind tests to real call sites) deferred → Epic 4 candidate (deferred-work.md)
  1-4-hardened-config-migration-pre-migration-backup-error-propagation: done # Code review PASS 2026-05-31 (3 adversarial layers): 4 patches applied (timestamp collision → migration name in filename; None-arm CWD fix → app_data_dir; literal <ts> placeholder → actual filename returned + embedded; stt-test fixture → hotkeySlots added, backups.len()==1 now correct). 1 decision dismissed (AC-6 gap: read-only dir blocks both backup+save, structural guarantee holds). 4 findings dismissed as noise. 537 lib tests / 0 fail, clippy clean on config/mod.rs | Opus 4.8 re-review 2026-05-31 (Sonnet result distrusted): all Sonnet claims independently verified (98/98 config tests green, config/mod.rs clippy-clean, win-target not installed). 3 further patches applied: (D1) chained-migration backup message reworded "original config"→"a backup … before the migration" (no data loss — keys/license safe in every backup, only wording overstated for migrations 2/3); (P) AC-4 on-disk key-survival now directly asserted in Spec D (was untested); (P) 3 duplicated warning blocks → migration_save_warning() helper. 2 deferred (backup-file GC/retention; win cross-compile gate). 5 dismissed. Stays done.
  1-5-migration-ladder-regression-test-history-db-open-db: done # Code review PASS 2026-05-31 (3 adversarial layers: Blind/Edge/Auditor, Opus 4.8): all 6 ACs satisfied, tests verified green first-hand (26/26 history, 539 total). 4 patches applied to strengthen regression value: (F1) AC-4 now asserts the uuid index is UNIQUE via PRAGMA index_list col 2, not just the name (dedup contract); (F2) row data-survival + COUNT==1 now asserted (the silent-corruption case); (F3) shared OLD_SCHEMA_DDL const + inline file-DB precondition assert (guard was testing a re-declared copy); (F4) is_note/app_name existence now asserted (2 of 5 migrations were unchecked). 9 dismissed (6 Edge verified-safe; unwrap-diagnostic noise; second-open idempotency out-of-scope; v4-nibble relaxation sanctioned by Dev Notes). clippy clean on history/mod.rs
  epic-1-retrospective: done # 2026-06-01 — first retro of the remediation ledger; 4 action items (AI-1 build-freshness hard-gate + AI-2 bind-tests-to-real-paths → Epic 2 DoD/writing; AI-3 keep 6 deferred tracked, boot-warning center = home for 3; AI-4 open Windows gates → next canonical build). Sequencing: Epic 2 → Epic 3 → Epic 4. No epic-update needed (Epic 2 independent surface).

  # Epic 2 — Android Security Guardians [gated ADR-0016] (surface-class: smoke-test DoD)
  epic-2: done # All 4 stories done (2-1..2-4), on-device smoke green 2026-06-01. Retro optional. Next per sequencing: Epic 3 (test-integrity).
  2-1-port-the-hallucination-filter-to-android: done
  2-2-min-length-silence-pre-filter-before-the-groq-stt-call: done # On-device smoke 2026-06-01: all 4 ACs green (TooShort/Silent/Positive + auto-loop ends on silent segment = confirmed acceptable). Code-review PASS 2026-06-01 (3 adversarial layers, Opus 4.8): all 4 ACs PASS, Rust silence_skip/compute_wav_rms parity verified on every reachable path. GATE-2 fix: canonical test mirrored to android/kotlin-test/ (was gen-only/unversioned). 3 patches applied (test-honesty: tautological threshold test → real just-below/just-above asserts; computeWavRms now divides by samplesRead not header-claimed count; sampleCount==0 NaN-guard → Some(0.0) parity), 18 JVM tests/0 fail. 1 decision dismissed (auto-loop terminates on silent/short segment = intended, mirrors empty-audio kill; verify in smoke). 3 deferred (no WAV-magic/channel validation; async-prev TOCTOU pre-existing; gen-mirror [DIAG] drift). 7 dismissed noise. STAYS review — Task 4 on-device smoke (TooShort/Silent/Positive + AUTO-loop check, AI-1 build-freshness) owed before done (surface-class hard-gate).
  2-3-sanitize-paste-text-on-all-android-paths: done # On-device smoke GREEN 2026-06-01: AI-1 build-freshness (scripts/android-build.sh timestamped APK; app has NO in-UI version/About screen — freshness proven by the build+install act + the script's timestamp gate, not by a version number) + positive-path normal dictation (AC-4 regression) confirmed. Code-review PASS 2026-06-01 (3 adversarial layers, Opus 4.8): all 4 ACs satisfied, gen-mirror byte-identical, AI-2 test binding to real KlarvoApi.sanitizeLlmOutput confirmed. 1 patch applied (dc377f3): review found a 4th unsanitized paste path the story missed — cleanupLocal returned raw text on MNN model-load-failure WITHOUT throwing, bypassing the wrapped catch → finalText → clipboard; now sanitizes on all return paths. 54 JVM tests/0 fail. 4 deferred to deferred-work.md (chokepoint refactor; broader control-char set; ANSI malformed-seq; RTL legitimacy — all parity-with-Rust/pre-existing), 6 dismissed.
  2-4-banking-app-blocklist-guards-the-paste-path-not-just-the-bubble: done # On-device smoke GREEN 2026-06-01 (Andi): AI-1 build-freshness (fresh APK via scripts/android-build.sh) + positive-path normal dictation (AC-3) + banking-guard scenario (recording started, switched to banking app pre-pipeline-end → paste blocked, toast shown, app returned to Idle) all confirmed. Code-review PASS 2026-06-01 (3 adversarial layers, Opus 4.8): all 4 ACs satisfied, gen-mirror byte-identical, AI-2 binding to real BankingGuard.shouldBlockPaste confirmed; Blind-Hunter high-claims (data-loss, TOCTOU, thread-safety) independently refuted (saveToHistory@1155 precedes paste; bankingAppActive write@393 is main-looper). 1 patch applied (9c036b6): autoLoopActive=false parity in the guard block (sole terminal path missing it). 58 JVM tests/0 fail. 2 deferred to deferred-work.md (pending auto-send Enter not cancelled on block; banking-detection latency — both pre-existing/out-of-scope), rest dismissed.
  epic-2-retrospective: done # 2026-06-02 — second retro of the ledger (epic-2-retro-2026-06-02.md). First PROVEN learning transfer: Epic-1 AI-1 (build-freshness) + AI-2 (bind-tests-to-real-paths) both applied AND effective — AI-2 caught the 4th unsanitized paste path in 2.3 (dc377f3). 3 AIs: AI-1 parity down-payment in Story 3.3 (shared WAV-RMS fixture + thin Kotlin consumer); AI-2 full cross-platform parity net → deferred-work.md + post-Epic-3 story/ADR-0016 amendment (NOT an Epic-3 scope story); AI-3 gen-mirror resync discipline. Sequencing unchanged: Epic 3 next (lead with 3.3), Epic 4 last. No epic-update needed.

  # Epic 3 — Test Integrity: Close the False-Safety Islands
  epic-3: done # All 4 stories done (3-1..3-4) 2026-06-02, each a code-review PASS. Retro optional. Next per sequencing: Epic 4 (god-file depth, fenced/last).
  3-1-spec-test-the-real-silero-auto-stop-path: done # Code-review PASS 2026-06-02 (3 adversarial layers, Opus; fix-loop 1 round). 540 Rust tests / 0 fail. Extracted process_vad_step seam from recording_thread (TEST-01 closed: 6 dead run_silence_state_machine tests + RMS helper deleted). Review caught a NEW false-safety island: the stop-priority test was tautological (`let fired=false; assert!(!fired)`, never called the seam) — DECISION (a): deleted it, stop-priority documented as a recording_thread loop-ordering property out of the per-chunk seam's scope (not unit-covered). 2 patches: hangover-timing now pinned (premature-fire regression would fail) + mid-pause hysteresis suppression (replaces deleted counter-reset test) + empty/sub-512-chunk no-progress path. 5 spec tests drive the REAL Silero state machine via process_vad_step. clippy clean on touched code (pre-existing type_complexity @702 out of scope). Pure test-integrity → no Windows/Android smoke owed.
  3-2-execute-the-real-feedback-pi-privacy-gate-in-test: done # Code-review PASS 2026-06-02 (3 adversarial layers, Opus; fix-loop 1 round). 544 Rust lib tests / 0 fail. TEST-02 closed: extracted pure build_feedback_payload (the include_dictation privacy gate) out of send_feedback; deleted the tautological test_payload_no_dictation_sample_when_not_requested (built payload manually, never ran the gate); 3 pure gate specs drive the REAL gate. AC-4 inversion empirically verified (flip gate → specs RED). GATE-3 (Andi): un-deferred the one Blind-Hunter finding (send_feedback call-site binding untested) and closed it now — extracted send_feedback_inner (gate→build→POST seam) + 2 wiremock wire specs that pass include_dictation INTO the seam (wiremock 0.6 dev-dep, ADR-0005); empirically verified (flag flip in seam → both wire specs RED). Round-1 fix attempt was insufficient (wire tests bypassed the binding), caught by re-review, corrected in round 2. clippy clean on feedback.rs (only pre-existing METRICS_FILENAME/app_data_dir warnings). 1 defer to deferred-work.md (11-arg builders' slot-safety, low-sev). Pure test-integrity + thin extraction → no Windows/Android smoke owed.
  3-3-spec-test-the-wav-rms-computation-independently: done # Code-review PASS 2026-06-02 (3 adversarial layers, Opus 4.8; fix-loop 1 round). 541 Rust + 59 JVM tests / 0 fail. Shared JSON fixture read at runtime by BOTH Rust parametric spec + Kotlin WavRmsVectorsTest (AI-1 parity down-payment). insta snapshot removed. F2 resolved Option B: float32 Rust↔Kotlin asymmetry lifted into the fixture as first-class data (expected_rms_kotlin:null + divergence_reason), no id-hardcode. F1/F3 comment-honesty patches applied. 1 production change (SilencePreFilter audioFormat guard, float32→null) — behavior-neutral (encodeWav always emits audioFormat=1), GATE-2-ratified, no on-device smoke owed. ~8 low-sev items → deferred-work.md.
  3-4-independent-system-prompt-leak-detection-spec: done # Code-review PASS 2026-06-02 (3 adversarial layers, Opus; fix-loop 1 round). TEST-05 closed: 7 pure specs drive the REAL check_system_prompt_leaked (no production change, no seam needed). All 4 ACs met. Review caught a NEW false-safety island: spec_all_markers_present_always_fails was tautological for AC-3 (all N present → Fail holds at any threshold ≤ N). Fix round 1: added spec_exactly_two_markers_boundary as the by-design AC-3 guard (2 of 3 markers → Fail; 2→3 relaxation flips it RED) + corrected the overclaiming comment; tightened msg.contains('3')→contains("found 3"); added spec_case_insensitive_marker_side (marker-side to_lowercase coverage). Opus re-review verified the inversion empirically (flip >=2→>=3 → boundary+case specs RED, tautological+1-marker stay green). 18 pi_security / 0 fail, 544 lib / 0 fail. 8 deferred to deferred-work.md (judge-hardening/coverage: empty-output branch, substring-in-word collision, false-negative direction, dup-marker double-count, whitespace/Unicode folding, empty-markers footgun, brittle-substring prod design), 4 dismissed. Pure test-integrity → no Windows/Android smoke owed. Epic 3 COMPLETE (3.1-3.4 all done).
  epic-3-retrospective: done # 2026-06-02 — third & FINAL retro of the remediation ledger (epic-3-retro-2026-06-02.md). Durable lesson: review caught a NEW tautological false-safety construct in ALL 4 stories' new specs (same disease as 1.3/1.5/2.2) — the empirical INVERSION CHECK (flip invariant → prove RED) is the one repeatable tool that caught it across all 12 new-test stories of the ledger. Epic-2 AI-1 (parity down-payment) delivered in 3.3 (shared cross-lang WAV-RMS fixture, insta removed). project-context drift-check CLEAN (ADRs 0015/0016, versions match Cargo.toml). DECISION (Andi, revised in retro discussion): all ROB/DIV/TEST risk findings closed (Epics 1-3); Epic 4 was briefly fenced, then RE-OPENED as the next sprint — Andi plans continued feature work, so the behavior-preserving god-file split is worth doing NOW as foundation while the Epic-1+3 test net is fresh (catches refactor regressions). Order: 4-1 (load_config split, unlocks deferred 1.3-D1) then 4-2 (pipeline.rs contracts). Ledger stays OPEN until Epic 4 done; feature work follows. 3 AIs: AI-1 pull inversion-check to writing-time (DoD line — apply in 4.1/4.2); AI-2 full parity net stays backlog/ADR-0016-amendment; AI-3 retro-record discipline.

  # Epic 4 — God-File Depth Refactor (behavior-preserving)
  # NEXT SPRINT 2026-06-02 (epic-3-retro discussion): Epic 4 RE-OPENED as the next sprint (was briefly fenced).
  # Andi plans continued feature work → do the behavior-preserving god-file split NOW while the Epic-1+3 test
  # net is fresh. Order: 4-1 (load_config → migrate_and_normalize, unlocks deferred 1.3-D1) then 4-2 (pipeline.rs).
  # epic-4 DONE 2026-06-03: all 3 stories done (4-1, 4-2, 4-3). The scope-fence is lifted — 4-1/4-2 were
  # created+implemented after the epic-3-retro re-opened Epic 4 as the next sprint. With Epic 4 closed, the
  # entire remediation ledger (Epics 1-4) is complete. epic-4-retrospective remains optional.
  epic-4: done # All 3 stories done (4-1 migrate_and_normalize split, 4-2 pipeline.rs contracts, 4-3 save_config_locked choke-point); behavior-preserving god-file depth refactor complete. Closes the remediation ledger (Epics 1-4). Durable cross-epic lesson: both 4-1 and 4-2 dev-workers shipped tautological/false inversion-check claims in Completion Notes that only code-review caught (Epic-3-retro AI-1's writing-time inversion-check DoD not yet honored by dev-workers) — input for the optional epic-4-retro.
  4-1-isolate-the-load-config-core-into-a-tested-migrate-and-normalize: done # Code-review PASS 2026-06-02 (3 adversarial layers, Opus; fix-loop 1 round). Extracted pure migrate_and_normalize (env-merge + 3 migrations + provider validation/auto-switch/fallback) out of load_config_reporting; persistence moved to the I/O boundary via Vec<MigrationWrite> (each applied with backup_pre_migration_config + save_config). Behavior-preserving — all 3 layers concur, no regression. 13 new pure unit tests. Review caught the recurring false-safety disease (Epic-3 retro AI-1): the 2 Groq-Llama inversion-check tests were TAUTOLOGICAL — auto-fallback independently yields "groq", so deleting the whole block left both green; the dev-worker's Completion-Notes RED claim was empirically FALSE. Fix: openai_api_key discriminator added to both tests; conductor empirically re-verified RED (block deleted → _fires gives "openai"≠"groq" RED; deepseek-guard removed → _skipped gives "groq"≠"deepseek" RED). Provider-enum AC-5 DEFERRED (serde blast radius, rationale in story). 557 lib tests / 0 fail, clippy clean on config/mod.rs. 4 deferred to deferred-work.md (pre-existing carry-overs), 5 dismissed. Pure test-integrity + behavior-preserving extraction → no Windows/Android smoke owed. Unlocks deferred 1.3-D1.
  4-2-tighten-pipeline-rs-contracts-demote-test-only-pub-surface: done # Code-review PASS 2026-06-02 (3 adversarial layers, Opus 4.8; no fix-loop round needed — code clean). Behavior-preserving pipeline.rs refactor: SttPromptPair groups dict_prompt+stt_hint_text (type-enforced consistency); deliver_outcome extracts the ProcessOutcome match arms (NARROW scope per GATE-1 decision — usage/paste/history stay in stop_and_process_pipeline); 5 helpers pub→pub(crate). GATE-1 fired at create-story: epic AC-2 was internally contradictory (prose listed usage/paste/history as moving, but code shows match-block 1499-1544 holds only error-metrics+consume_command_mode; Andi chose NARROW, AC-2 corrected). Edge Case Hunter empty, Auditor all 6 ACs PASS, arm-for-arm verbatim extraction. Review caught a FALSE inversion-check claim in Completion Notes (claimed test_process_audio_* cover deliver_outcome's None paths + would catch a Stopped→Some flip — they call process_audio, never reach deliver_outcome; corrected — the recurring false-safety disease). 557 lib tests/0 fail, clippy zero-new on pipeline.rs. 2 deferred (direct deliver_outcome test needs AppState fixture / named-struct for the 7-tuple). Pure structural refactor → no Windows/Android smoke owed.
  4-3-single-sanctioned-config-write-path-save-config-locked: done # Pulled forward from Epic 4 (scope-fence exception, 2026-05-31) — code-review-1.3-D1 Option 2. Extracted AppState::save_config_locked choke-point; routed all 18 prod config-save sites (corrects 1.3's "17" miscount); save_config→pub(crate); concurrency specs rebound to the real helper. 3-layer review PASS (1 import fix + 2 hygiene patches applied; Blind Hunter Medium = documented won't-fix, property now structural). 531 lib tests/0 fail, clippy clean on touched files (0 new warnings)
  epic-4-retrospective: done # 2026-06-03 — fourth & FINAL retro of the ledger (epic-4-retro-2026-06-03.md). Closes the headline cross-epic thread: Epic-3-retro AI-1 ("inversion-check as a writing-time DoD line") was HONORED in 4-1's story file and STILL failed — both 4-1 and 4-2 dev-workers shipped false RED-claims that only review caught. Sharpened lesson: a self-attested DoD line is not a control; the reviewer's MECHANICAL inversion is (12/12 false-safety constructs caught across the ledger), and the worker's prose RED-claim is itself a process-layer false-safety island (lulls the reviewer into trusting). AI-1 (corrects Epic-3 AI-1): reviewer-inversion is THE codified control + DROP the worker's prose RED-claim from the conductor dev-worker prompt; mutation-CI explicitly REJECTED as over-engineering for a no-user codebase. AI-2 parity net stays backlog/ADR-0016. AI-3 ledger closed → feature work follows. project-context drift CLEAN. 4-2 came in with ZERO fix-loop rounds; GATE-1 caught a contradictory epic-AC pre-implementation.

  # Epic 5 — Live-Cleanup-Preview (FIRST FEATURE EPIC) [L3 feature route; surface-class smoke-test DoD]
  # Planning artifact: _bmad-output/planning-artifacts/epics-live-preview.md (separate from the CLOSED
  # remediation epics.md). Opt-in live preview for Toggle/Hold: pause-triggered raw Groq segments accumulate
  # in an auto-expand FloatingBar panel (Variant 1); the preview NEVER feeds the paste (Variant B — finish
  # path unchanged). 7 detail forks resolved 2026-06-03 (docs/feature-ideas.md "✅ ENTSCHIEDEN"). L3 guards:
  # G-A characterization test before touching code (5-1 AC1); integration in the ACs (surface smoke DoDs).
  # Dependency flow: 5-1 (backend foundation) → 5-2 + 5-3; 5-4 (Regler B) fully independent / deferral seam.
  # Backlog until story files are created via bmad-create-story (→ ready-for-dev). Wave 1 = 5-1.
  epic-5: in-progress
  5-1-backend-pause-triggered-delta-flush-for-toggle-hold: done # Code-review PASS 2026-06-04 (3 adversarial layers, Opus 4.8; fix-loop 1 round). FIRST feature-epic story. 6 patches applied + CONDUCTOR-VERIFIED (not self-attested): P1 AC-7 flush-time offline recheck (mid-recording provider swap to "local" bypassed install-time guard); P2 AC-6 recorder-level mode-scope test (resolves F3 dead-code; NOTE: proxy — asserts fresh-recorder has no preview config, does NOT drive the real Auto/AutoStop pipeline path → real protection is architectural, triple-enforced; full bind needs AppHandle fixture, deferred to 5.2 runtime smoke); P3 AlreadyRecording stale-config-leak guard in maybe_install_preview_flush (Hold path was unguarded); P4 #[allow too_many_arguments] on recording_thread (NEW clippy warning — dev-worker's "clippy clean" self-claim was FALSE, caught by mechanical review; verified 20→19, no new lint); P5 release live_buffer+delta_marker locks before encode_to_wav (cpal-thread stall) + safe_marker.min bound; P6 corrected misleading inversion comment. 566 lib tests / 0 fail. EMPIRICAL INVERSION (Epic-4-retro AI-1, the one real control): AC-2 marker-advance disabled → spec_delta_snapshot_disjoint_union RED; AC-7 offline guard removed → spec_preview_flush_guard_offline_stt RED — both confirmed, files byte-restored. 4 deferred to deferred-work.md (C2 out-of-order/no-backpressure preview flushes → validate in 5.2; cancel-path marker reset; hangover .max(200) clamp; 3 pre-existing clippy), ~9 dismissed (H3 drop-on-fail = by-design throwaway preview; empty-chunk underflow = safe 0..0). Backend-only → no Windows/Android smoke owed; runtime smoke carried by 5-2.
  5-2-frontend-auto-expand-preview-panel: done # Code-review PASS 2026-06-04 (3 layers, Opus): no AC violations + 4 patches (P1 recording-entry livePreview reset; P2 dragRef==null setPosition guard; P3 chunk.trim; P4 overflowWrap). Windows smoke 2026-06-04/05: FUNCTIONAL ACs VERIFIED ("fast perfekt") after 2 smoke-fixes — (S1) set_bar_shape("panel"): region was hardcoded 200x36 → clipped the expanded 220x196 panel + right edge; new round-rect region w/ explicit card radius 14 matched to CSS borderRadius (resolves the white-line defer too); (S2) pill row flex:1 when closed → content re-centred (bordered-wrapper pushed it ~2px low). Root cause of "no chunks" was a DOCS bug: config.json key is camelCase livePreviewEnabled, not snake_case live_preview_enabled (serde silently ignored it) — flipped Andy's config + corrected story doc. Chunk pipeline confirmed working end-to-end. COSMETIC REVISION 2026-06-05 (Andy's 3 tweaks): panel now grows UPWARD above the pill (pill anchored), auto-heights with the text instead of fixed-scroll, screen-capped fallback to scroll; set_bar_shape("panel") reads dynamic inner_size() (region tracks the dynamic height). The misc.rs cfg-windows path is ALREADY Windows-compiled+verified — Andy's screenshot-build showed the dynamic-height box hugging the text (impossible if inner_size region failed). PANEL_WIDTH then 220→320 (220 fragmented flowing text into stubby lines) — TS-only one-liner, NOT yet rebuilt on Windows. AC-2/AC-3 semantics revised (downward-scroll → upward-autogrow) — see story-file "Cosmetic Revision". STILL review: re-smoke the revision on Windows + final review→done sign-off pending; 1 defer remains (5.1-C2 late-chunk race, not yet reproduced on Auto-Loop). Appearance now also user-configurable → new Story 5-5 (backlog). DONE 2026-06-05 — Andy sign-off ("noch nicht perfekt, reicht aber für done"): a deliberate good-enough close, NOT pixel-perfect; residual cosmetic polish consciously accepted, none logged specifically. 1 defer still tracked (5.1-C2 late-chunk race, not reproduced).
  5-3-settings-opt-in-preview-toggle-and-preview-pause-slider: done # Settings-UI for live_preview_enabled (default off, gates the whole feature) + Preview-Pause slider (Regler A) writing preview_pause_silence_secs via sanctioned save_config (ADR-0015). Code-review PASS (3 layers, Opus; 1 patch fixed + AC-5 inversion reviewer-verified RED, 2 deferred). Commits a538d17+a635c70. Windows settings-smoke GREEN 2026-06-05 (Andy): toggle→livePreviewEnabled:true camelCase, slider→previewPauseSilenceSecs, Save-button clears after save (the fixed f32-resync stuck-dirty bug). Surface DoD met.
  5-4-config-send-stop-pause-consolidation: backlog # INDEPENDENT (the deferral seam). One Send/Stop-Pause slider (Regler B) → writes BOTH existing auto_mode_silence_secs + autostop_silence_secs; NO key rename/removal, Android-read parity preserved (NFR3 / ADR-0016 / android_silence_field_divergence). Config-surface → smoke DoD. Can ship, defer, or drop without touching 5-1..5-3.
  5-5-settings-preview-display-form-presets: review # Compact/Comfortable/Wide preset picker in Settings (new AppConfig key preview_panel_form, serde default "comfortable"; FloatingBar FORM_APPEARANCES map replaces hardcoded 320/320). Code-review PASS 2026-06-05 (3 adversarial layers, Opus; fix-loop 1 round). All 3 layers converged on a real AC-4 gap: the bar is a SEPARATE Tauri window that never re-mounts on settings-save → the mount-only getSettings load left a saved preset inert until app restart (story Dev-Note contradicted AC-4; conductor-verified empirically). FIX: FloatingBar re-reads getSettings on the isPanelOpen closed→open transition → preset applies on the next preview open without restart; + PANEL_WIDTH added to the measure useLayoutEffect deps. AC-2 inversion empirically RED ("wide"!="compact"). 568 Rust tests/0 fail, tsc/vite green, clippy no-new. 2 LOW defers → deferred-work.md (bogus hand-edited value: no picker highlight/no self-heal; getFormAppearance fallback untested). STILL review — Windows release build + manual smoke (cycle all 3 presets, comfortable byte-identical to shipped look, persistence across relaunch) owed before done (surface DoD).
