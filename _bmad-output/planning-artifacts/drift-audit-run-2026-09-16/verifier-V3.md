# VERIFIER V3 — LLM cleanup (prompts · chunking · providers/fallback · response parsing · local LLM)

Tree `e9543fe` (v1-ship), read-only. Desktop = `src-tauri/src/`, Android = `android/kotlin-src/com/klarvo/voice/`,
React = `src/components/`. All line numbers re-read on this tree; where a reader's line was off I give the real one.

**Baseline established mechanically (new fact, use it when reading everything below):** the three *cloud*
base prompts are **byte-identical** across platforms. I unescaped the Rust literals
(`llm/mod.rs:159-193` Polished, `:196-226` Verbatim, `:229-254` Chat) and diffed them against the Kotlin raw
strings (`KlarvoApi.kt:1043-1072`, `:990-1019`, `:1020-1042`): 1931 / 1744 / 1133 chars, `diff` empty on all
three. The dictionary sentence, the custom-instruction sentence and the sandwich are identical too
(`llm/mod.rs:126-155` vs `KlarvoApi.kt:917-925`). So in this subsystem the *only* cloud-prompt divergence is
the missing translation section; everything else is local-LLM, chunking, provider-selection or parsing.

---

## 1. Verdicts

| claim id(s) | verdict | severity (mine) | corrected statement (user-visible effect) | Desktop file:line | Android file:line | React gate |
|---|---|---|---|---|---|---|
| **A10 = B4** `outputLanguage` translation | **CONFIRMED** | HIGH | Set "Translate to English" and dictate German: Desktop pastes English, Android pastes German, no error. `outputLanguage` does not exist anywhere in the Kotlin tree (whole-tree grep: zero hits for `outputLanguage`/`output_language`), and `KlarvoApi.cleanup` has no translation parameter at all. | `pipeline.rs:1842-1846` → `chunked_cleanup(…, output_lang)` `:1462`/`:1499` → `llm/mod.rs:140-148` ("Translate the cleaned output to {name}. Output ONLY the translated text."); `language_name` `llm/mod.rs:81-97` | absent: no field in `Config` (`KlarvoApi.kt:140-227`), no param in `cleanup()` (`:982-988`), `appendPromptExtensions` has dict+custom+sandwich only (`:911-926`) | **none** — `settings/LanguageContent.tsx:67-75` ("Translate to", 13 languages `:13-27`); `isMobile` there is layout only. Mounted `SettingsPanel.tsx:869-876`; the `language` category carries no `desktopOnly` (`settings/types.ts:42-47`, filter at `settings/SettingsHome.tsx:26-28`) |
| **A15 = B17** local-LLM cleanup prompt | **PARTIAL** | MEDIUM | Both readers' code facts are right (`cleanupLocal` uses a 5-bullet prompt and never calls `appendPromptExtensions`, so no dictionary / no Cleanup Instructions / no translation / no punctuation table). But the stated effect — "a much weaker prompt" — understates it: Android's local cleanup **cannot run at all** on this tree (two independent blockers, V-LLM-1 and V-LLM-2), so the real outcome is *no cleanup*, reported as success. The prompt divergence is latent behind that. | `llm/local.rs:308` (`system_prompt`) and `:331-332` (`system_prompt_with_translation`) — full prompt incl. dictionary, custom, translation, sandwich; reached through `chunked_cleanup` like any other provider | `KlarvoApi.kt:884` (`buildSystemPrompt(style)` only) + `:896-900` (the 5-bullet prompts); `appendPromptExtensions` `:911-926` never called from `cleanupLocal` | **none** — provider option `{value:"local", label:"Local (Offline)"}` at `settings/RecordingAudioContent.tsx:150`, rendered on both platforms |
| **A16** local-LLM load failure returns raw as success | **CONFIRMED** | HIGH | Local cleanup failure: Desktop withholds the paste (clipboard-only) and names the cause on the card; Android returns the raw transcript **as a successful cleanup** — `llmCleanupFailed` stays `false`, so the normal accessibility paste fires and no toast or degrade message appears. Two return paths do this: the load-failure early return and the outer `catch`. Already recorded in `docs/backlog.md:293-295` and acknowledged in code at `KlarvoOverlayService.kt:201-203`. | `pipeline.rs:1549-1565` (any `LlmError` → `llm_error = true` + `degrade_cause`) → `deliver_text` `:2315-2319` `copy_only`, Ctrl+V never simulated | `KlarvoApi.kt:873-880` (load fails → `return sanitizeLlmOutput(text)`), `:889` (`result.ifBlank { text }`); `KlarvoOverlayService.kt:2170-2179` (catch → raw, flag untouched) → `:2333-2336` `decision.paste == true` | none (same control as A15) |
| **A18 = B20** config-load fallback ladder differs | **CONFIRMED** | MEDIUM | Desktop's *config-load* ladder is deepseek → openai → **groq → anthropic** → openrouter; Android's only ladder is deepseek → openai → openrouter (Groq excluded by AC2/12-1, Anthropic absent). Reachable example: `sttProvider="groq"` with a Groq key, `llmProvider="openai"`, openai/deepseek/openrouter keys empty (reachable by picking OpenAI and later removing the key) → Desktop switches the LLM to Groq and cleans; Android finds no candidate, skips cleanup, pastes raw + toast. **Two corrections:** (a) the switch is **in-memory only** — it is not in `pending_writes` (`config/mod.rs:1303/1337/1355` are the only write sites), so `config.json` keeps the user's value and Android keeps reading it; it *does* get persisted the first time the user saves Settings, because `get_settings` returns the normalized value (`commands/settings.rs:643`). (b) B20's own example (`sttProvider="openai"`) is weak — an OpenAI STT provider needs an OpenAI key, which would itself satisfy candidate 2. | `config/mod.rs:1402-1429`, candidates `:1412-1418`; the Groq-Llama pre-rule `:1387-1396` | `KlarvoApi.kt:283` → `cleanupFallbackCandidates` `:295-314`; the Groq-Llama pre-rule twin `:526-536` | n/a (runtime resolution). Provider picker `settings/RecordingAudioContent.tsx:145-151` disables keyless options but does not clear a stale selection |
| **B21** retryable predicate narrower → no fallback for non-`IOException` | **PARTIAL** | MEDIUM | True only **below the chunking threshold**. `collectChunkResults` (`KlarvoApi.kt:1273-1281`) re-wraps every non-`IOException` cause as an `IOException` whose message carries no `HTTP nnn`, so on the *chunked* path `isRetryableCleanupFailure` returns `true` (status `null`) and the fallback **is** attempted. Only a single-call dictation (<400 UTF-8 bytes) leaks a bare `JSONException` past `e is IOException` and degrades straight to clipboard-only. **Correction to the Desktop half:** Desktop is broader than its own docstring claims — `pipeline.rs:288-295` asserts `LlmError::Request` means "no HTTP response was ever received", but `response.json().await?` (`llm/mod.rs:625`) also converts a *body-decode* failure through `#[from] reqwest::Error` (`llm/mod.rs:39`), so a malformed 200 body IS retryable on Desktop. The docstring is wrong and should be fixed. Desktop *does* skip fallback for `ResponseFormat` / `OutputTruncated` / `EmptyInput`. | `pipeline.rs:299-302`; `llm/mod.rs:625` + `:39`; ladder `pipeline.rs:334-353` | `KlarvoOverlayService.kt:2221` + `:2703-2706`; single-call path `KlarvoApi.kt:1331-1333` → `:1114-1125` | n/a |
| **B11** no key: Desktop clipboard-only vs Android normal paste | **PARTIAL** (the headline scenario is unreachable) | MEDIUM | Both halves of the *code* are as described (`resolve_cleanup_provider` has no key guard and always returns a provider; `resolveLlmProvider` returns `null` and `KlarvoOverlayService.kt:2258-2266` pastes normally with an informational toast). But **"reachable at defaults: a Groq-only install" is wrong**: a Groq-only install auto-selects the Groq LLM on *both* platforms (`config/mod.rs:1387-1396` ≡ `KlarvoApi.kt:526-536`), so both clean. I walked every no-key config: with all five keys empty, STT only works with `sttProvider="local"`, and then Desktop takes the `OfflineRaw` branch (`pipeline.rs:1425-1432`, `is_offline` `:599-601`) — **no LLM call, `llm_error=false`, normal paste** — i.e. Desktop matches Android (Android just adds a toast). With a *bad* (non-empty, invalid) key both platforms end clipboard-only + degrade message. The outcome pair B11 describes is reachable in exactly one shape: a **whitespace-only** key, where Desktop's `is_empty()` accepts it and 401s into clipboard-only while Android's `isNotBlank()` rejects it into "no provider → normal paste of raw text". That makes B11 a consequence of the `is_empty`/`isBlank` drift (A36 = B39), not an independent row; and the reachable-at-defaults divergence in this area is A18 = B20. | `pipeline.rs:255-281` (no key guard), `:1549-1565`, `:2315-2319`; `OfflineRaw` `:1425-1432` | `KlarvoApi.kt:241-284`; `KlarvoOverlayService.kt:2258-2266` (toast "Text pasted without cleanup (no LLM key configured).") → `:2333-2336` paste | n/a |
| **A38 = B1** (empty-`content` half) | **CONFIRMED** | HIGH | An empty `choices[0].message.content` (a real provider behaviour — content-filter refusals, degenerate completions) makes Android paste an **empty field** and write `""` into `history.db` as the dictation; Desktop raises `ResponseFormat("Empty content in response")`, degrades to the raw transcript, withholds the paste and names the failure. Silent loss of the dictation on Android, and Android also `.trim()`s first, so a whitespace-only completion collapses to `""` there while Desktop pastes the whitespace. No config needed — settle severity at HIGH, not CRITICAL (needs a provider anomaly, not a default config) and not LOW (the whole dictation is lost, silently, and persisted as lost). | `llm/mod.rs:642-647` → non-retryable `pipeline.rs:1549-1565` → clipboard-only `:2315-2319` | `KlarvoApi.kt:1119-1125` (`getString("content").trim()`, no emptiness check) → `KlarvoOverlayService.kt:2271-2278` history, `:2322` clipboard, `:2334-2336` paste | n/a |
| **A39 = B1** (`finish_reason == "length"` half) | **CONFIRMED** | MEDIUM | `finish_reason=="length"` → Desktop `OutputTruncated` → raw transcript, clipboard-only, card; Android has no check and pastes the truncated half-sentence. A's "practically unreachable with 350-byte chunks" is too strong: `max_tokens` is 2048 on both (`llm/mod.rs:488` ≡ `KlarvoApi.kt:66`) and a degenerate repetition loop on garbled STT input does reach it from a ~100-token chunk. Rare, not unreachable → MEDIUM. | `llm/mod.rs:638-640` | no check anywhere in `KlarvoApi.kt:1114-1126` | n/a |
| **A40 = B49** trailing-whitespace trim | **CONFIRMED** | LOW | On the successful cloud path Android trims the provider response, Desktop never does: neither `sanitize_llm_output` (`pipeline.rs:2931-2976`, char filter only) nor `strip_stockphrase_ghosts` (`hallucination.rs:181-187`, byte-identical fast path) trims. A trailing newline from the provider is pasted on Desktop only. Both degrade branches trim on neither side. Note the *local* arms are the other way round: `llm/local.rs:247` trims, `KlarvoApi.kt:889` does not. | `llm/mod.rs:642-653` (no trim; `rewrite` at `:707` is the only trimming arm), `pipeline.rs:1573-1574` | `KlarvoApi.kt:1124` `.trim()` | n/a |
| **A41 = B47** trivial-chunk predicate | **CONFIRMED** | LOW | Rust `char::is_alphanumeric` = Alphabetic ∪ {Nd,Nl,No}; Kotlin `Char.isLetterOrDigit` = L* ∪ Nd. A chunk (or a whole input) made only of `Nl`/`No` characters (`Ⅻ`, `①`, `²`) is "trivial" on Android (passed through verbatim) and non-trivial on Desktop (sent to the LLM — i.e. Desktop is the side exposed to the id=3041 meta-refusal leak the guard exists to prevent). Confirmed unpinned: all three vectors in `test-fixtures/chunking-cleanup-vectors.json` are pure ASCII. | `llm/mod.rs:1343-1345`; whole-input guard `:1451-1457` | `KlarvoApi.kt:1143`; whole-input guard `:1327-1329` | n/a |
| **A42** HTTP success predicate | **CONFIRMED** | LOW | Desktop accepts any 2xx (`status.is_success()`), Android only exactly 200. A 201/202/204 reply would make Android throw, and the thrown message carries `HTTP 2xx` so `isRetryableCleanupFailure` classifies it non-retryable → no fallback → clipboard-only + degrade message, while Desktop cleans normally. Wire-level; no chat-completions provider does this. | `llm/mod.rs:613` | `KlarvoApi.kt:1109-1112` | n/a |
| **B48** whitespace-only cleanup input guard | **CONFIRMED (not a divergence)** | LOW | The code asymmetry is real (`LlmError::EmptyInput` at `llm/mod.rs:670/686/701/712`, no Kotlin equivalent) and B's own "unreachable" caveat is correct: `cleanup()` has exactly one caller chain (`cleanupChunked`, verified by grep — `KlarvoOverlayService.kt:2184` and `:2228` are the only call sites) and both the whole-input guard and the per-chunk guard short-circuit whitespace first; `splitIntoChunks` also drops `chunk.trim().isEmpty()` ranges (`KlarvoApi.kt:1214`). No observable behaviour differs → outside ADR-0016 scope. | `llm/mod.rs:670,686,701,712`; `:1451-1457` | none; guards `KlarvoApi.kt:1327-1329`, `:1352`, `:1214` | n/a |
| **A47 = B40** dictionary predicate + blank filtering | **CONFIRMED (extended)** | LOW | Three drifts on one path, not one. (1) inner gate: Rust `!terms.is_empty()` vs Kotlin `!isNullOrBlank()`. (2) outer gate: Rust `if l.is_empty() { None }` (`pipeline.rs:1834-1840`) vs Kotlin `takeIf { it.isNotBlank() }` (`KlarvoOverlayService.kt:2188`). (3) the list itself: Rust `terms.join(", ")` with **no** blank filtering vs Kotlin `.filter { it.isNotBlank() }`. So `{"terms":["A","","B"]}` yields `"A, , B"` on Desktop and `"A, B"` on Android, and `{"terms":["  "]}` injects an empty "preserve these exactly:" line on Desktop and nothing on Android. Reachable only by hand-editing `dictionary.json` — which the module documents as human-editable (`dictionary/mod.rs:8`) while `add_term` trims and rejects empties (`:48-59`). | `llm/mod.rs:126-131`; `dictionary/mod.rs:88-90`; `pipeline.rs:1834-1840` | `KlarvoApi.kt:917`; `loadDictionaryTerms` filter `:387-390`; `KlarvoOverlayService.kt:2188` | none (Dictionary category ungated, `settings/types.ts:59-63`) |
| **A27 = B18** anthropic: no Kotlin arm; key + model rendered ungated | **CONFIRMED (corrected)** | MEDIUM | Desktop has a full Anthropic cleanup provider; Android has none, and the two Anthropic controls render on Android with no consumer. **Correction to B18:** the fall-through site is `KlarvoApi.kt:262-267` (not `:234-253`), and it does **not** unconditionally become DeepSeek — it yields DeepSeek only if `deepseekApiKey` is non-blank, otherwise `null`, and the ladder then runs; with an Anthropic-only install the end state is **no cleanup at all** (raw paste + toast) while Desktop cleans with Claude. The provider picker no longer offers `anthropic` on either platform, so the *provider* is only reachable from a legacy/hand-edited `config.json`; the *key field* and the *model-ID field* are reachable on Android today. Recorded as an accepted deviation (ADR-0016 Amd 1) at `KlarvoApi.kt:77-80`, `:222-223`, and asserted desktop-only by `TwinConstantsVectorsTest.anthropicModelEntryIsDeclaredDesktopOnly`. | `pipeline.rs:231-234`, `:260`; `llm/mod.rs:1029-1031` (`claude-haiku-4-5-20251001`), impl to `:1290` | absent; `KlarvoApi.kt:262-267` (`else ->` = deepseek-or-null), documented `:77-80`, `:222-223` | **none** — Anthropic key `settings/AiProvidersContent.tsx:149-171`; "Model: Anthropic" `AdvancedSettingsPanel.tsx:329-332`; picker without an anthropic option `settings/RecordingAudioContent.tsx:145-151` |
| **A36 = B39** blank check on the Groq-LLM auto-select | **CONFIRMED (extended)** | MEDIUM | Both readers cite only the DeepSeek half and both cite it a few lines off. The rule has **two** key predicates and they drift in **opposite directions**: `deepseek_api_key.is_empty()` vs `deepseekKey.isBlank()` (whitespace-only DeepSeek key → Desktop does not auto-select Groq, Android does), and `!groq_api_key.is_empty()` vs `groqKey.isNotBlank()` (whitespace-only Groq key → Desktop auto-selects Groq and 401s, Android does not auto-select and ends with no cleanup). Keys are parsed untrimmed on Android (`KlarvoApi.kt:444-445`), so the divergence is not neutralised at parse time. This same `is_empty`/`isBlank` drift governs every provider-key gate and is what makes B11's outcome pair reachable. | `config/mod.rs:1387-1391` (deepseek `:1389`, groq `:1390`) | `KlarvoApi.kt:526-531` (deepseek `:529`, groq `:530`) | n/a |

---

## 2. Recall sweep (new rows)

| id | verdict | severity | statement (user-visible effect) | Desktop file:line | Android file:line | React gate | confidence |
|---|---|---|---|---|---|---|---|
| **V-LLM-1** | new | HIGH | **Android's local-LLM native library is not in the APK.** `LocalLlmInference` loads `libklarvo_mnn` in its `init`; nothing builds or ships it — the only CMake config is `android/jni/CMakeLists.txt`, it hard-codes `MNN_ROOT "/tmp/mnn"` for `libMNN.so`/`libllm.so`, and no tracked Gradle file has an `externalNativeBuild`/`ndk` block (`src-tauri/gen/android/app/build.gradle.kts` is generated and has none; `jniLibs/arm64-v8a/` contains only `libc++_shared.so` and `libklarvo_lib.so`). So `nativeAvailable` is permanently `false`, `load()` returns `false`, and `cleanupLocal` returns the raw transcript **as a success**: choosing "Local (Offline)" cleanup on Android silently means *no cleanup*, while Desktop runs llama.cpp. This is the root cause behind A15/A16/B17. | `llm/local.rs` (llama.cpp, `llm/mod.rs:25-26` `cfg(windows)`) | `LocalLlmInference.kt:26-35`, `:51-55`, `:83-87`; `KlarvoApi.kt:873-880` | none — `settings/RecordingAudioContent.tsx:150` | high |
| **V-LLM-2** | new | HIGH | **Second, independent blocker: the MNN model directory does not match.** The Rust Android downloader writes the bundle to `app_data_dir()/models/qwen2.5-1.5b-mnn`, and `app_data_dir()` on Android resolves to `context.dataDir` — that premise is load-bearing and device-proven elsewhere in this app (`config.json` and `history.db` are read from `getDataDir(context)` and are stated as such at `KlarvoApi.kt:357-359`, `:435`). But `cleanupLocal` looks in **`context.filesDir`** (= `dataDir/files/…`), a different directory. Even with V-LLM-1 fixed, `load()` would fail and the raw transcript would be returned as success. The whisper twin hedges by probing both dirs (`KlarvoOverlayService.kt:2642-2658`) — with a comment that contradicts `KlarvoApi.kt:357-359` about where `app_data_dir()` points — the LLM path does not hedge. | `commands/llm_model.rs:453-459` (`app_data_dir/models/qwen2.5-1.5b-mnn`), `:408-422` | `KlarvoApi.kt:874-875` (`context.filesDir`), vs `:361-367` `getDataDir` | none — `LlmModelManager.tsx` mounted ungated at `settings/RecordingAudioContent.tsx:131` and `:158` | high (code) / med-high (the `app_data_dir()==dataDir` premise, corroborated by config.json + history.db) |
| **V-LLM-3** | new | MEDIUM | **Android's local cleanup bypasses chunking and the trivial-input guard entirely.** Desktop routes *every* provider, local included, through `chunked_cleanup`, so the local arm gets the `is_trivial_chunk` whole-input guard, the 400-byte threshold, sentence splitting and the trivial-fragment fold. Android calls `cleanupLocal(transcript)` directly with the full text and no guard, so a content-free transcript (a lone `.`) would be handed to the model and its conversational meta-refusal would become the user's pasted text — the exact history-id=3041 class the guard was written for — and a long dictation goes unchunked. Latent today behind V-LLM-1/V-LLM-2. | `pipeline.rs:1456-1464` (one `chunked_cleanup` for all providers); guards `llm/mod.rs:1451-1457`, `:1405-1428` | `KlarvoOverlayService.kt:2171`; `KlarvoApi.kt:871-890` (no `isTrivialChunk`, no `shouldChunk`, no `splitIntoChunks`) | none | high |
| **V-LLM-4** | new | MEDIUM | **"Cloud STT + Local cleanup" resolves three different ways.** The picker allows `sttProvider != "local"` with `llmProvider == "local"` (`RecordingAudioContent.tsx:136-159`). Desktop: llama.cpp on-device. Android **bubble/overlay** path: `cleanupLocal` → inert raw text (V-LLM-1/2). Android **in-app button / pending reprocess** path: that runs the Rust pipeline inside `TauriActivity`, where the `"local"` arm of `resolve_cleanup_provider` is `#[cfg(target_os = "windows")]` and `llm::local` is too, so it falls through to `_ => deepseek` and **sends the transcript to the DeepSeek cloud** — under a setting whose whole promise is on-device. One setting, three outcomes, no error on any of them. | `pipeline.rs:263-277` (`cfg(windows)` arm) + `:279`; `llm/mod.rs:25-26`; consumers `commands/recording.rs:287`, `commands/history.rs:226` | `KlarvoOverlayService.kt:2168-2179` | none — `settings/RecordingAudioContent.tsx:150` | high (code) / med (needs the in-app button or a pending retry) |
| **V-LLM-5** | new | LOW | **Android never sanitises a trivial passthrough.** `cleanupChunked` returns the input unchanged on the whole-input guard (`return text`, not `sanitizeLlmOutput(text)`), and `KlarvoOverlayService` assigns the result straight to `finalText` with no post-join sanitise — so on that path bidi overrides / zero-width characters reach the clipboard and the accessibility paste. Desktop sanitises the assembled cleanup output unconditionally, after every branch. Practically unreachable (a zero-width-only transcript would have to survive the hallucination/empty guards) but it is the one sanitisation gap left on the Android cleanup path. | `pipeline.rs:1573` (`sanitize_llm_output` on every branch) | `KlarvoApi.kt:1327-1329` (unsanitised early return), `:1352` (per-chunk passthrough); `KlarvoOverlayService.kt:2184-2194` (no post-join sanitise) | n/a | high (code) / low (reachability) |
| **V-LLM-6** | new | LOW | **`customPrompt` blank/trim tables drift in both directions.** Rust `p.trim().is_empty()` follows Unicode `White_Space`; Kotlin `isNullOrBlank()`/`trim()` follow `Character.isWhitespace ∪ isSpaceChar`. The two tables disagree on U+0085 (Rust blank, Kotlin not) and U+001C–U+001F (Kotlin blank, Rust not), so a control-character-only Cleanup Instruction injects an "Additional user instructions:" section on exactly one platform. This is the *same* table drift that `effectiveCleanupModel` was explicitly rewritten to neutralise (`KlarvoApi.kt:104-120` documents it, and filters `< 0x20` plus U+0085 *before* trimming) — but `customPrompt` gets no such filter on either side, so an interior control character also travels into the system prompt, the provider request and the log verbatim, which is the log-forging concern that filter exists for. Reachable by hand-editing `config.json`. | `llm/mod.rs:133-138`; `pipeline.rs:1826-1831` | `KlarvoApi.kt:920-922`; `:478`; contrast `:131-134` | none — `settings/AiProvidersContent.tsx:204-213` (disabled when `!isPaid`) | high (code) / low-med (reachability) |

Deliberately **not** filed as new rows (checked, parity holds): `CHUNK_THRESHOLD`/`CHUNK_TARGET_SIZE` (400/350 both); `shouldChunk`'s `>=` vs Rust's `<`-then-single-call (equivalent); the boundary-search loop, the UTF-8 boundary floor (`isUtf8ContinuationByte` bit test vs `is_char_boundary`), the ASCII-whitespace skip, the trivial-fragment backward *and* forward folds, and `joinChunkResults`' separator suppression (all faithful twins); abort-on-first-error ordering; `temperature` 0.3 / `max_tokens` 2048; all four base URLs and all three default model IDs; `effectiveCleanupModel`; request-body shape; 15 s connect / 30 s read timeouts; `default_cleanup_style()` = Polished ≡ Kotlin's `optString("cleanupStyle","polished")`. The 4-thread Kotlin pool vs Rust's unbounded `join_all` changes latency only, and the "fallback provider succeeded" message asymmetry (Desktop silent, Android toasts) is already in both readers' degrade matrices.

One doc defect worth a one-line fix outside the audit: `test-fixtures/m12-dictionary-scope-vectors.json` (`M12-DICT-SCOPE-VERBATIM`) says "Verbatim is the product default cleanup style" — it is Polished (`config/mod.rs:848-850`), and the same file's claim that "the two platforms phrase [the dictionary sentence] differently" is no longer true (byte-identical, see the baseline above).

---

## 3. Out-of-subsystem claims (not verified)

- None of the 14 assigned claims fell outside the subsystem. B11 reaches into paste delivery, but its cause is provider resolution, so I verified it.
- **B14** (`is_offline` has no Android equivalent → "Offline" still sends every transcript to the cloud LLM) was **not** assigned to me and I did not verify it — but two of my findings bear on it and whoever holds it should see them: (a) `is_offline` is `stt=="local" && llm!="local"` (`pipeline.rs:599-601`), which is what makes B11's no-key scenario unreachable; (b) `commands/recording.rs:102-109` uses a *different* offline test (`stt_provider == "local"`, no LLM clause), so the Rust in-app path and the Rust hotkey path already disagree with each other on `stt=local + llm=local` — relevant to V-LLM-4.

---

## 4. Reader quality note

Reader **B** was the more precise of the two in this subsystem: its `file:line` citations landed within a line or two (`C:1412-1418`, `L:126-131`, `KOS:2703-2706`, `KAPI:1143` all exact), and it flagged its own reachability caveats honestly (B48 "unreachable", B1's chunking caveat, and §5 item 7 on anthropic reachability). Reader **A**'s claim *content* was equally sound but its line numbers drift systematically 2–5 lines low on `llm/mod.rs` and `config/mod.rs` (A36 cites `cfg:1392`, which is the assignment, not the predicate at `:1389`; A42 cites `llm:617` for `:613`; A47 cites `llm:129-133` for `:126-131`), which looks like citing the line it *read to* rather than the line that decides.

The one systematic error shared by both: **neither validated reachability against the config-load normalisation before assigning severity.** B rated B11 "high, reachable at defaults" and B1 CRITICAL; A rated A38/A39 LOW. Walking the five-key matrix through `migrate_and_normalize` (`config/mod.rs:1387-1429`) and the `OfflineRaw` branch shows B11's headline scenario cannot occur at all, while A's LOW on the empty-`content` half understates a silent whole-dictation loss that is persisted to `history.db`. My two most consequential corrections are therefore **B11 → PARTIAL/unreachable-as-stated** (the reachable divergence there is A18 = B20, amplified by the `is_empty`/`isBlank` drift of A36 = B39), and **the discovery that Android's local-LLM cleanup cannot execute at all** (V-LLM-1, V-LLM-2) — which both readers missed and which reframes A15/B17 from "weaker prompt" to "no cleanup, reported as success". A third correction worth carrying forward: `is_retryable_llm_error`'s docstring (`pipeline.rs:288-295`) is factually wrong about `LlmError::Request`, and B21's conclusion holds only below the 400-byte chunking threshold.
